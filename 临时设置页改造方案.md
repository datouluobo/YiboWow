# Core 系列设置页改造方案（临时实施规格）

## 目标与边界

本轮将 Yibo 系列设置统一到 `YiboCore` 的设置工作台，减少层级和重复角色清单，并让已安装插件数量从 0 到全部时都保持稳定布局。

不包含无障碍专项改造；性能、响应式和控件生命周期属于本轮验收范围。业务插件独有能力仍由各插件保存和校验，Core 只承担统一外壳、共用设置、入口和字段显示。

## 信息架构与归属

固定左侧导航依次为：`窗口`、`角色与排序`、`显示与入口`、按技术名字母序排列的已注册业务插件。

`窗口` 只包含窗口尺寸、位置和重置操作。`角色与排序` 只有一份角色总表：短名、显示/隐藏、置顶与自定义顺序、缓存管理；其下附角色过滤规则。不得为每一种角色设置重复列出角色名。

`显示与入口` 是所有共用显示能力的唯一入口：Core 的 Broker/小地图模式、业务插件页面显示、独立入口模式、主表字段和悬停预览字段。Core 的 Broker/小地图不得放在窗口页或插件页。

每个业务插件页只显示其独有的 `业务设置` 与（确有需要时）`数据与缓存`。不再复制页面启用、入口、字段、悬停字段或通用角色排序。

## 页面布局规则

设置工作台固定左侧导航、右侧内容，导航、页面标题与底部关闭操作不滚动。内容超出安全高度时，仅右侧内容纵向滚动；禁止横向滚动。

页面可在足够宽度时采用两栏，但响应式的最小单位是完整的语义组：标签、说明、控件及其紧邻状态必须一起换列或换行。不得为了自动分栏将说明留在一行、按钮或下拉框落到下一行。

`显示与入口` 的业务区域采用“插件总表 + 当前选择插件详情”：总表每个已注册插件一行，展示页面状态、独立入口模式与“配置字段”动作；选择一行后，在下方显示该插件的主表字段与悬停字段；未选择时显示引导，不渲染所有插件的字段矩阵。没有插件时展示空状态。

独立入口模式使用一个共享下拉菜单：`不显示 / 仅 Broker / 仅小地图 / 两者都显示`。这避免了多状态循环按钮、重复字段清单和 0/1/N 插件时的失衡布局。

## 数据与迁移

账号设置新增 `settings.accountView.entry.coreMode`，值为 `none`、`broker`、`minimap`、`both`，是 Core 自身入口的唯一真相。保留既有 `entry.broker.show` 与 `entry.minimap.show` 作为兼容字段；迁移时由其推导 `coreMode`，后续保存时同步兼容字段。

新增版本 9 数据库迁移。迁移必须幂等，缺少旧字段时默认 `both`，不丢弃任何现有页面入口模式、字段、角色排序或窗口位置。

新增插件目录（`AddonCatalog`）和运行时状态解析器（`AddonStatus`）。目录声明 Core 子插件与独立插件、图标、说明和可选项目链接；运行时读取安装版本、启用状态和已连接（已注册）状态。状态只允许为：`未安装`、`已安装但未启用`、`已启用尚未连接`、`已连接`。

## 组件与页面改造

- `Theme`：新增可复用下拉控件，支持打开/关闭、选项列表、当前值、回调与释放；不依赖桌面程序式无障碍焦点状态。
- `AccountView`：拆分设置工作台的渲染和控件复用；修复行控件的“控件类型”与“视觉变体”共用字段而导致的重复创建。旧控件在翻页/换页时必须隐藏并解除回调引用。
- `Entry`：以 `coreMode` 决定 Core Broker 和小地图入口；业务插件独立 Broker 不得被 Core 自身 Broker 开关误伤。运行中无法可靠注销已注册的 Broker 时，界面提示“重载界面后完全生效”。
- `About`：使用目录、打包版本清单和运行时状态生成，不再在 UI 中手写各插件版本。
- 业务插件：删除或停止渲染已迁入 Core 的通用区块，仅保留业务设置与业务缓存动作。

## 打包版本清单

Core 打包脚本在两个临时 staging 目录中生成 `Data/PackagedAddonVersions.lua`：扫描项目根目录下的 Yibo 插件 `.toc`，读取版本并对同一插件的多个渠道 TOC 做一致性校验。

生成文件只注入 staging 的 `YiboCore.toc`，不写回工作区源码；Core 关于页将其标为“Core 打包时版本”，不得表述为在线“最新版本”。任一 TOC 版本不一致、生成文件缺失或未写入两个包时，打包失败。

## 推荐实施顺序

1. 更新本文档、`AGENTS.md` 与 Core UI 契约。
2. 新增 schema 迁移与 `coreMode`。
3. 新增插件目录、运行时状态解析与打包版本清单注入。
4. 新增共享下拉控件并修复设置控件生命周期。
5. 拆分设置工作台，先完成窗口、角色与排序、显示与入口。
6. 清理业务插件中的重复通用设置。
7. 重构关于页。
8. 扩展打包脚本的校验并完成验收。

## 验收场景

- 旧账号数据库升级后，Core 入口可正确映射到四种模式，现有业务入口与角色设置不丢失。
- 角色管理页面只存在一份角色清单；过滤规则在同页，不产生第四个 Core 导航项。
- `显示与入口` 在没有、只有一个、以及全部业务插件注册时均无横向滚动、无空洞，字段只显示当前选择插件的详情。
- 窄窗口下完整语义组整体转为单栏，说明和下拉框/按钮不被拆开。
- 连续切换设置页、折叠列表与刷新数据后，控件数量稳定，旧控件不可见且不再响应。
- Core Broker 关闭时，业务插件仍可使用自己配置的 Broker 独立入口；小地图和 Broker 预期在重载界面后完全同步。
- 关于页能区分四种安装/启用/连接状态，并同时显示本机 TOC 版本与 Core 打包时版本。
- 两种 Core 发布包均含生成版本清单；扫描和 TOC 一致性校验失败时不产出新包。
