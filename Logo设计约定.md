# Yibo Logo 设计约定

> 适用范围：`YiboCore`、所有 Yibo 系列业务插件，以及项目 README、插件列表、关于页、Broker/小地图入口和发布素材中的 Logo 与图标。
>
> 本文定义品牌层级和 Logo 资产规则。它与 `YiboCore/Docs/UI视觉系统与页面准则.md` 配合使用：Logo 可以保留游戏徽章的材质表现，产品界面本身仍遵循克制、清晰和高密度的 UI 规则。

## 1. 品牌层级

### 1.1 YiboCore 是系列母品牌

`YiboCore` 是 Yibo 系列插件的统一母品牌，代表账号视图框架、窗口壳、角色目录、入口生命周期和共享视觉基础设施。

- 系列总品牌使用 `YiboCore` 的 Logo 与图标作为识别基准。
- 业务插件 Logo 不得取代、削弱或重新定义 YiboCore 的母品牌角色。
- README、项目首页、关于页或系列介绍中，优先使用 YiboCore 母品牌标识；业务 Logo 用于说明具体功能。
- `YiboCore` Logo 的核心识别元素是：六边形结构、中心节点和三向连接/通路。

### 1.2 业务插件是母品牌下的功能单元

业务插件使用“统一外框/结构 + 各自业务符号”的扩展方式：

```text
YiboCore 统一品牌结构
    ├── YiboAltoBoss       + 首领/战斗符号
    ├── YiboLegendary      + 传奇宝石/目标符号
    ├── YiboQuestBlocker   + 任务阻断/警示符号
    └── 后续插件           + 对应业务符号
```

业务符号负责表达插件做什么；统一结构负责表达它属于 Yibo 系列。两者必须同时存在，不能只复制业务图形，也不能只使用没有业务含义的 Core 图标。

## 2. 统一视觉骨架

### 2.1 必须统一的部分

业务插件 Logo 至少应共享以下三项中的两项；正式发布时建议全部采用：

1. **统一轮廓**：优先使用六边形、圆角六边形或由六边形演化出的徽章轮廓。
2. **统一外框**：采用深色金属/深青黑外框，并使用青绿色内描边或能量线作为系列识别。
3. **统一中心结构**：保留中心节点、连接线、三向分支或等价的“网络/索引/聚合”构图。

允许业务插件改变中心图案、内部材质和局部强调色，但不得改变整体徽章化、中心化和高对比的识别方式。

### 2.2 可由业务插件自由表达的部分

- 中心业务符号：首领、任务、路线、宝石、角色、收藏等。
- 内部图形的具体材质：金属、宝石、卷轴、火焰、地图或其它与业务相关的视觉隐喻。
- 业务强调色：在不破坏系列识别的前提下使用红、橙、金等颜色。
- 细节复杂度：大 Logo 可以丰富，小尺寸图标必须主动简化。

### 2.3 禁止事项

- 不得为每个插件重新设计完全不同的品牌外框。
- 不得把文字、版本号、Slash 命令或长名称直接塞进小地图图标。
- 不得使用纯文字作为插件唯一 Logo。
- 不得依赖细小纹理、阴影或发光效果才能辨认主体。
- 不得让业务颜色取代 YiboCore 的系列识别结构。
- 不得把截图、角色头像、地图截图或第三方游戏素材直接作为系列 Logo。

## 3. 色彩语义

### 3.1 系列基础色

| 色彩 | 用途 | 说明 |
|---|---|---|
| 深青黑 | 背景、内衬、主体暗面 | 建立统一的深色游戏界面基底 |
| 青绿色/青蓝色 | 内描边、连接线、中心能量 | 表示 YiboCore、系统连接、索引和可识别的系列归属 |
| 金色 | 徽章边缘、重点装饰、历史/高价值感 | 可作为系列高级感或业务重点，但不应成为所有插件的唯一主色 |
| 红色 | 危险、阻断、错误、警示 | 只在业务语义需要时使用，避免整枚 Logo 被红色吞没 |
| 橙色/亮黄色 | 传奇、稀有、奖励、强目标 | 用于明确的业务语义，不作为 Core 默认色 |

### 3.2 对比和缩小要求

- 主体轮廓与背景必须有明确明度差。
- 业务符号至少有一层清晰外轮廓，避免与内衬融为一体。
- 在 `64×64` 预览时，主体符号仍应能被一眼识别。
- 不使用只有颜色差异才能区分的两个版本；轮廓或形状也必须有差异。

## 4. 资产层级与尺寸

每个系列或业务插件按用途准备三类资源：

| 层级 | 建议尺寸 | 用途 | 规则 |
|---|---:|---|---|
| 主 Logo | `1024×1024` 或更高 | README、官网、宣传图、关于页大图 | 可保留完整材质和细节 |
| 插件图标 | `512×512` | 插件列表、窗口标题、关于页 | 保留统一外框和业务主体 |
| 小地图图标 | `64×64`，必要时提供 `32×32`、`16×16` | Broker、小地图入口、紧凑列表 | 必须是主动简化版本，不是简单缩小主 Logo |

所有 Logo 默认使用透明背景 PNG；需要游戏客户端纹理格式时，另行提供对应格式，不改变 PNG 主资产。

小尺寸版本应按以下顺序保留信息：

1. 外部轮廓；
2. 业务主体符号；
3. Core 系列识别色或连接结构；
4. 材质纹理、细小高光和装饰线。

如果空间不足，优先删除第 4 项，而不是压缩主体符号或外框。

## 5. 构图规则

- 默认采用正方形画布，主体在画布中心对齐。
- 主体四周保留安全留白；不得让尖角、光晕或阴影贴住画布边缘。
- Logo 的重心应稳定，避免业务符号偏向一侧导致小尺寸视觉失衡。
- 统一外框应能在去除内部业务符号后仍被识别为 Yibo 系列。
- 业务符号应尽量使用一个主意象，不把多个独立图标堆叠成信息清单。
- 大图可使用立体高光和纹理；小图应减少渐变层数、细线数量和微小镂空。

## 6. 现有资源的归档与迁移方向

当前项目中的资源可按以下方式理解：

- `YiboCore/Media/YiboCoreLogo-v7.png`：YiboCore 大 Logo，作为母品牌复杂版本的参考。
- `YiboCore/Media/YiboCoreIcon-v13.png`：YiboCore 小图标，作为系列统一结构的主要参考。
- `YiboBeastPaths/Assets/Brand/logo-main.png`：保留为 BeastPaths 的业务/项目主视觉，不再默认视为整个 Yibo 系列母品牌。
- `YiboBeastPaths/Assets/Brand/icon-addon.png`：保留为 BeastPaths 业务图标的简化参考。
- `YiboAltoBoss`、`YiboLegendary`、`YiboQuestBlocker` 的现有图标：可继续使用，但后续重绘或新增版本时应逐步补入统一外框/结构。

迁移期间不要求一次性替换所有现有资源。旧资源可以继续用于兼容和已发布页面；新增 Logo、重绘 Logo 和新的宣传素材必须遵守本文。

## 7. 文件命名与存放

推荐使用以下命名：

```text
<Addon>Logo-v<版本>.png       # 主 Logo
<Addon>Icon-v<版本>.png       # 插件列表/窗口图标
<Addon>MinimapIcon-v<版本>.png # 小地图入口
```

规则：

- 版本号用于表示视觉资产变化，不因复制或改名随意递增。
- 同一视觉版本的不同尺寸使用同一个版本号。
- 资源放在对应插件的 `Media/` 或现有品牌资源目录中。
- 业务插件不得直接引用另一个业务插件目录中的 Logo；共享的 YiboCore 资源应由 YiboCore 提供。
- README 中使用的主 Logo 必须引用本插件明确归属的品牌资源，避免把业务 Logo 误标为母品牌 Logo。

## 8. 发布前检查

新增或更新 Logo 时，至少检查：

- 是否能看出它属于 Yibo 系列；
- 是否能在不看文字的情况下理解业务类别；
- `64×64` 和 `32×32` 下主体是否仍清楚；
- 透明背景放在深色和浅色背景上是否都可辨认；
- 是否存在过细线条、过小文字、边缘裁切或过度发光；
- 是否误用了红色、橙色或金色的业务语义；
- 是否同时更新了主 Logo、插件图标和小地图图标的对应版本；
- 是否没有把 UI 状态色、版本号或调试信息画进 Logo；
- 是否在 README、`.toc`、窗口标题和小地图入口中使用了正确层级的资源。

## 9. 一句话原则

> 先让人认出“这是 Yibo”，再让人认出“这是哪个插件”；统一外框和结构表达系列归属，中心业务符号表达功能差异。
