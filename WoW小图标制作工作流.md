# Yibo WoW 小图标制作工作流

> 适用范围：所有 Yibo 系列插件的游戏内图标、Broker/小地图入口、按钮、列表图标及其配套中图和展示图。
>
> 本文是 **Icon-first**（小尺寸优先）制作流程的唯一执行标准。品牌归属、透明背景和文件命名仍遵循 `Logo设计约定.md`；Agent 通用门禁见根目录 `AGENTS.md`。

## 1. 核心原则

游戏内 `24×24` 和 `32×32` 不是大 Logo 的缩略图，而是独立的 UI 图标资产。先让玩家在 24px 第一眼认出功能，再为中图和展示图增加质感。

固定资产层级：

| 资产 | 尺寸 | 目标 |
|---|---:|---|
| 游戏内小图 | `24×24`、`32×32` | 第一眼可识别；用于 WoW 紧凑 UI |
| 中图 | `64×64`、`128×128` | 在不破坏骨架的前提下增加有限材质 |
| 展示图 | `512×512`、`1024×1024` | 用于 README、发布页与宣传，可增加完整材质和光效 |

小图必须满足：

- 主体占画布面积至少 `85%`，透明外围延伸到画布边缘。
- `24×24` 下第一眼可识别；一个图标只表达一个功能意象。
- 最多 `3–4` 个主要视觉块；小于约 `2×2 px` 的独立细节、缩小后消失的纹理和装饰不画。
- 优先斜向构图、粗暗轮廓、中心亮/外围暗与高明度对比；不依赖真实透视或复杂纹理。
- 保留 Yibo 的统一 frame、深青黑结构和青绿识别线；业务符号负责功能差异。

## 2. 用户确认门禁（不可跳过）

每个阶段完成时，Agent 必须停止。只有用户给出明确确认（如“确认”“可以进入下一步”“选 B”）后才可开始下一阶段。沉默、模糊评价、已读或“看一下”都不是确认。

```text
状态：阶段 N — <阶段名>（待确认）
已完成：<本阶段具体产物与候选/版本>
请确认：<需要用户做出的单一决定>
```

收到确认后，必须按以下格式回复，再开始下一阶段：

```text
已确认：<被批准的具体结果>。
下一步要做什么：<下一阶段的唯一工作与预期交付物>。
状态：阶段 N+1 — <阶段名>（进行中）。
```

返工时不得悄悄替换选择：

```text
状态：阶段 N — <阶段名>（返工中）
已记录：<保留的已确认事实>。
本次只调整：<用户指定的问题>。
```

最终全部导出、实尺寸验收并获得最终批准后，状态才可改为 `已完成`。未确认草案不得覆盖正式资源、写入 `.toc` 或宣称为定稿。

## 3. 固定流程

### 阶段 1：功能语义拆解

把插件功能压缩为一个能被看见的意象，列出：

- 一句话功能，例如“自动打开可安全开启的容器”。
- 一个主主体，例如“掀开的宝箱”。
- 2–4 个不可少的形状，例如“箱盖、发亮箱口、箱体”。
- 必须避免的干扰信息，例如文字、金币雨、场景背景、微小锁扣。
- Yibo frame 和业务强调色的使用理由。

**门禁：**提交语义简报后停止，等待用户确认语义和限制。

### 阶段 2：8–12 个 silhouette 方向

仅制作黑白或双色轮廓方向，每个方向只展示大形状、构图角度和明暗关系。不得在此阶段做材质、细纹、完整大图或品牌宣传图。

- 每个方向先在 `32×32` 网格上判断，再同时给出 `24×24` 预览。
- 每个候选标注编号、主轮廓、视觉块数量和预期识别点。
- 用统一 frame 时，frame 不得抢走业务符号的阅读优先级。

**门禁：**用户明确选定候选（或指定合并方向）后才可进入骨架制作。

### 阶段 3：32px 像素感知矢量骨架

在 `32×32` 网格上建立可编辑的 SVG/Figma 矢量骨架，而非把概念图自动描边。

- 外轮廓建议 `2–3 px`；主要内部切分建议约 `1.5–2 px`。
- 单独出现的点或缺口至少 `2×2 px`；删去小于一个物理像素的线、孔和高光。
- 先锁定主体比例、斜向角度、块面关系和负空间，再考虑色彩。
- 主体覆盖至少 `85%` 画布，且在 24px 不与 frame 或透明边缘互相吞没。

**门禁：**提供骨架的 `24/32` 实尺寸预览，等待用户确认形状；不可提前风格化。

### 阶段 4：实尺寸预览与 WoW UI 模拟

输出同一骨架的 `24×24`、`32×32`、`64×64` 预览，并放入 WoW 深色 UI 的模拟位置（例如小地图按钮、Broker 行、紧凑列表）。

检查：

- 24px 是否不放大即可读出主意象；
- frame、业务符号和高光是否互相挤压；
- 深色 UI 上的暗部是否丢失、透明 Alpha 是否干净；
- 64px 是否只是更清楚，而非换成另一个图形。

**门禁：**用户确认真实尺寸可读性后才可增加 WoW 材质和光效。

### 阶段 5：WoW 风格化材质与光效

只在已经通过实尺寸验收的结构上增加材质：深色金属/深青黑 frame、青绿能量线、业务强调色、有限的大块高光和阴影。

- 材质必须服从已确认的 3–4 个视觉块；不能新增需要小尺寸辨认的物体。
- 光效应服务于边缘分离或中心焦点，不能充当背景、遮盖轮廓或制造方形底色。
- `24/32` 版本可比展示图更克制；同一骨架的视觉语言应一致。

**门禁：**提供风格化实尺寸预览，等待用户确认。

### 阶段 6：像素级修整

用像素级审视而不是继续加细节：检查锯齿、半透明脏边、单像素噪点、断线、对比不足和视觉重心。

- 在 Aseprite 或等效像素编辑器逐像素修正 `24/32` 版本。
- 每一次修整都回看 `24/32/64` 与 WoW UI 模拟；若识别性下降，回退而非补细节。
- 再确认透明区域直达画布边缘，且没有棋盘格、实色底或背景渐变被烘焙进资源。

**门禁：**提交像素级终稿预览，等待最终批准。

### 阶段 7：最终导出

仅在最终批准后导出并接入资源。最少输出：

```text
<Addon>Icon-v<版本>-24.png
<Addon>Icon-v<版本>-32.png
<Addon>Icon-v<版本>-64.png
<Addon>Icon-v<版本>-128.png
<Addon>Icon-v<版本>-512.png
<Addon>Icon-v<版本>-1024.png
<Addon>Icon-v<版本>.tga
<Addon>Icon-v<版本>-master.svg
```

- PNG/TGA 保留 Alpha；TGA 供 WoW 纹理使用。SVG 是可编辑骨架，不替代实际像素验收。
- 若现有插件已有命名约定，兼容该约定并记录尺寸映射；不要无确认覆盖旧的正式资源。
- 导出后重新检查 `24/32/64` PNG、TGA Alpha 与游戏内/模拟 UI；展示图不能作为小图验收凭据。

## 4. 建议工具链及职责

| 工具 | 负责什么 | 不负责什么 |
|---|---|---|
| ChatGPT / 图像生成 | 发散功能意象与 8–12 个概念方向 | 不直接输出可定稿的 24px 资源 |
| Figma | 32px 网格、矢量骨架、统一组件/frame 与尺寸预览 | 不取代像素级检查 |
| Aseprite | 24/32px 像素修整、边缘与 Alpha 检查 | 不增加新叙事元素 |
| Python / ImageMagick | 批量缩放、透明检查、contact sheet、WoW UI 模拟和多格式导出 | 不自行决定构图或风格 |

自动化脚本只可在已确认的骨架/终稿上批处理。它们应生成带尺寸标签的预览和透明通道检查结果，以便用户在每个门禁作出判断。

## 5. Yibo 系列可复用设计系统

每个业务图标先继承同一套设计 token，再替换中心功能符号：

| Token | 系列默认 | 小图执行规则 |
|---|---|---|
| `frame` | 六边形或圆角六边形徽章；深青黑金属外框 | 只保留能在 24px 识别的最少边缘层 |
| `seriesLine` | 青绿色/青蓝色内描边、连接线或能量线 | 使用 1 个连续可读的识别线，不堆叠细线 |
| `outline` | 深色高对比轮廓 | 24/32px 约 `2–3 px`，优先保护主体分离 |
| `highlight` | 中心或朝上的单一大块高光 | 高光是视觉块，不是散落像素点 |
| `shadow` | 外围/下侧深阴影 | 不遮挡主体轮廓，不制造实体背景 |
| `accent` | 依业务语义使用金、橙、红、紫等 | 每图一个主强调色；不能取代青绿系列识别 |
| `alpha` | 画布外缘透明 | 禁止方形底、背景渐变和烘焙棋盘格 |

共享 frame 应以 Figma 组件或版本化 SVG master 保存。业务图标只替换中心符号和允许的业务强调色；若改动 frame、线宽、主色或导出规则，必须先更新本表和相应版本记录。

## 6. Agent 执行清单

1. 读取本文件、`Logo设计约定.md`、根目录 `AGENTS.md`，并检查目标插件现有 `Media/` 命名与资源引用。
2. 建立阶段状态；从阶段 1 开始。不要因已有概念图、SVG 或预览文件跳过用户确认。
3. 每次输出只请求当前阶段的一项确认；不要把下一阶段的候选混在本阶段交付物里。
4. 获批后先总结批准对象，说明下一步，再制作下一阶段。用户可以选择、合并、返工或终止。
5. 在阶段 7 以前，所有资源都放在草案/预览位置或带有明确草案标记；不修改 `.toc`、正式引用或已发布文件。
6. 完成后报告：最终版本、导出文件、实际尺寸检查结果、是否已接入 WoW，以及仍未完成的用户选择。

## 7. 一句话准则

> 先在 24px 让玩家看懂，再在 1024px 让玩家觉得好看；每个阶段先获得明确确认，才进入下一步。
