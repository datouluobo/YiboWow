# Yibo 插件发布前测试清单

> Core 0.6.0 收尾日期：2026-08-20
> 实机状态：用户确认当前游戏实测正常
> 测试范围：YiboCore、YiboAltoBoss、YiboLegendary、YiboQuestBlocker  
> 不在范围：YiboBeastPaths、CurseForge 打包与发布  
> 客户端：魔兽世界经典怀旧服 5.5.4（Interface 50504）  
> 测试方式：游戏内实机操作、完整 `/reload`、插件自检、Lua 语法与 TOC 静态检查

## Core 0.6.0 发布结论

- [x] 用户已确认当前游戏实测正常；Core 0.6.0 的领域采集、账号视图、入口和业务页无阻塞问题。
- [x] Core Public API 为 v5，Database Schema 为 v7；原 v4 页面、入口和角色档案 API 在 0.6.x 兼容期保留。
- [x] 三个业务插件均已升级为要求 YiboCore API v5，并正确声明 `RequiredDeps: YiboCore`。
- [x] 2026-08-20：四个目标插件的 TOC 加载引用检查通过。
- [x] 2026-08-20：`git diff --check` 通过。
- [i] 本机未安装 `luac` / `luac5.1`，未执行 Lua 语法解析；游戏内实测由用户确认通过。
- [x] Slash 命令分别为 `/yco`、`/yab`、`/yle`、`/yqb`，均为未重复的三个英文字母。
- [x] `git diff --check` 未发现空白错误。
- [x] 测试前已备份 8 份 SavedVariables 至 `tmp/release-test-backup-20260813-153322`。
- [x] 本轮没有删除任何文件。
- [x] 原列为“跳过或需人工补测”的游戏内场景已由用户以 90 级角色补测，未发现问题。

当前状态可作为 0.6.0 发布候选；CurseForge 元数据、截图、压缩包和实际上架按要求留到发布时处理。

## CurseForge 分发策略

- `YiboCore`：许可证选择 `All Rights Reserved`，第三方分发选择 `Don't allow distribution to 3rd party`。Core 的唯一官方发布与支持渠道为其 CurseForge 项目页。
- 依赖 `YiboCore` 的业务插件：可选择 `Allow distribution to 3rd party` 以保持第三方渠道的可见性，但发布包不得包含 `YiboCore/` 目录或其文件。
- 每个业务插件的 CurseForge 描述开头须包含必需依赖说明及 Core 的官方 CurseForge 链接，并明确 `YiboCore is not included in this download.`
- 业务插件必须继续在 `.toc` 声明 `## RequiredDeps: YiboCore`；第三方页面指向 Core 的链接不构成 Core 的再分发授权。

## YiboCore 0.4.0（API v4）

### 已通过

- [x] `/yco status` 正常输出版本、API、角色数量、入口状态及业务页登记信息。
- [x] `/yco addons` 正确列出 YiboAltoBoss、YiboLegendary、YiboQuestBlocker。
- [x] Core 初始化正常，Broker 与小地图主入口状态均为启用。
- [x] 统一账号窗口可由 `/yco` 打开和关闭。
- [x] 概览页正确聚合传说任务、Boss 周常和任务屏蔽摘要。
- [x] 角色档案页识别 `30/30` 名角色，长列表显示滚动条，搜索、隐藏和管理控件正常呈现。
- [x] 传说之路、Boss 周常、任务屏蔽三个业务页均能从左侧导航切换。
- [x] 设置页能在通用设置和各业务设置之间切换。
- [x] 窗口手动缩放生效，执行 `/reload` 后尺寸保持。
- [x] 当前服务器/所有服务器范围切换生效。
- [x] 三个业务快速入口资源均成功注册。
- [x] 30 名缓存角色和多个服务器的数据聚合未触发 Lua 错误。

### 补测结果与环境边界

- [x] 小地图/Broker 悬停预览：90 级角色实机补测通过，预览与主窗口一致。
- [x] 战斗锁定期间开关窗口：90 级角色实机补测未发现问题。
- [i] 删除缓存角色：仍未执行；这是破坏性数据操作，不作为发布前常规测试项。
- [i] 极端 UI 缩放、4K、多显示器：当前仅覆盖现有显示环境；不影响本次候选发布结论。

## YiboAltoBoss 2.1.0（需要 Core API v3）

### 已通过

- [x] `/yab` 主命令已注册；Core 中 Boss 周常页面可正常打开。
- [x] `/yab selftest` 返回：`自测通过：1 sample / 180 seconds / idempotent`。
- [x] 当前服务器视图显示 10 个角色列，缓存击杀统计为 `20/60`。
- [x] 所有服务器范围切换后统计为 `20/78`。
- [x] Boss 行、角色列、击杀状态和颜色正常渲染。
- [x] Boss 设置页可打开，页面启用、排序、独立入口和字段设置均正常呈现。
- [x] `/reload` 后插件重新登记且页面数据仍可读取。
- [x] TOC 版本、依赖、SavedVariables 和源码引用一致。

### 补测结果与环境边界

- [x] 真实 Boss 击杀事件与状态刷新：90 级角色实机补测未发现问题。
- [x] Broker/小地图悬停预览：90 级角色实机补测通过。
- [i] 周重置边界、服务器维护和异常掉线恢复：需要跨真实时间窗口观察。
- [i] 20 名以上角色的横向高密度显示：未制造新的第 21 个同范围角色。

## YiboLegendary 0.3（需要 Core API v3）

### 已通过

- [x] `/yle` 能直接打开 Core 统一窗口中的“传说之路”页。
- [x] `/yle probe` 成功执行。
- [x] `questCompletion` 可用，返回函数。
- [x] `questLogIndex` 可用，返回函数。
- [x] `questObjectives` 调用未报错；当前测试角色没有对应任务，因此结果类型为 `nil`。
- [x] `reputation` 调用可用并识别黑王子声望。
- [x] 页面识别 12 名已有业务快照的角色，其中 11 名进行中、1 名可接取。
- [x] 角色、章节、当前任务、目标、行动五列正确渲染。
- [x] 不同章节、任务、目标计数和行动文案能按角色独立显示。
- [x] `/reload` 后插件重新登记且缓存快照仍可读取。
- [x] 修正 `TEST_CHECKLIST.md` 中过期的 Core API v2 描述为 v3，并同步统一窗口入口描述。

### 补测结果与环境边界

- [x] 接取、完成传说任务后的实时状态跃迁：90 级角色实机补测未发现问题。
- [x] Broker/小地图悬停预览：90 级角色实机补测通过。
- [i] 联盟/部落分支、声望门槛放行、服务器阶段开放：未在本次单一角色补测中穷举。
- [i] 第 21 个传说任务角色触发固定表头滚动：当前只有 12 名有效业务角色。

## YiboQuestBlocker 2.0.0（需要 Core API v4）

### 已通过

- [x] `/yqb` 能直接打开 Core 统一窗口中的“任务屏蔽”页。
- [x] 页面识别 2 个全局拒绝任务和 11 名具有业务数据的角色。
- [x] 当前服务器/所有服务器范围控件正常。
- [x] 日常、普通、隐藏已完成筛选控件正常；普通任务筛选可切换并恢复。
- [x] 自动放弃开关、等级阈值、角色矩阵和统计值正常呈现。
- [x] 当前角色任务区域可展开；当前角色没有匹配任务时保持空列表且不报错。
- [x] 设置页可打开，排序、独立入口和业务配置正常呈现。
- [x] `/reload` 后插件重新登记，两个全局拒绝任务和角色矩阵仍可读取。
- [x] TOC 版本、Core API v4 依赖、SavedVariables 和源码引用一致。

### 补测结果与环境边界

- [x] 实际接受被屏蔽任务、自动放弃及规则增删：90 级角色实机补测未发现问题。
- [x] Broker/小地图悬停预览：90 级角色实机补测通过，末列右侧布局正常。
- [i] 日常重置、任务完成后隐藏、等级阈值边界：需要跨重置时间或刻意构造边界数据观察。

## 发布前建议保留的人工冒烟项

以下高等级角色冒烟项已于 2026-08-14 由用户实机补测，未发现问题：

- [x] 鼠标悬停 Core、AltoBoss、Legendary、QuestBlocker 的 Broker/小地图入口，预览表格与主窗口一致。
- [x] 左键四个入口能打开目标页面，右键业务入口能打开对应入口设置。
- [x] 完成真实 Boss 击杀并观察 AltoBoss 状态刷新。
- [x] 接取或推进传说任务并观察 Legendary 当前任务与目标刷新。
- [x] 用测试任务新增 QuestBlocker 角色规则，确认自动放弃后恢复测试数据。
- [x] `/reload` 后未见 Lua 错误，SavedVariables 正常落盘。

## 本轮修复与非阻塞事项

- [x] 已修复：QuestBlocker 矩阵内容宽度曾把全局列按 52px 计算、实际按 40px 绘制，导致最后一个角色列右侧多出 12px；现已统一使用 `GLOBAL_W`。
- [x] 已修复：QuestBlocker 当前服务器角色列由 54px 调整为 72px，中英文角色名统一为单行固定宽度，完整名称由悬停提示保留；主窗口行底色铺满可视区，悬停继续按矩阵内容紧凑显示。
- [x] 已修复：QuestBlocker 悬停预览原先未完整分离矩阵宽度与滚动条占位，最右角色列右侧会出现伪空列或被滚动条覆盖；现为滚动条保留独立 16px 槽位，表头和数据行按精确矩阵宽度渲染。
- [x] 已修复：YiboLegendary 旧测试文档把所需 Core API 写成 v2，实际代码与当前登记均为 v3。
- [x] 已修复：YiboLegendary 旧测试文档仍描述独立窗口标题，已改为 Core 统一账号窗口入口。
- [i] 非阻塞：Legendary 探针的 `questObjectives` 在当前无对应任务的 1 级角色上返回 `nil`，但调用成功且没有兼容性错误；需在持有目标任务的角色上补充内容正确性验证。
- [i] 非阻塞：仓库现有 QuestBlocker `dist` 中包含旧版 CurseForge 构建产物；按本轮要求未处理，发布打包时应重新生成，不应直接复用旧包。
