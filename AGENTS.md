# 项目约定

## 插件 Slash 命令

- 每个插件的主 Slash 命令必须恰好使用三个英文字母，例如 `/yle`、`/ybp`、`/yqb`、`/yab`。
- 新增或修改命令前，必须在整个工作区搜索所有 `SLASH_*` 注册，确认该三字母命令未被其它插件占用。
- 正式命令不得保留旧命令别名；如确需迁移例外，必须由用户明确批准，并在对应插件文档中记录移除计划。

## 插件列表命名与依赖

- 所有插件在 `.toc` 中的 `## Title` 必须使用统一作者前缀 `[Yibo]`，格式为 `|cff20e070[Yibo]|r 插件名称`。
- `[Yibo]` 前缀统一使用青绿色 `20e070`；不要按插件新旧、类别或依赖层级使用不同前缀或颜色。
- 插件的新旧、核心与否均属于作者内部定义，不得出现在插件显示名称的前缀中。
- `YiboCore` 是核心依赖插件。需要依赖核心的插件必须在其 `.toc` 中声明 `## RequiredDeps: YiboCore`，以便插件列表将其归在核心下级。
- 无 `YiboCore` 依赖的插件保持独立显示；显示名称不因其独立性而改变。

## 统一入口与账号视图

- `YiboCore` 负责 Yibo 系列的统一账号视图框架、角色目录、窗口壳、范围切换、表格滚动与入口生命周期；业务插件只注册业务数据、悬停摘要、目标页面和视图布局。
- 默认仅显示 `YiboCore` 的 Broker/小地图入口。需要快速查看的业务插件可由用户逐项启用独立入口，位置可配置为“不显示 / 仅 Broker / 仅小地图 / 两者都显示”。
- 独立入口仍由 `YiboCore` 创建和注销；左键应打开统一框架中的对应业务页面，悬停应直接显示该插件的多角色摘要。插件不得重复实现 Broker、小地图拖拽、位置存档或入口设置逻辑。
- 账号视图不强制统一矩阵布局。业务插件可按数据性质选择角色为行、角色为列或自定义布局；当角色数不超过 20 时，应优先在同一页面稳定展示，超过后再启用固定表头的静态滚动。

## Broker 与小地图悬停规范

- 每个 Yibo 入口的 Broker 与小地图悬停必须复用对应主窗口的账号快照，默认展示全部有效角色（最多 20 名）；不得只展示当前角色而让玩家再打开窗口确认其它角色。
- 悬停必须是正式账号视图的“预览列投影”：复用同一个窗口、行渲染器、状态颜色与角色筛选，只按插件设置隐藏列；不得另建文本式悬停窗口或维护第二套摘要排版。
- 预览列由插件保存于 `settings.previewColumns`；默认列应优先保证玩家能直接判断任务需求，不得机械保留“行动”列。用户可在后续 Core 入口设置中逐列显示或隐藏。完整窗口与悬停预览的任务、目标和行动数据必须完全一致。
- 悬停内容仅显示有该插件业务快照且满足插件准入条件的角色。内容不得包含调试信息、重复说明、无数据角色或纯装饰文本。
- 左键始终打开该插件在统一框架中的主视图（尚未迁移时打开现有主窗口）；由 Core 管理的入口右键用于入口设置。探针与诊断仅通过插件 Slash 命令触发，不占用常规入口交互。
- Broker、小地图与主窗口必须使用同一份摘要生成逻辑，避免任务、行动或角色筛选在不同入口出现不一致。

## 统一设置工作台

- 所有依赖 `YiboCore` 的 Yibo 插件必须把设置纳入 Core 的统一设置工作台；业务插件可以保留设置数据、校验和业务控件，但不得自行维护第二套设置窗口壳、标题栏、关闭按钮、滚动条或窗口位置。
- 设置工作台采用固定左侧导航和右侧内容区。左侧层级固定为：`Core 常规设置`（`窗口`、`角色与排序`、`显示与入口`），随后为各业务插件；切换页面不得改变导航的位置或顺序。
- 业务插件在设置导航、账号页面、入口配置、缓存影响说明及任何其它列表中的顺序，统一按插件技术名的字母序排列；已知顺序为 `YiboAltoBoss`、`YiboLegendary`、`YiboQuestBlocker`。新增插件自动按相同规则插入，不得依赖注册先后或业务页面显示名排序。
- `角色与排序` 是角色缓存删除、角色排序、角色置顶与自定义顺序的唯一入口；`显示与入口` 是所有业务插件的页面显示、独立入口、主表字段和悬停字段的唯一入口。
- 每个业务插件设置页必须在右侧以相同顺序提供适用分区：`页面与入口`、`显示与排序`、`业务设置`、`数据与缓存`。没有内容的分区不显示；插件不得用业务逻辑改变通用分区的位置。
- `页面与入口`、主表字段、悬停预览字段和角色排序由 `YiboCore` 渲染并保存。业务插件只通过注册接口提供业务设置分区；业务数据仍保存在各自 SavedVariables 中。
- 设置页优先采用紧凑的两栏布局；目标或字段等同类开关按可用宽度自动分栏。不得出现横向滚动条、裁切控件或依赖水平滚动才能完成的操作。
- 设置窗口与内容区应先在安全屏幕高度内自适应；只有内容确实无法在安全高度内呈现时，右侧内容区才可使用纵向滚动。导航、页面标题和底部状态/关闭操作必须保持可见。
- 长列表优先采用自动分栏、分页或“展开更多”。角色与缓存管理使用分页，禁止以无限拉长页面或横纵双滚动替代信息组织。
- 破坏性动作必须位于 `数据与缓存` 分区末尾，使用明确风险文案和确认框；不得与普通显示开关混排。

## Logo 与图标资源规范

- 所有 Yibo 系列 logo 与图标的主图外围必须保持透明，不得带实色底、方形背景、渐变画布或其它会破坏透明边界的背景元素。
- 透明区域应延伸到资源画布边缘；棋盘格仅可作为预览工具的透明提示，不得被烘焙进最终 PNG/TGA 资源。
- 生成或修改 logo 时必须保持主体图案不变，优先调整主体比例、配色和细节；不得用底色填充透明外围来模拟图标底座。
