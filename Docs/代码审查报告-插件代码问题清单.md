# Yibo 系列插件代码审查报告（问题清单）

> 审查范围：`E:\Development\YiboWow` 下各 WoW 插件源码（不含第三方 `Libs/`、`dist/` 构建产物）
> 审查维度：UI 重名、内存泄漏、本地化、API 兼容，以及逻辑正确性、性能、项目约定（`AGENTS.md`）符合性
> **本报告只做审查，未改动任何代码。**
> 状态：**已完成全部 8 个插件** —— `YiboTodo`、`YiboBeastPaths`、`YiboAutoOpen`、`YiboQuestBlocker`、`YiboReputation`、`YiboCurrency`、`YiboBuilds`、`YiboMounts`

---

## 一、跨插件共性问题（优先级最高）

### 1. 打印前缀颜色/标签不统一（AGENTS.md 要求 `|cff20e070[Yibo]|r`）

已发现 **9 种**不同前缀：

| 插件 | 前缀 | 问题 |
|---|---|---|
| YiboTodo | `|cff20e070[YiboTodo]|r` | 颜色对、标签缺 `[Yibo]` 规范 |
| YiboBeastPaths | `|cffffcc00[YiboBeastPaths]|r` | 颜色错 |
| YiboBeastPaths | `|cff4fd8ffYiboBeastPaths|r` | 颜色错、无方括号 |
| YiboBeastPaths Minimap | `|cff4fd8ff[YiboBeastPaths Minimap]|r` | 颜色错 |
| YBP 调试 | `|cff4fd8ff[YBP调试]|r` | 颜色错、简写 |
| YiboAutoOpen | `|cff20e070[Yibo]|r` | 合规（但缺插件名） |
| YiboQuestBlocker | `|cff00ccff[YQB]|r` | 颜色错、简写 |
| YiboQuestBlocker | `|cffff0000[YQB]|r` | 颜色错、简写 |
| YiboQuestBlocker | `|cff888888[YQB]|r` | 颜色错、简写 |

### 2. `CopyDefaults` 同类函数被复制 5 份

`YiboBeastPaths/Core.lua:19`、`YiboBeastPaths/Maintenance/DebugCalibrator.lua:114`、`YiboBeastPaths/Maintenance/CoreDebugAdapter.lua`、`YiboAutoOpen`、`YiboQuestBlocker/MergeTables` —— 语义各不相同的"默认值合并"，无共享实现。

### 3. 时钟混用

- `YiboTodo/Providers/SpecialActivity.lua:13` —— 服务器纪元配本地 `date("%Y-%W")`
- `YiboTodo/Providers/SpecialActivity.lua:34/40/58` —— 本地 `date("*t")` 喂给服务器日历
- `YiboBeastPaths/RouteResolver.lua:311`、`BuildResolvedRouteForPet` —— `date("!...Z")` 本地时钟标 UTC
- `YiboQuestBlocker_Core.lua:494`（`time()`）与 `:668/671`（`GetTime()`）—— 两套时钟写入同一诊断集

### 4. 本地化缺失（含**逻辑层**用中文做判断）

- `YiboTodo/Providers/FarmOperationObservation.lua:27` —— `InferPlantType` 中文子串启发式
- `YiboTodo/Providers/FarmOperationObservation.lua:98` —— `"^生长中的"` 匹配
- `YiboTodo/Catalog/DailyActivityCatalog_MoP.lua:29-30` —— `itemName = "烹饪学校大钟"` / `interactName = "诺米"` 用于 `/targetexact` 宏与物品匹配
- `YiboTodo/Providers/SpecialActivity.lua` —— `string.find(title,"暗月")` 判断暗月马戏团
- `YiboBeastPaths/Renderer.lua:28-44/65-68` —— `zoneNameToMapID` / `blockedParentMapNames` 中英双语地图名映射
- **`YiboQuestBlocker_Core.lua:924`** —— `string.find(questName, "产品订单", 1, true)` 判定日常任务，非中文客户端**完全失效且静默**
- **`YiboQuestBlocker_Core.lua:281-291`** —— `levelOp` 用中文枚举 `"以上"/"以下"/"等于"/"至"` 做迁移判断
- `YiboBeastPaths/RouteNodes.lua`、`CuratedRoutes.lua`、`RouteOverlays.lua` —— 中文 `zone`/`titleSuffix`/`trackName`
- `YiboQuestBlocker/AccountPage.lua` 约 20 处中文 UI 文案
- `YiboQuestBlocker/Settings.lua:32` 约 200 字中文长文案
- `YiboQuestBlocker/CoreIntegration.lua:70/71/102` 中文 label/description

### 5. 输入清洗不处理全角字符

- `YiboAutoOpen/ItemResolver.lua:3` `Trim` 只处理 `%s`，不处理 U+3000
- `YiboAutoOpen/Commands.lua:6` 同上
- `YiboQuestBlocker_Core.lua:943` `NormalizeLevelExpr` 同上

### 6. 同一真相多份声明

| 真相 | 重复位置 |
|---|---|
| 14 个冷却组 | `YiboTodo` 的 `ActivityCatalog` / `CooldownGroupCatalog` / `CooldownCatalog_MoP` / `Ruleset_50504`（**4 份**） |
| `mapID` | `YiboBeastPaths` 的 `Data.lua` / `RouteOverlays.lua` / `ReferenceRoutes.lua` / `CuratedRoutes.lua`（**4 份**） |
| 坐标数据 | `CuratedRoutes.lua` 与 `ReferenceRoutes.lua` **逐字节相同**（约 1300 张表 ×2） |
| icon 路径 | `YiboAutoOpen.toc:6` 与 `CoreIntegration.lua:13`；`YiboQuestBlocker.toc:6` 与 `CoreIntegration.lua:85/116`（**3 份**） |
| `previewColumns` 默认值 | `YiboQuestBlocker_Core.lua:158` / `:242` / `CoreIntegration.lua:26`（**3 份**） |
| `filters` 默认值 | `YiboQuestBlocker_Core.lua:143-150` / `:262-269`（**2 份**） |
| 元素清单 | `YiboBeastPaths/DebugCalibrator.lua` 的 `sectionContents` / `sectionLayout.movableElements` / `autoHeightElements`（**3 份**，约 60 个控件各列一遍） |

### 7. 热路径重复重算

- `YiboTodo/Catalog/Validator.lua:107-109` —— `GetActiveRecipes()` 每次调用重跑整张目录五表全量校验
- `YiboTodo/Settings.lua:97-98` —— `GetMonitoringGroupState` 复杂度 O(items × groups × recipes)
- `YiboBeastPaths/Core.lua:124` —— `WorldMapFrame:HookScript("OnUpdate")` **每帧**调 `RefreshMapLayer()`，内部做 Catmull-Rom 平滑 + 建 Frame
- `YiboBeastPaths/MinimapRenderer.lua:675` —— 每 0.05s 全量重建
- `YiboBeastPaths/RouteResolver.lua:687` —— 每次解析重建 `BuildRouteIndex`
- `YiboBeastPaths/DebugCalibrator.lua:2905` —— `WorldMapFrame` 第二个 `OnUpdate`，每 0.25s 调 `GetCurrentWorldMapID()`，面板关闭时仍运行
- `YiboQuestBlocker_Core.lua:538/550` —— `IsQuestBlockedByAny` / `GetBlockStatus` 每次遍历全角色，被**逐任务**调用 → O(任务 × 角色 × 屏蔽)

### 8. 无上限的池 / 泄漏的 Frame

- `YiboBeastPaths/Renderer.lua` —— `vectorLayers` / `overlayFrames` / `routeNodeFrames` 只隐藏不回收
- `YiboBeastPaths/MinimapRenderer.lua` —— `segmentPool` / `footprintPool` 同上；`MINIMAP_ROUTE_SEGMENT_STEP_PX = 1` 导致每像素一个 Frame
- `YiboAutoOpen/Settings.lua:100` —— 目录行 Frame 只 `Hide()`
- `YiboQuestBlocker/AccountPage.lua:115` —— 行 Frame 只 `Hide()`
- `YiboBeastPaths/Maintenance/CoreDebugAdapter.lua:11` —— `notifiedReasons` 永久增长
- `YiboBeastPaths/Maintenance/CoreDebugView.lua:596` —— `view` 单例换父级时泄漏

### 9. 裸全局 / Core 私有访问

- `_G.YiboBeastPathsDB`、`_G.YiboBeastPathsDebugDB`（`YiboBeastPaths`）
- `WorldMapFrame.mapID`、`WorldMapFrame.ScrollContainer.Child`（`YiboBeastPaths/Renderer.lua`）
- `_G.YiboCore.UITheme` 无 nil 保护（`YiboTodo/Settings.lua:155`、`YiboAutoOpen/Settings.lua:3/14`、`YiboQuestBlocker/Settings.lua:3`、`AccountPage.lua:3`）
- `core.AccountView:NotifyPageChanged`、`"addon-settings:" .. SETTINGS_ID`（`YiboBeastPaths/CoreDebugAdapter.lua:38/183`）
- `Core.AccountView:RefreshNavigation/RefreshPage` + `activePageID == "settings"` 私有字段（`YiboAutoOpen/CoreIntegration.lua:18-21`）
- `Theme` 内部字段 `check.box` / `check.label` / `check.mark` / `check.disableHoverAccent`（`YiboQuestBlocker/AccountPage.lua:34-39`）
- 裸 `wipe`（`YiboBeastPaths/MinimapRenderer.lua:271/300`、`YiboQuestBlocker_Core.lua:385/643/1447`）

### 10. 依赖声明与 `.toc` 约定违反

| 插件 | 声明 | 应为 |
|---|---|---|
| `YiboAutoOpen.toc:8` | `## OptionalDeps: YiboCore` | `RequiredDeps`（代码是硬依赖） |
| `YiboBeastPaths.toc` | `## OptionalDeps: YiboCore` | `RequiredDeps`（`CoreDebugAdapter` 要求 Core API v6） |
| `YiboQuestBlocker.toc:8-9` | `RequiredDeps` + `Group` | **合规，可作样板** |

- `## Interface` 不一致：`YiboBeastPaths` = `50400`，`YiboAutoOpen`/`YiboQuestBlocker` = `50504`
- `## X-GameFlavor` 只有 `YiboBeastPaths` 有（`:Mists`），但 `YiboAutoOpen` 同样写了跨版本 API 却无声明
- `## LoadOnDemand: 0` 在 `YiboAutoOpen`/`YiboQuestBlocker` 中是无意义声明
- `## Author` 不一致：`YiboSoft` vs `Reasonix`（`YiboQuestBlocker.toc:4`）
- `## Title` 格式不一致：`YiboAutoOpen - 自动开包` vs 纯 `YiboQuestBlocker`

### 11. 死代码 / 死文件

- **`YiboQuestBlocker/YiboQuestBlocker_UI.lua`（1291 行）不在 `.toc` 加载列表** —— 永不加载
- `YiboQuestBlocker/.tmp-libdatabroker/LibDataBroker-1.1.lua` —— 与 `Libs/LibDataBroker-1.1/` 重复
- `YiboQuestBlocker/dist/` 下 3 套 `1.2.x` 构建产物 vs 当前 `3.0.2` —— 严重脱节
- `YiboBeastPaths/ResolvedRoutes.lua`、`ReferenceRoutePoints.lua`、`ReferenceRouteSources.lua` —— 3 个空表占位
- `YiboBeastPaths/FootprintAnchors.lua` —— 只创建空 `points = {}`
- `YiboBeastPaths/MapCanvasBounds.lua` —— 7 条恒等变换占位
- `YiboBeastPaths/DebugCalibrator.lua:258` `ToggleDebugSection` 忽略参数
- `YiboBeastPaths/DebugCalibrator.lua:1422` 死局部变量 `local active`
- `YiboBeastPaths/DebugCalibrator.lua:2585` `HideDebugPanel()` 空函数
- `YiboQuestBlocker/AccountPage.lua:106` `ScopeControlsWidth`、`:151` `FinishCharacterColumnBorder`（空函数）、`:495` `GetLayoutMetrics`
- `YiboQuestBlocker/AccountPage.lua` 的 `PREVIEW_MARGIN`/`PREVIEW_SCROLLBAR_GUTTER`/`CORE_PREVIEW_BORDER`、`scopeButtons`、`row.characterBands`/`characterBandEdges`
- `YiboQuestBlocker_Core.lua:1021` `CharPassLevelFilter`、`:398` `ClearDiagnostics`、`:1436` `YQB.SyncUIBindings`
- `YiboAutoOpen/Bootstrap.lua:22` `ResetAllItemRuntimeState` 无调用点

---

## 二、YiboTodo（27 个源文件，已完成）

### 严重

1. `Catalog/Validator.lua:107-109` —— `GetActiveRecipes()` 无缓存、无失效标志，每次调用重跑五表全量校验 + 全量字符串分配；被 `ProfessionCooldown:Collect:86`、`CollectForCurrentCharacter:145`、页面刷新反复调用。
2. `Providers/DailyQuest.lua:282-288` —— `ObserveNomiCompletion` 把**一次性**课程完成当作"今日已完成"，根因是 `Catalog/DailyActivityCatalog_MoP.lua:4-7` 的头部注释与 `stages`+`graduation`+`daily` 链式结构矛盾。
3. `Providers/DailyQuest.lua` —— `"ready-to-turn-in"` 是**从不写入的死状态**（229/383 行），却被 `AccountPage.STATUS_TEXT` 渲染。
4. `Providers/ProfessionCooldown.lua:102/109` —— `observations[recipe.cooldownGroupID]` / `group.recipes[recipe.recipeSpellID]` 在 key 为 nil 时崩溃（该路径可达）。
5. `Providers/SpecialActivity.lua:33-54` —— `IsDarkmoonOpen` 在 `C_Calendar` 不可用时（45 行）**断言开放**，可用时（53 行）却返回 false —— 两条路径语义相反。
6. `Probe.lua:423` —— 诊断载荷（含任务日志、≤80 个全局名、鼠标/目标 **GUID 与坐标**）持久化进 SavedVariables。
7. `Settings.lua:151-215` 与 `:230-302` —— **两个并存的设置面板** `CreateLegacyPanel` 与 `CreatePanel`，违反 AGENTS.md"不得自行维护第二套设置窗口壳"。

### 中等

8. `Catalog/*` 四份冷却组声明（见跨插件第 6 条）。
9. `Catalog/Validator.lua:75` —— `#activity.members ~= 6` 是死代码（被 72 行白名单遮蔽）。
10. `Catalog/FarmOperationCatalog.lua:6` —— 裸赋值 38 个中文 label；`verificationStatus = "user-observed"` 是项目内**第 6 种**验证状态词表。
11. `Catalog/CooldownCatalog_MoP.lua:123-134` —— 默认值在字面量之后回填，`catalogEnabled = recipe.active == true` 静默禁用 99/109 条。
12. `Catalog/CooldownCatalog_MoP.lua:128` —— 对 `excluded-no-cooldown` 条目也设 `action.castSpellID`。
13. `Catalog/CooldownGroupCatalog.lua:26` —— `wlk.inscription.minor-research` 的 `requiredSkillLevel = 1`。
14. `Catalog/MonitoringGroupCatalog.lua:15` —— `darkmoon-faire` 手写 15 个 id，必须与 `SpecialActivityCatalog_MoP` 循环生成的 id 一致但**无交叉校验**。
15. `Catalog/Ruleset_50504.lua:24` —— `activeGroups` 14 项是冷却组的**第 4 份**声明；`interface = 50504` 与 `.toc` 形成第二真相源。
16. `Providers/SpecialActivity.lua:97-121` —— `RecordLoot` 忽略 `definition.scope`（`RecordTurnIn:129` 却处理了）。
17. `Providers/SpecialActivity.lua:113` —— 仅凭拾取消息就标记 `completed`。
18. `Providers/SpecialActivity.lua:130-131` —— 账户分支直接写 `Addon.db.byAccount.specialActivities`。
19. `Providers/DailyQuest.lua:165/238/359/391/424` —— 失败路径仍写 `record.state = "available"`。
20. `Providers/DailyQuest.lua:243-248` vs `:6-8` —— `IsQuestCompleted` 只认 `== true`，`IsQuestLogComplete` 认 `true or 1`。
21. `Providers/DailyQuest.lua:250-268` —— `ObserveLegacyCompletions` 用 `pairs` + 提前 `return`，结果不确定。
22. `Providers/DailyQuest.lua:274/284/290/301` —— `GetCurrentDay`/`GetCurrentCookingDay`/`GetCurrentActivityDay` 调用时未传 `now`。
23. `Providers/DailyQuest.lua` —— `self.queued` 永不清空；`QueueObserve` 触发 5 个观察函数各重取一次。
24. `Providers/ProfessionCooldown.lua:52-60` —— `RecipeCraftable` 疑似读 `numAvailable` 而非 `difficulty`，`~= "none"` 恒真。
25. `Providers/ProfessionCooldown.lua:49` —— `tonumber(remaining) or 0` 把解析失败变成"立即可用"。
26. `Providers/ProfessionCooldown.lua:128-131` —— `MarkUnavailable` 不递增 `revision`/`lastSuccessAt`，状态无法按 `Database.lua:148-150` 传播。
27. `Providers/ProfessionCooldown.lua:161` —— `ObserveWindow` 写 `Addon.db.diagnostics.lastWindow`。
28. `Providers/FarmOperationObservation.lua:10-15` —— `TrimHistory` 排序后从索引 9 删除，而其声明上限是 9。
29. `Providers/FarmOperationObservation.lua:81-94` —— `RecordSucceededCast` 在 `RecordOperation` 已写库后重取 provider record/day。
30. `Providers/FarmOperationObservation.lua:5-8` —— `CurrentMapID` 无 `C_Map` 回退。
31. `Providers/DailyQuest.lua:184-186` vs `:200-201` —— `IsRepeatableNomiDaily` 与自身注释矛盾。
32. `Providers/SpecialActivity.lua:16-24` —— `QuestInLog` 用位置式 `GetQuestLogTitle`，而 `DailyQuest` 用 `C_QuestLog.GetInfo`（**插件内两种 API 风格**）。
33. `Providers/SpecialActivity.lua:51` vs `:66` —— 暗月马戏团标题两处匹配规则不同（子串 vs 精确）。
34. `Probe.lua:14-25` —— `CaptureNamedGlobals` 遍历全部 `_G`，每键 1 次 `string.lower` + 2 次 `string.find`，截断用 O(n²) `table.remove`。
35. `Probe.lua:372` —— `eventTrace` 40 条上限用 O(n²) `table.remove(...,1)`。
36. `Probe.lua:422` —— `dailyEventTrace` 无写入点。
37. `Probe.lua:461` —— `completedQuestBaseline` 自称"session baseline"但不持久化。
38. `Probe.lua:399` —— `serverClock.resetHour = 7` 硬编码，430 行再打印一次。
39. `Probe.lua:93-122` —— `SafeCall` 的 `{pcall(fn)}` + `ipairs` 在首个 nil 处截断；`result.values = packed` 存入引擎对象。
40. `Probe.lua:363` —— `FinishSpecial` 清 `self.specialCapture` 但不清 `Addon.db.diagnostics.specialActivityCapture`。
41. `Probe.lua:387` —— `Probe:Run` 重跑 `ValidateCatalog`。
42. `Probe.lua:373-383` —— `CaptureEvent` 对每个 `QUEST_LOG_UPDATE` 重建完整快照。
43. `Probe.lua` 详细模式输出 100+ 行聊天信息。
44. `AccountPage.lua` / `Model/Snapshot.lua` —— `Snapshot:Invalidate()` 与 `nextTransitionAt` 的脏标记链路在多处入口未调用（`Settings.lua:122/129` 的监控 setter 只调 `NotifyChanged`）。
45. `Settings.lua:8` —— `PROFESSION_ORDER` 硬编码 8 个专业 ID。
46. `Settings.lua:97-98` —— `GetMonitoringGroupState` 因 `IsMonitoringItemEnabled` 每次重扫 `Catalog.recipes` 而退化为 O(items × groups × recipes)。
47. `Settings.lua:115/117` —— `SetMonitoringGroupEnabled` 调 `GetMonitoringItems` 两次。
48. `Settings.lua:134` vs `:217-228` —— 两套 checkbox 绑定机制（`modeClickBound` 守卫 vs `BindMonitoringCheckbox`）会互相覆盖。
49. `Settings.lua:30-35` vs `:84-92` —— `modeOverrides` 与 `monitoringGroups` 两套开关模型，单向同步。
50. `Settings.lua:27` —— `SetMode` 的 activity 映射只覆盖 3 个 id。
51. `Settings.lua:11` —— `GetMode` 无保护读，写入路径（16 行）却有保护。
52. `Settings.lua:75/77` —— `MonitoringStore` 在**读路径**上写。
53. `Settings.lua:91` —— `IsMonitoringItemEnabled` 默认 `true`。
54. `Settings.lua:187-213` —— farm/nomi/cooking 三个 checkbox 是三段硬编码复制粘贴。
55. `Settings.lua:137-142` vs `:272` —— 三态用两种方式实现。
56. `Settings.lua:171/251` —— 用 `parent:GetWidth()` 做布局。
57. `Catalog/ActivityCatalog.lua:5` —— 只校验 `id`，`Validator.lua` 存在却未覆盖其它字段。
58. `Catalog/ActivityCatalog.lua` —— 三张并行 activity 表（`activities`/`dailyActivities`/`specialActivities`），导致 `GetActivity("mop.nomi")` 返回 nil。
59. `Catalog/ActivityCatalog.lua` —— `completionMode` 与 Legacy 的枚举冲突（`any-member-starts-group` vs `any-member-completes-group`）；`defaultMode` 是第 3 套模式词表；三个不同的启用标志（`active`/`catalogEnabled`+`trackCooldown`/`defaultEnabled`）。
60. `Catalog/ActivityCatalog.lua:26-27` —— `sourceExpansion = "wlk"` 而 `CooldownCatalog_MoP.lua:124` 标 `introducedIn = "5.x"`。
61. `Catalog/CooldownCatalog_MoP.lua:133` —— 裸 `Catalog.recipes[recipe.id] = recipe` 无重复守卫。
62. `Catalog/CooldownCatalog_MoP.lua:54` —— `research-131593` 缺 `resultItemID`。
63. `Catalog/CooldownCatalog_MoP.lua` —— 6 个同名 `mop.jewelcrafting.research-*` 共用同一 `cooldownGroupID`。
64. `Catalog/DailyActivityCatalog_Legacy.lua:4-6` —— 头部注释说 opt-in 探针，实际 9 个全是 `defaultEnabled = true`。
65. `Catalog/DailyActivityCatalog_Legacy.lua:7-16` —— `LegacyDaily` 裸写 `Catalog.dailyActivities[id]`；icon 路径格式与 MoP 版不同（缺 `.blp`）。
66. `Catalog/DailyActivityCatalog_Legacy.lua:50-54/60-64` —— 联盟/部落 ID 作为并行成员且 label 完全相同。
67. `Catalog/DailyActivityCatalog_Legacy.lua` —— 20 个几乎相同的成员 label；构造后字段补丁（28/34/41/66/78 行）。
68. `Catalog/DailyActivityCatalog_MoP.lua` —— `stages`/`graduation`/`daily` 三个字段名表达同一形状；两套启用机制；只有 `mop.halfhill.cooking-daily` 的成员带 `turnInNPCID`。
69. `Catalog/FarmOperationCatalog.lua:39-43` —— 同一作物拆成 `bundle`/`single` 且 label 不同；`plantType` 只有两个值；`mapID = 376` 魔数。
70. `Catalog/MonitoringGroupCatalog.lua:7` —— 裸赋值；`["profession-cooldown"]` 缺 `members`（与其它项不一致）。
71. `Catalog/SpecialActivityCatalog_MoP.lua` —— NPC 字段两种形状（`turnInNPCID` vs `npcID`）且**都不被 `SpecialActivity.lua` 读取**；`questIDs` 无写入点却被 `SpecialActivity.lua:127` 读取。
72. `Catalog/Validator.lua:46-47` —— `duplicate-farm-spell` 不可达；第 28-31 行 `result.activeRecipes` 在 if/elseif 链尾填充，导致任何提前分支静默排除该 recipe；第 71 行硬编码课程数 5。
73. `Catalog/Validator.lua:10` —— `Catalog.rulesets[self.RULESET_ID]` 无保护；返回值在 `Bootstrap.lua:54-55` 被丢弃。

---

## 三、YiboBeastPaths（23 个源文件 + 2 个测试，已完成）

### 严重

1. **`Renderer.lua:1182` `RefreshMapLayer` 被 `Core.lua:124` 的 `OnUpdate` Hook 每帧调用**，无缓存、无脏标记、无节流；`RenderRouteLayer:947-994` 每帧重跑约 1300 点的 Catmull-Rom 平滑；`AcquireVectorSlot:611-631` **每线段创建一个完整 Frame**。
2. **`MinimapRenderer.lua:675-685`** —— `OnUpdate` 每 0.05s 调 `RefreshMinimapLayer()`，且 `stats` 表重建、`EnsureRouteCache`、3 次 HBD 查询、4 次 `string.format` 都在签名前就已付出；`DrawMinimapScreenLine:565` 以 `MINIMAP_ROUTE_SEGMENT_STEP_PX = 1` 切片 → **每像素一个 Frame**，再被 `BuildScreenSmoothedPoints:520-545` 放大 4 倍。
3. **`Renderer.lua:721-761` `BuildReferenceDisplayTransformFromSegments` 依赖 `route.displaySegments`，该字段在任何文件中都不存在** → 恒返回 nil 并缓存 `false`（778 行）→ **参考层显示变换永远不生效**。
4. **`MinimapRenderer.lua:160-164/763` 读取 `transform.minimapOffsetX/minimapScale/minimapLineThickness`**，而 `ns.routeTransforms` 中**没有这些字段**（真实数据在 `ns.minimapTransforms`，字段名 `lineThickness`）—— 只因为 `YBP:GetResolvedMinimapTransform`（定义于 `DebugCalibrator.lua:575-599`）做了字段重命名才"碰巧"工作。
5. **`RouteResolver.lua:311` 用 `date("!...Z")`** —— 本地时钟标成 UTC。
6. **`RouteResolver.lua:801` 与 `:269` 双重 `DeepCopy`**；`ns.resolvedRoutes`（891-897 行）**无失效路径**。
7. **`CoreDebugAdapter.lua:169-174`** —— `TryRegister` 在 `RegisterAddon` 成功后若 `RegisterSettingsPanel` 失败**不回滚**；`notifiedReasons:11` 永久增长导致失败被静默抑制。
8. **`RouteResolver.lua:135-142` `InvertMapPointToRouteSpace` 修正了 `scaleX/Y` 接近 0 的情况，却漏了 `scale`**。
9. **`RouteResolver.lua:52-88` `EnsureFusionDB` 把生产设置写进 debug SavedVariable**，并重复默认值（58-68 vs 72-83）。
10. **`CuratedRoutes.lua` 坐标远超 [0,1]**：`y = 1.016`、`x = 1.176`、`y = -0.230`、`x = 1.309`、`x = 1.389, y = -0.691`、`y = 1.557`；与 `RouteNodes.normalizedX/Y` **不是同一坐标系却都叫 `normalized`**，且无坐标系注释。

### 中等

11. `Core.lua:145` —— `SLASH_YIBOBEASTPATHS2 = "/yibobeastpaths"` **违反 AGENTS.md 的"不得保留旧命令别名"**。
12. `Core.lua` —— **完全未接入 Core**（无 `RegisterAddon`/入口/设置/账号视图）。
13. `Core.lua:124-126` —— `WorldMapFrame:HookScript("OnUpdate", ...)` 每帧调 `RefreshMapLayer()`。
14. `Core.lua:11-17` —— `IsAddOnLoadedCompat` 缺 `type() == "function"` 检查。
15. `Core.lua:204-208` —— 裸 `_G.YiboBeastPathsDB`，无 `schemaVersion`/迁移。
16. `Core.lua:93-97` —— `button.label` 死代码（`GetFontString()` 恒存在）；第 54/59 行双 `SetText`；金色硬编码两次（61 行 + `RouteDisplayMeta`）。
17. `Core.lua:149-196` —— 5 个 `/ybp` 分支重复三次刷新。
18. `Data.lua:4-13` —— `nameEN` 无读取点；`zone`（中文）+ `mapID` 双标识；10 条记录缺 `label`/`icon`/`verificationStatus`/`evidence`。
19. `Flavor.lua:5` —— `ns.WOW_PROJECT_ID` 未使用；`CLASSIC`/`IS_CLASSIC` 同义（9/26 行）；5 个字段表达 2 个布尔；`projectID` 为 nil 时静默变 `"unknown"`。
20. `FlavorOverrides.lua:25-40` —— `overrideTargets` 在加载时快照 15 个 namespace 字段，**尚未执行的文件会被永久绑定为 nil**；第 52-55 行的 `retry`/`mists` 覆盖**当前完全为空**却仍在 57 行应用。
21. `RouteDisplayMeta.lua:23` —— `perPet[petID].routeColor = route.color` 引用赋值；`evidenceColor:24` 与 `defaults.footprintColor:15` 及 `Core.lua:61` **三处字节相同**。
22. `MapCanvasBounds.lua` —— 7 条记录全是恒等变换 `{0,0,1,1}`（零信息占位）。
23. `RouteTransforms.lua` —— 10×7 个无来源魔数；`opacity` 恒为 `1.0000`；`scale`/`scaleX`/`scaleY` 组合契约未定义。
24. `MinimapTransforms.lua:12` —— `[50822]` 的 `scale = 1.2050` 配 `scaleX/Y ≈ 0.35` 异常；字段名 `lineThickness` 与 `RouteTransforms` 的 `thickness` 不一致。
25. `FootprintAnchors.lua:9` —— 只创建空 `points = {}`，整个功能是空壳。
26. `RouteOverlays.lua` —— `texture` 硬编码完整插件目录 10 次且无扩展名；`slug` 与 texture 尾部重复；`mapID` 第 3 份。
27. `RouteNodes.lua:134-135` —— 注释说"debuggable start placeholder"，但所有条目 `isPlaceholder = false`；`[50813]` 的 texture `50843_portent_jade` 与 `colorName = "四色随机魁麟"` 冲突；`[66522].titleSuffix = "（待验证）"` 是唯一的验证标记。
28. `ResolvedRoutes.lua` / `ReferenceRoutePoints.lua` / `ReferenceRouteSources.lua` —— 3 个空表占位，却都被列进 `FlavorOverrides.lua:28-30` 的 `overrideTargets`。
29. `ReferenceRoutes.lua` —— `version = "1.4.0-overlay-refresh"` ×10 与 `.toc` 的 `v1.6` 不一致；`migratedFrom` ×10（一次性迁移产物永久保留）；`referenceDisplayTransform` 4 参数 vs `RouteTransforms` 7 参数；同一宠物的 scale 在两表间完全不同（50811：0.5973/0.4773 vs 0.5900/0.9950/0.7950）。
30. `RouteResolver.lua:438-513` 与 `:515-599` —— 约 120 行重复；`:733-750` 每个锚点重算循环不变量；`:122-127` 在 `YBP:GetResolvedTransform` 缺失时静默退化为恒等；`:608` `limit` 固定为 8；`:924-936` `GetRouteLayerColor` 每次分配 3 个表且回退色与 `RouteDisplayMeta.defaults` 不同；`:946-969` 6 个 getter 各自重推 `routeDisplayMeta.defaults`；`Clamp`/`Lerp` 不处理 nil；`DeepCopy` 无循环/深度保护；`GetFootprintsForPet:290` 返回内部引用。
31. `RouteResolver.lua:643-653` 与 `:656-669` —— 评分块重复；约 10 个无来源魔数（`minimumForwardStep = -0.02`、`allowedStep = max(0.04, step*3.5)`、`×0.25/0.5`、`×0.0005`、`×4`、`0.72`、`0.85`）。
32. `Renderer.lua:1077-1106` 与 `:1108-1120` —— 两个函数逐字重复且各自分配新表；`:1229-1231` `GetRouteLayerColor` 每宠物调 3 次再包 3 个新表；`:108-130` 靠地图下拉框文本 + 中英双语映射判图，并回退到私有 `WorldMapFrame.mapID`；`:690` `referenceDisplayTransformCache` 永不失效；`:594` `ApplyOverlayEmphasis` 的 `thickness` 参数未使用；`:229` 读 `pet.zoneEN` 而 `Data.lua` 命名为 `nameEN`；`:318-374` 自建 tooltip（不用 `GameTooltip`）且每次显示重建全部 FontString；`:398-400` `EnsureRouteNodeButton` 换父级时泄漏 Frame；`:534-538` 与 `:539-543` 两个 else 分支相同；`:184-189` 每宠物创建 16 个对象（12 个 echo 硬编码）；`:548-592` 大量魔数（`0.4`、`inset = (1-strength)*6`、`0.65+0.35*strength`、`*8+0.5`）+ 中文注释；`:1239` 主 overlay 传恒等变换而宿主用 `legacyOverlayTransform`；`:1183` `HideAllOverlays` 后 1282 行又 `Show()` 一遍；`:1029-1036` 每帧重排 frame level；`:1269` `display.showReference` 双重门控（含 debug 模式）且默认 false；`:203-213` `GetNodeRoleLabel` 全中文且 `"end"`/`"mid"` 是死分支。
33. `MinimapRenderer.lua:186-211` —— `TransformPoint` 为约 1300 个点各自重算与点无关的 `baseCenterX/Y`，并对每点调 `HBD:GetWorldCoordinatesFromZone`；`:297` `InvalidateMinimapRouteCache` 被每次 `RefreshRouteFusionViews` 调用；`:346-354` `TrimUnusedSegmentSlots` 只隐藏；`:492` `UpdateSegmentSlot` 把 alpha 钳到 `[0.96, 0.99]` 忽略配置；`:614-655` `CollectVisibleFootprints` 排序全部候选后只留 6 个却返回两个列表（只用到计数）；`:95-120` `ResetDebugStats` 在生产路径上每次刷新重建 20 字段表；`:32-49` `minimapSizes` 硬编码 12 个半径（含 `466 + 2/3`、`266 + 2/6`）；`minimapDualLaneBoost` 无消费者；`:751-755` `no_routes_for_map` 分支跳过 `TrimUnusedSegmentSlots`；`:509` `0.000001` 去重阈值无效；`:699` 第 4 种前缀色；`:367` `ClipLineToCircle` 对退化线段返回 nil 且不计数。
34. `CoreDebugView.lua:395-475` —— `layout` 每次都布局全部 4 个 tab 的 group（456 行 `for key, tab in pairs(panel.tabs)` 无 `key == active` 短路）；`:347-361` 宽度分配 while 循环可能迭代数百次；`:330` `label:gsub("[^\128-\193]", "")` 是字节区间字符计数启发式，`MeasureText` 回退把字符数乘字号；`:92` `addButton` 的 OnClick 恒调 `RefreshDebugPanel` → 一次点击触发完整路线重解析 + 完整重布局；`:596` `view` 单例换父级时泄漏；约 20 处**未加保护**直调 `YBP:AdjustDebug*`/`ResetDebug*`/`ConfirmDebugAction`；`:562-571` 每次刷新重设 12 个行闭包；`:582-585` `panel.lastExportID` 会阻止导出框在仅视图变化时刷新；`:586-592` 与 `CoreDebugAdapter.RefreshDebugPage:92-93` 重复设置高度，可能经尺寸 Hook 递归；大量硬编码布局常量（`math.max(580,…)`、`controlWidth = width < 700 and 96 or 112`、`tabWidth = (tabBandWidth - 40 - 3*8)/4`）；`:626` `layout` 在 `Show` 之前运行；`:499-500` 步进预设浮点相等比较；`:307-308` `SetWidth(500)/SetHeight(700)` 与 `:468` `SetSize(width-40, 320)` 矛盾；`:236` 匿名数组 + `unpack`；约 60 处中文；第 5 种前缀色。
35. `DebugCalibrator.lua:1618` —— **`math.floor(((panel:GetWidth() or 620) - 28 - 24) / 4)` 中 `GetWidth()` 返回 0（非 nil），`or 620` 永不生效 → `tabWidth = -13`**；且 1452 行在构造前就 `panel:Hide()`。
36. `DebugCalibrator.lua:1619-1628` —— **4 个 tab 按钮创建时无 `SetText`**（全文件无该调用）；1422 行的死变量 `local active` 说明标签分支被改写。需确认是否渲染为空按钮。
37. `DebugCalibrator.lua` —— **同一元素清单维护三份**：`sectionContents`（2384-2432）、`sectionLayout[*].movableElements`（2434-2501）、`autoHeightElements`（2524-2562）。
38. `DebugCalibrator.lua` —— 约 20 级硬编码纵向偏移链（`adjY→thickY(-34)→advY(-34)→nodeY(-36)→stepY(-36)→miniMapAdjY(-34)→…→exportY(-296)`）。
39. `DebugCalibrator.lua:1660` —— `MakeAdjustButton` 的 `petID` 形参在所有调用点传 `nil` 且函数体不读，是死参数。
40. `DebugCalibrator.lua:2601/2644/2650` —— 单次刷新 3 次调用 `GetDebugPetIDsForCurrentMap()`。
41. `DebugCalibrator.lua:2642-2645` —— `petText` 连续 `SetText` 两次，第一次纯浪费。
42. `DebugCalibrator.lua:2857-2858` —— 每次刷新都无条件重序列化导出文本（导出页不可见时也照算）。
43. `DebugCalibrator.lua:2797-2804` —— 每次刷新对每行重绑 `SetScript`。
44. `DebugCalibrator.lua:2905-2914` —— `WorldMapFrame` 第二个 `OnUpdate` Hook，面板关闭时仍每 0.25s 运行；`lastMapID` 初值 nil 导致首帧必刷。
45. `DebugCalibrator.lua:2585-2588` —— `HideDebugPanel()` 是空函数（只有注释）。
46. `DebugCalibrator.lua:76-100` —— `GetButtonLabel` 是 10 分支全中文 if/elseif 链；1811-1816 行又改回硬编码中文，与 1819-1822 行风格不一致。
47. `DebugCalibrator.lua:2071-2081/2098-2108/2117-2124/2169-2176` —— 四段"建完再重锚"惯用法（首次锚点是死代码）。
48. `DebugCalibrator.lua` —— 第 5 份 `CopyDefaults`（114-125）；`stepOrder`（50）只被标签查找使用；`ToggleDebugSection`（258-260）忽略参数；`GetDebugContextMapID`（189-212）硬编码向上走 12 层；`SerializeLua`（854-903）无循环/深度保护；导出函数硬编码目标文件名注释；`StaticPopupDialogs["YIBO_BEAST_PATHS_CONFIRM_DEBUG_ACTION"]`（1146-1157）占全局名无冲突检查；`ApplyExportTabLayout`（1316-1327）硬编码 `-286/-312/54`；`ApplyDynamicLayout`（1329-1395）硬编码 `-212/-288/-396/-466`；`ApplySectionVisibility`（1397-1425）含死局部变量；`GetOrCreatePanel`（1427-1451）硬编码 `math.max(620,…)` 与高度 `1160`，并对滚动子 Frame 调 `EnableMouse(true)`。
49. `RouteResolver.lua:318/335/352/365/381` —— 5 个完全相同的 auto-resolve 分支；`:361` 缺少 return（返回不一致）。
50. `YiboBeastPaths.toc` —— `## Interface: 50400` 与项目 50504 不一致；`## OptionalDeps: YiboCore` 应为 `RequiredDeps`；两个 SavedVariables 无 schemaVersion；加载顺序隐式且只靠巧合满足依赖。
51. `_NonRelease/Tests/DebugContextSmoke.lua` —— `CreateFrame` 桩返回纯表（无 `CreateTexture`/`CreateFontString`/`SetPoint`/`HookScript`），把被测文件绝大部分排除在覆盖外；夹具硬编码路由表与真实数据无交叉校验；`WorldMapFrame:HookScript` 从未触发（0.25s 轮询逻辑零覆盖）；相对路径 `loadfile`；全局桩不还原；11 条问题。
52. `_NonRelease/Tests/CoreDebugViewSmoke.lua` —— **`widget` 的 `__index` 返回 `function() end`（22 行）吞掉一切错误**，结构上无法发现"调用了不存在的 Frame API"；`CreateFrame` 桩丢弃 name 参数 → **无法检测 UI 重名**；`SetPoint` 桩只保留最后锚点；桩在迎合被测代码（`preset`/`settingKey`/`exportKey` 特判返回 nil）；字段名契约错配（夹具给 `imageTexture`，实现读 `iconTexture`）；夹具把 mapID 376 当 petID；中文文案写进断言；16 条问题。

---

## 四、YiboAutoOpen（11 个源文件 + 11 个测试，已完成）

### 严重

1. **`Commands.lua:11` 的 `and/or` 链 bug**：`local ok, result = command == "add" and Addon.Database:AddItem(id) or Addon.Database:RemoveItem(id)` —— 当 `command == "add"` 且 `AddItem` 返回 `nil, "already_exists"` 时，`(true and nil)` 为 `nil` → 继续求值 `or` 右侧 → **`/yao add <已在目录中的物品>` 会把它从目录里删掉**。
2. **`Queue.lua:108` 的 `_SHOW$` 通配会把队列永久暂停**：任何以 `_SHOW` 结尾的事件（`GOSSIP_SHOW`/`TRADE_SKILL_SHOW`/`QUEST_FRAME_SHOW` 等）都会命中该分支把 `queueState` 设为 `PAUSED`/`SENSITIVE_UI`，而 `Safety:SetSensitive` 的 7 项白名单查不到 key 就什么都不做，110 行的 `_CLOSED$` 恢复表也只有 7 项 → **没有任何恢复路径**。
3. **`Safety.lua` 是 fail-open 的**：`Addon.db`（8）、`Addon.runtime`（9/14）、`Addon.BagAdapter`（15）全裸访问；`BagAdapter` 出问题会让 `CanRun()` **抛错而不是返回 `false`**。安全门应当 fail-closed。
4. **`Safety.lua` 无 `Reset()`，且 `sensitiveFrames` 可能永久为 true**（某界面只发 SHOW 不发 CLOSE）→ **插件静默永久失效**。
5. **`Queue.lua:73` 在无 `C_Timer` 时队列死锁**：超时定时器不注册 → `pending` 永不清空 → `ProcessNext:38` 永久 early-return。
6. **`Queue.lua:80` 仅靠物品数量减少判定成功** → 物品被其它途径消耗会误判成功；袋子开出同类物品（数量不变）会误判失败并累加 retries → **误隔离**。
7. **`BagAdapter.lua:25` 把"未知 locked"硬编码为 `locked = false`** —— 调用方（`CountCatalogued:31`、`FindNextEligible:73`）据此判断安全性 → **未知状态被当成可安全使用**。
8. **`BagAdapter.lua:19-25` 的第三条路径在 MoP Classic 上恒返回 nil** —— 注释声称"Some Classic clients expose the C_Container slot ID/link helpers"与事实不符（`C_Container` 自 10.0 引入）。
9. **`Catalog.lua:5` `migrations = {}` 为空** + `Database.lua:22` 把 `db.catalogVersion` 顶到 `Catalog.version` → **新增默认物品对已有用户永不生效**；整个迁移框架是死代码。
10. **`Catalog.lua:4` 的 40 个 ID 混入多资料片**（39883/44751/45724 属 WotLK，199210/200239/200238 属 10.0+，72201/90735 属 WoD/Legion），**无 flavor 门禁** → MoP Classic 上列出不存在的物品。
11. **`ItemResolver.lua:11` `GetItemInfo(raw)` 把中文名当 itemID 传入** —— 语义无效，该行几乎总是返回 nil；作者误以为 `GetItemInfo` 能按名称查询。
12. **`ItemResolver.lua:9` 全包扫描 + 每格 `GetItemInfo`**，而 `GetItemInfo` 对未缓存物品返回 nil → **冷启动时任何名称解析都静默失败**。

### 中等

13. `Safety.lua:4` —— 事件→键映射表每次调用重建。
14. `Safety.lua:5` —— `opened == true` 使非布尔真值一律当"已关闭"。
15. `Safety.lua:13` —— 只查 `LootFrame`，漏掉 `GroupLootFrame1..4`/`LOOT_ROLL`/`ItemTextFrame`/`StaticPopup`/`GossipFrame`/`QuestFrame`。
16. `Safety.lua:12` —— 只查 `UnitCastingInfo`/`UnitChannelInfo`，未查 `UnitAffectingCombat` 与专业/交易技能施法。
17. `Safety.lua:11` —— `UnitIsDeadOrGhost` 与 `UnitInVehicle` 共用返回码 `PLAYER_UNAVAILABLE`。
18. `Safety.lua:4` —— `VOID_STORAGE_OPEN` 是 Retail-only 事件，MoP Classic 上永不触发（死配置）；文件内无任何 flavor 门禁。
19. `Safety.lua:16/17` —— `freeSlots` 为 nil 时直接比较报错；未按 `Addon.LIMITS.minFreeSlots` 校验范围。
20. `Safety.lua:18` —— 失败返回 `false, reason` 而成功只返回 `true`，返回元数不一致。
21. `Defaults.lua:2` —— `Addon.DEFAULTS` 是共享表；`Database.lua:7` 把其引用直接赋给 db。
22. `Defaults.lua:3` —— `LIMITS` 命名不自洽（1 项是范围表，3 项是标量）；`minFreeSlots` 的 min/max 与 `DEFAULTS.minFreeSlots` 无一致性校验。
23. `Defaults.lua:2` —— `notificationMode = "issues"` 无枚举白名单表，与 `Database.lua:11`、Settings 选项三处各自维护。
24. `Defaults.lua` —— `schemaVersion = 1` 与 YiboCore 的 `CURRENT_SCHEMA = 10` 体系未对齐；无迁移入口。
25. `Database.lua:3-4` —— `CopyOrder`/`BuildEntries` 与 `AddItem`/`RemoveItem`/`Normalize` 内联逻辑重复；`catalog.order` 与 `catalog.entries` **双真相**需 4 处同步；`entries` 以 itemID 为 key 的集合表会写入 SavedVariables（体积大、不可读）。
26. `Database.lua:8` —— `db.schemaVersion = 1` 硬编码，与 `DEFAULTS.schemaVersion` 重复。
27. `Database.lua:9` —— `db.enabled = db.enabled ~= false`，`0`/`"false"` 都被判为开启。
28. `Database.lua:10` —— `or 5` 硬编码，与 `DEFAULTS.minFreeSlots = 5` 重复。
29. `Database.lua:13` —— `tonumber(id)` 静默接受字符串数字。
30. `Database.lua:18` —— `catalogSeeded` 不在 `DEFAULTS` 中，属隐式字段。
31. `Database.lua:20` —— `ipairs(migration.addedIDs)` 无 nil 保护。
32. `Database.lua:22` —— 迁移可被静默跳过（版本被顶到当前 Catalog 版本）。
33. `Database.lua:24` —— 直接写全局 `YiboAutoOpenDB`；`Addon.db` 与 `self.db` 双引用。
34. `Database.lua:26` —— `EnsureInitialized` 调 `Addon:Initialize()`（方法挂在 `Addon` 而非 `Database`），与 `Database:Initialize` 形成两个同名入口。
35. `Database.lua:29/30` —— 单行 8 条语句；`RemoveItem` 倒序删除循环与 `AddItem` 的 `already_exists` 不变量不匹配。
36. `Database.lua:31` —— `GetOrderedItems` 每次返回拷贝，循环中调用即 O(n²)。
37. `BagAdapter.lua:3/5/10/11/22/23/35/38/40/56/59/60` —— 函数存在性检查缺 `type() == "function"`。
38. `BagAdapter.lua:27-34` 与 `:64-85` —— 同一遍历逻辑两份实现，遍历方向相反（`slot = 1..N` vs `N..1`）。
39. `BagAdapter.lua:29` —— `CountCatalogued` 只扫 `bag = 4..0`，遗漏银行(-1)/公会银行(-3)/邮箱(-2)；而 `GetGenericFreeSlots:48` 是 `bag = 0..4`。
40. `BagAdapter.lua:35` —— `UseItem` 无 `type()` 检查；Retail 已移除 `UseContainerItem`。
41. `BagAdapter.lua:44/50` —— `GetFreeSlots` 返回 `known`，`GetGenericFreeSlots` 把它取反成 `complete`（语义反转）。
42. `BagAdapter.lua:50` —— `family == 0` 判"通用背包"，专业包 family 也为 0 时会重复计入。
43. `BagAdapter.lua:54` —— `GetItemCount(itemID, false, false, false, false)` 五参数硬编码，签名随版本变化。
44. `BagAdapter.lua:62` —— **无冷却时返回 `duration = 1`**（谎报 1 秒冷却）。
45. `BagAdapter.lua:74/79` —— 锁定物品重试间隔恒为 0.5 秒无退避；`math.max(0.1, readyAt - now + 0.05)` 魔数。
46. `BagAdapter.lua:70` —— 倒序扫描（优先最后一个包的最后一格）无注释说明。
47. `BagAdapter.lua:72` —— `quarantined[item.itemID]` 无 nil 保护。
48. `BagAdapter.lua` —— 未校验物品是否可右键开启（装备/任务物品会误排队）；无"银行/邮件界面打开时不使用物品"门禁。
49. `Catalog.lua:2` —— 裸表赋值，无 namespace 保护、无 `Validator`、无 `verificationStatus`/`evidence`。
50. `ItemResolver.lua:5` —— `item:(%d+)` 分支缺范围校验。
51. `ItemResolver.lua:6` —— 魔数 `9007199254740991` 无注释，且对 itemID 而言形同虚设。
52. `ItemResolver.lua:7` —— `raw == ""` 检查排在两次 `match` 之后。
53. `ItemResolver.lua:8` —— `Add` 闭包每次 `Resolve` 分配 2 表 + 1 闭包。
54. `ItemResolver.lua:12-14` —— 同名多候选一律 `ambiguous`，未按资料片优先级排序。
55. `ItemResolver.lua:3` —— `Trim` 不处理全角空格 U+3000。
56. `ItemResolver.lua:9/10` —— `Addon.BagAdapter`/`Addon.Database` 无 nil 保护。
57. `ItemResolver.lua:6` —— `"0123"` 静默归一为 123。
58. `Queue.lua:13/14/16` —— `scanQueued` 无超时复位，`Run` 未执行则**扫描永久停摆**。
59. `Queue.lua:39` vs `:46` —— 全包扫描发生在安全检查**之前**，顺序颠倒；单次 `ProcessNext` 至少两次全包遍历（`FindNextEligible` + `CanRun` 内 `GetGenericFreeSlots`）。
60. `Queue.lua:67` —— `pending.startedAt` 全文无读取点（死字段）。
61. `Queue.lua:68` —— `UseItem` 返回值被丢弃。
62. `Queue.lua:83` —— `fromBagEvent` 分支不清理 `pending`，依赖 0.2s 后第二次调用。
63. `Queue.lua:92/96-101` —— 一次 `BAG_UPDATE` 触发两次 `ResolvePending` + 两次 `RequestScan`，开包时 `BAG_UPDATE` 连续触发 → 扫描风暴。
64. `Queue.lua:32-36` —— `Clear` 未重置 `runtime.warned` → 清空后空间不足提示不再出现。
65. `Queue.lua:55` 与 `:43/51/60` —— `warned.space` 设置与清空分离，提示会反复弹出。
66. `Queue.lua:86` —— 提示 `"物品 #" .. pending.itemID` 无物品名。
67. `Queue.lua` —— `quarantined`/`failures`/`warned` 存于 `runtime`，**不持久化**，重载即重置。
68. `Queue.lua:3-7` —— `GetStatus` 裸访问 `Addon.runtime`。
69. `Queue.lua:119` —— `{ 0.5, 2.0, 5.0, 10.0 }` 魔数；10 秒后不再补扫。
70. `Queue.lua:112` —— `event:find("UNIT_SPELLCAST")` 无锚点，`FAILED`/`INTERRUPTED` 也触发扫描且无节流。
71. `Queue.lua:145` —— 31 个事件名硬编码在单个超长数组；`VOID_STORAGE_*` 在 MoP 不存在，`pcall` 静默失败且无日志（147 行）。
72. `Bootstrap.lua:4` —— `runtime` 单行 14 个字段。
73. `Bootstrap.lua:34` —— `IsLoggedIn()` 可能让 `StartPostLogin` 在 `PLAYER_LOGIN` 之前触发，导致 `CoreIntegration:Initialize()` 在 Core 未就绪时执行。
74. `Bootstrap.lua:38-41` —— `CoreIntegration:Initialize()` 只在首次执行；首次时 Core 未加载则**永不重试**。
75. `Bootstrap.lua:33` —— `self.Database:Initialize()` 无 nil 保护。
76. `Bootstrap.lua:6-10` —— 前缀 `|cff20e070[Yibo]|r` 颜色合规但**缺插件名**；`"自动开包："` 中文硬编码。
77. `Bootstrap.lua:7-8` —— `Print` 的 `level` 参数无枚举校验。
78. `Bootstrap.lua:11-14` —— `NotifyIssue` 的 key 去重后本次登录内不再提示，key 无统一清理入口。
79. `Bootstrap.lua:45-47` —— `Addon:Refresh` 只是 `Queue:RequestScan()` 的一层无价值转发。
80. `CoreIntegration.lua:7/9/52` —— 魔法数字 `6` 写三处。
81. `CoreIntegration.lua:7` —— `Core:CheckAPIVersion` 无 `type()` 检查，老版 Core 会直接抛错。
82. `CoreIntegration.lua:6/8/10` —— **所有失败路径都 `return true`**，返回值无信息量。
83. `CoreIntegration.lua:17` —— `RegisterAddon` 成功后 `RegisterSettingsPanel` 失败会**重复注册**（无回滚）。
84. `CoreIntegration.lua:28` —— 加载时立即初始化；若 Core 已存在可能在 `Settings.lua` 加载前调用 `Addon.Settings:CreatePanel` → nil 报错（约束只写在 25-27 行注释里）。
85. `CoreIntegration.lua:18-21` —— 直调 Core 私有方法 `Core.AccountView:RefreshNavigation/RefreshPage` 并以私有字段 `activePageID == "settings"` 判断。
86. `CoreIntegration.lua:13` —— icon 硬编码完整插件目录 + `-v2` 版本后缀 + 无扩展名。
87. `CoreIntegration.lua:12` —— `title`/`description` 中文硬编码。
88. `CoreIntegration.lua` —— **未接入 Core 的账号视图与数据域体系**（无 `RegisterBusinessEntry`/`DataDomains`/悬停摘要），与 AGENTS.md 要求不符。
89. `Commands.lua:5` —— `SlashCmdList["YIBOAUTOOPEN"]` 键名与 `Addon.NAME` 大小写/拼写两套规范。
90. `Commands.lua:5-46` —— **命令处理整体无 `xpcall` 保护**（只有 `status` 分支内部有）。
91. `Commands.lua:6` —— `%s` 不匹配全角空格。
92. `Commands.lua:10` —— `table.concat(candidates, ...)` 无 nil 保护。
93. `Commands.lua:12` —— 其它错误码（如 `"invalid"`）会显示误导性的"该物品不在目录中"。
94. `Commands.lua:17` —— `ItemText(items[i]) .. " · " .. items[i]` 在无名称时输出 `物品 #123 · 123`（重复）。
95. `Commands.lua:3` —— `ItemText` 对未缓存物品不触发数据加载。
96. `Commands.lua:24` —— `/yao status` 每次全扫背包。
97. `Commands.lua:31` —— `startedAt` 为 nil 时 `elapsed` 会变成巨大的 `GetTime()`。
98. `Commands.lua:22-26` —— `xpcall` 错误处理丢弃堆栈。
99. `Commands.lua` —— 无 `on`/`off` 启停子命令，无 `reset` 子命令（`ResetAllItemRuntimeState` 因此成死代码）。
100. `Commands.lua:15` —— `"页码范围：1–"` 用 en dash（U+2013）而非 ASCII `-`。
101. `Commands.lua:45` —— `/yao` 或未知子命令打印 4 行用法，噪音大，无 `help` 别名。
102. `Settings.lua:100` —— 目录行 Frame 只 `Hide()` 不回收；无上限。
103. `Settings.lua:59` —— 每次 `CreatePanel` 全包扫描 `CountCatalogued`，而 `Refresh()` 在本文件被调 **8 次**。
104. `Settings.lua:31` —— `panel.itemInfoEvents` 是匿名无父 Frame，永久监听 `GET_ITEM_INFO_RECEIVED`，面板隐藏后回调仍执行。
105. `Settings.lua:3/14` —— `_G.YiboCore.UITheme` 无 nil 保护；第 14 行重复查找两次。
106. `Settings.lua:21/57` —— 下拉框选项定义两遍（第一次是死数据）。
107. `Settings.lua:42` vs `:29/30` —— `host.bindTooltip` 有保护而 `host.createCheckbox`/`host.refreshPage` 无保护。
108. `Settings.lua` —— 绝对像素定位遍布（`-8/-34/-58/-68/-106/-136/-154/-166/-190`），无布局常量。
109. `Settings.lua:66` —— `GetWidth() or 600` 兜底失效；注释声称宽度已由 Core 解析与事实矛盾。
110. `Settings.lua:68/71/96` —— 三套列布局公式不同步（魔数 `-24`/`+12`/`12`/`280`），列宽与列数可能不匹配导致右侧溢出。
111. `Settings.lua:74` —— 把布局调试信息（列数）显示给用户。
112. `Settings.lua:77-80` —— 逐项 `RequestLoadItemDataByID` + `GetItemInfo`（两者做同一件事，重复）。
113. `Settings.lua:48/51` —— 文案 `"（1–20）"` 与校验 `1`/`20` 硬编码，与 `LIMITS.minFreeSlots` 三处重复。
114. `Settings.lua:55-56` —— Enter 与失焦行为不一致（Enter 保存后不刷新）。
115. `Settings.lua:4-5` —— `StaticPopupDialogs` 占全局名无冲突检查。
116. `Settings.lua:8` —— 三重裸访问 `Addon.Settings.panel.host.refreshPage()`；`OnAccept` 的 `data` 为 nil 时 `RemoveItem(nil)` 报错。
117. `Settings.lua:98` —— 每次刷新重建闭包并重绑 `SetScript`。
118. `Settings.lua:6` —— 用中文全角引号 `“”`。
119. `Settings.lua:44` —— `panel:Show()` 由业务插件主动调用，与 Core 生命周期职责重叠。
120. `Settings.lua:120` —— 返回 `panel:GetHeight()`（数字）而非 panel 对象，与 `CreateSettingsPanel` 常规契约不一致。
121. `Settings.lua:46` —— `not c:GetChecked()` 点击瞬间改状态且无回滚；`GetChecked()` 返回 nil 时状态错乱。
122. `YiboAutoOpen.toc:8` —— `## OptionalDeps: YiboCore` 应为 `RequiredDeps`。
123. `YiboAutoOpen.toc` —— 无 `## X-GameFlavor`；`## LoadOnDemand: 0` 无意义；`## Interface: 50504` 与 `YiboBeastPaths` 的 50400 不一致。
124. `YiboAutoOpen.toc:20-21` —— `Settings.lua` 必须早于 `CoreIntegration.lua` 的约束**只写在注释里**，调换两行即引入 nil 崩溃。
125. `_NonRelease/Tests/TestRunner.lua` —— `specs` 硬编码 10 个文件名；**无 `pcall`**（任一 spec 抛错即中断后续）；无通过/失败计数；失败不返回非零退出码；spec 间无状态隔离；仓库内无调用入口；列表非字母序。
126. `_NonRelease/Tests/QueueSpec.lua` —— **名实不符**（只断言 2 个 `LIMITS` 常量，`Queue.lua` 逻辑零覆盖）；`YiboAutoOpen = {}` 覆盖全局不还原且会破坏后续 spec。
127. `_NonRelease/Tests/CatalogSpec.lua` —— `assert(#catalog == 40)` **锁死了错误的默认列表**；按索引断言具体 ID；凑数断言 `assert(not seen[5234000])`。
128. `_NonRelease/Tests/MigrationSpec.lua` —— **测试自造迁移，验证了生产中不存在的情况**；加载顺序与生产不一致；未断言 `catalog.order`。
129. `_NonRelease/Tests/ItemResolverSpec.lua` —— **名称解析路径完全未测**（问题最集中处）；未测 `ambiguous`/`not_found`/边界。
130. `_NonRelease/Tests/NoContainerSafetySpec.lua` —— `Safety` 是桩（`Safety.lua` 零测试）；手写桩与 `Bootstrap.lua` 14 字段表不同步；`FindNextEligible` 桩返回全 nil 使主路径未测。
131. `_NonRelease/Tests/BagEligibilitySpec.lua` —— **测试强化了生产上的死代码路径**（`C_Container` 桩）；第三条路径未覆盖；把实现魔数（0.5、1.05）写进断言。
132. `_NonRelease/Tests/BagSpaceReadinessSpec.lua` —— `Safety:CanRun` 的 5 个早期返回分支一个都没测；`LOOT_OPEN`/`SENSITIVE_UI` 未测；`family = 0` 桩掩盖了隐患。
133. `_NonRelease/Tests/DeferredEligibilitySpec.lua` —— `RunNextTimer` 用"最短 delay 优先"模拟定时器，与真实时间无关；超时路径未测；把 `Queue.lua:52` 的 0.5 写进断言。
134. `_NonRelease/Tests/QueueScanIsolationSpec.lua` —— **本项目质量最高的 spec**（第 9 行故意让 `PLAYER_UNGHOST` 抛错验证 pcall 隔离），但 `eventHandler` 只捕获从不调用 → `Queue:OnEvent` 全部分支（含 `_SHOW$` 高危 bug）仍零覆盖；把"仅靠数量判定成功"固化为正确行为；`RunNextTimer` 与 `DeferredEligibilitySpec` 逐字重复。
135. `_NonRelease/Tests/StartupLifecycleSpec.lua` —— 只测 `StartPostLogin` 幂等；未测 `Initialize` 的 `IsLoggedIn` 提前触发风险；未测 `Print` 三态过滤与 `warned` 去重。

---

## 五、YiboQuestBlocker（4 个源文件 + `.toc`，已完成；另有 1 个死文件）

### 严重

1. **`YiboQuestBlocker_Core.lua:1384-1398` `InstallSelectionGuard` 存在真实 API 兼容 bug** —— wrapper 签名 `function(questID, ...)` 假设首参是 questID，而 `C_GossipInfo.SelectAvailableQuest` 的签名是 `(index)` → **把序号当 questID 传给 `SkipBlockedQuestSelection`**，选择层判断错误。
2. **`YiboQuestBlocker_Core.lua:1368-1382` Hook 全局 `AcceptQuest`/`ConfirmAcceptQuest` 无逃生开关** —— 第 1374 行 `GetQuestID()` 在调用时可能返回上一个任务 ID（时序依赖）；第 1377 行 `if scope and RejectCurrentQuest(...) then return end` **静默吞掉调用** → 判断错误时玩家**无法接取任何任务**，且无禁用开关。
3. **`YiboQuestBlocker_Core.lua:773-790` `RunAbandonQuestFlow` 无 `InCombatLockdown` 检查、无 `pcall`** —— 连续调用 4 个受保护 API（`SelectQuestLogEntry`/`SetAbandonQuest`/`AbandonQuest`/`ConfirmVisibleAbandonPopup`），战斗中会触发 "Interface action failed because of an AddOn"。**全文件也没有 `PLAYER_REGEN_DISABLED`/`ENABLED` 监听**。
4. **`YiboQuestBlocker_Core.lua:732-744` `ConfirmVisibleAbandonPopup` 直接 `StaticPopup_OnClick(popup, 1)`** —— **程序化点击系统确认弹窗**，绕过用户确认；只查 `StaticPopup1..4`，被其它插件占用时可能误点。
5. **`YiboQuestBlocker_Core.lua:924` `string.find(questName, "产品订单", 1, true)` 用中文做业务判断** —— 英文/其它语言客户端下**完全失效且静默**。
6. **`YiboQuestBlocker_Core.lua:354-363` `SetRuntimeSetting` 是真实 bug** —— 第 359 行 `not not value` 把 `processMode` 的字符串值强制转成布尔 → `SetRuntimeSetting("processMode", "paused")` 会存入 `true`。
7. **`YiboQuestBlocker_Core.lua:96-131` 迁移无版本门禁** —— `SCHEMA_VERSION = 3`（9 行）只被写入、从不读取比较；`MigrateV1Database()` 在 133 行无条件执行，靠"v1 字段已清空"的副作用幂等。
8. **`YiboQuestBlocker_Core.lua:26-39` `MergeTables` 布尔语义是"只增不减"**（`target[key] = target[key] or value`）—— 布尔永远无法被合并为 false；用于 v1→v3 迁移时新值永远输给旧值，且无注释说明。
9. **`YiboQuestBlocker_Core.lua:800-802` `TryAutoAbandonQuest` 把 `canAbandon == nil`（未知）当作可放弃** —— 与 YiboAutoOpen `BagAdapter` 的 `locked = false` 同类"未知伪装成安全"。
10. **`YiboQuestBlocker_Core.lua:808-865` 自动放弃状态机每次只处理 1 个任务后递归，每任务固定 3 秒**（832 行）—— 日志 20 个被屏蔽任务需 60 秒；期间一旦打开任务界面整体挂起；第 862 行在循环内 `PersistDB()` 频繁落盘。
11. **`YiboQuestBlocker_Core.lua:1286-1312` `AbandonRejectedQuestsInLog` 无战斗检查、无 `pcall`、无确认**（即 `AccountPage` 那个按钮的后端）。
12. **`YiboQuestBlocker/AccountPage.lua:321-323` 破坏性操作无确认框** —— 一键放弃日志中所有已拒绝任务，**无确认对话框、无风险文案**（AGENTS.md 明确要求）。
13. **`YiboQuestBlocker/AccountPage.lua:538-547/550-560` `IsQuestBlockedByAny`/`GetBlockStatus` 每次遍历全角色**，却分别被**逐任务**调用（`Refresh:403`、`RenderQuest:226`、`GetSurfaceMetrics:448`）→ O(任务 × 角色 × 屏蔽)。
14. **`YiboQuestBlocker/Settings.lua:3` 与 `AccountPage.lua:3` 模块级捕获 `Core.UITheme`/`_G.YiboCore.UITheme` 无保护**；`CoreIntegration.lua:1-2` 在文件顶部捕获 `_G.YiboCore`/`_G.YQB`，若此时不存在则**永久为 nil 且无法恢复**。
15. **`YiboQuestBlocker_Core.lua:194-224` `knownAutomation` 硬编码 5 个第三方插件名 + 中文 note**；`["Blitz"]`（223 行）缺 `continuationStatus`/`continuationNote`，靠 425 行 `or` 兜底。
16. **`YiboQuestBlocker_Core.lua:874-937` `GetQuestTypeFlags` 依赖 `QuestieDB` 全局**（885 行，无 `type()` 检查）；906-916 行**重复调用** 886-901 行已调用过的查询；**918 行 `bit.band` 依赖 `bit` 库**（Retail 已移除）。

### 中等

17. `YiboQuestBlocker_Core.lua:176-178` —— 三种打印前缀（`|cff00ccff[YQB]|r`/`|cffff0000[YQB]|r`/`|cff888888[YQB]|r`）全部违规（颜色 + 简写）。
18. `YiboQuestBlocker_Core.lua:134-169` 与 `:236-311` —— **同一批默认值定义两遍**；`filters` 6 个字段两处；`previewColumns` 三处（含 `CoreIntegration.lua:26`）；`groupExpanded` 两处（含 `AccountPage.lua:66/73`）；`characterData[curCharacterID]` 默认结构两处（135/304 行）。
19. `YiboQuestBlocker_Core.lua:294-303` —— 角色 ID 迁移时 `latestID` 为 nil 会导致 `characterData[nil]` 索引报错。
20. `YiboQuestBlocker_Core.lua:277-292` —— `levelOp`→`levelExpr` 迁移用中文枚举 `"以上"/"以下"/"等于"/"至"` 做字符串比较；v1 若存英文枚举则**静默清空等级过滤**。
21. `YiboQuestBlocker_Core.lua:170` —— 注释声称本地引用"避免被外部篡改"，但 `PersistDB:226-234` 把 `YQBDB.*` 再赋回全局 → "防篡改"不成立；外部整体替换 `YiboQuestBlockerDB` 会导致数据分裂。
22. `YiboQuestBlocker_Core.lua:346-350` —— `IsAutoAbandonEnabled` 名实不符；**`YQBDB.filters.autoAbandon` 是死配置**（只在 148/267/273-275 行做默认值初始化，全文无读取点）。
23. `YiboQuestBlocker_Core.lua:351-353/368-370/395-397` —— `GetRuntimeSettings`/`GetProcessModeOptions`/`GetDiagnostics` 都返回内部表引用。
24. `YiboQuestBlocker_Core.lua:398-402` —— `ClearDiagnostics` 无调用点，而 `Settings.lua:58-60` 注释明确说"不提供该操作" → 死 API。
25. `YiboQuestBlocker_Core.lua:393-394` —— `GetRejectMode`/`SetRejectMode` 兼容别名**无移除计划**。
26. `YiboQuestBlocker_Core.lua:1021-1047` —— `CharPassLevelFilter` 全文无调用点（死函数）。
27. `YiboQuestBlocker_Core.lua:966-968` —— `parseLevelExpr` 静默纠正用户输入（`"90-10"` → `10-90`），无提示。
28. `YiboQuestBlocker_Core.lua:1002-1005` —— `ValidateLevelExpr` 的 `badToken` 被 `AccountPage.lua:311` 丢弃 → 错误提示无法指出问题 token。
29. `YiboQuestBlocker_Core.lua:941-948` —— `NormalizeLevelExpr` 不处理全角空格/全角逗号。
30. `YiboQuestBlocker_Core.lua:491-500` —— `AddDiagnostic` 用 `while #entries > 30 do table.remove(entries, 1) end`（O(n²)）；`time and time() or 0` 无 `type()` 检查；**诊断写入 SavedVariables**。
31. `YiboQuestBlocker_Core.lua:494` vs `:668/671` —— `time()`（本地 UNIX 秒）与 `GetTime()`（进程启动秒）**两套时钟写入同一子系统**。
32. `YiboQuestBlocker_Core.lua:1070-1109` / `:1111-1133` / `:1135-1148` —— **三个近乎逐行重复的日志扫描实现**。
33. `YiboQuestBlocker_Core.lua:1050-1066` —— `QuestPassFilter` 与 `GetCurrentQuestList` 的过滤逻辑重复；1051 行 `GetQuestLogIndexByID` 无 nil 检查。
34. `YiboQuestBlocker_Core.lua:1076`（及 1119 行）—— `GetQuestTypeFlags` 在 `name` 判空（1079 行）**之前**调用 → 对无效 idx 也执行完整 QuestieDB 查询。
35. `YiboQuestBlocker_Core.lua:1151-1189` —— `GetBlockedQuestList` 遍历全部角色；1181-1186 行排序先按 id 再按 name（id 唯一时 name 比较无意义）。
36. `YiboQuestBlocker_Core.lua:451/868/650/676/1075/1116/1140/1297` —— **位置式 `GetQuestLogTitle`**，与项目内 YiboTodo 已改用 `C_QuestLog.GetInfo` 不一致。
37. `YiboQuestBlocker_Core.lua:568-587` —— `DAILY_OVERRIDE_IDS` 19 个硬编码 ID 无来源/版本标注，横跨 MoP（29433）与 Legion/BfA（32642-32945）区间。
38. `YiboQuestBlocker_Core.lua:589-593` —— `tableSize` 是同一 `pairs` 计数的**第三份**实现。
39. `YiboQuestBlocker_Core.lua:527/533` —— `activeBatch.maxActions = 1` 与 `activeBatch.actions` 均无读取点（死字段）。
40. `YiboQuestBlocker_Core.lua:510-519` —— `EndQuestBatch` 用字符串前缀扫描删除 `directRejectActions`（O(n) 全扫）。
41. `YiboQuestBlocker_Core.lua:1315-1326` —— `GetStats` 第 1316/1318 行重复调用 `tableSize(globalBlocked)`；`characterData[curCharacterID].blocked` 无 nil 保护。
42. `YiboQuestBlocker_Core.lua:1436-1438` —— `YQB.SyncUIBindings` 在三个文件中均无定义 → 死调用。
43. `YiboQuestBlocker_Core.lua:1463-1473` —— `QUEST_DETAIL` 与 `QUEST_ACCEPT_CONFIRM` 是两个空分支（`if` 体内只有注释）。
44. `YiboQuestBlocker_Core.lua:1475-1479` —— `QUEST_LOG_UPDATE` 每次都 `NotifyCorePageChanged()`（无节流）→ 频繁刷新 Core 页面。
45. `YiboQuestBlocker_Core.lua:1440-1454` —— `PLAYER_ENTERING_WORLD` 每次进副本/换图强制全量扫描日志 + 清空 4 个状态表。
46. `YiboQuestBlocker_Core.lua:1413-1425` —— `frame:RegisterEvent` 11 次**无 `pcall`**（对比 YiboAutoOpen 有逐事件隔离）。
47. `YiboQuestBlocker_Core.lua:21` —— `local ADDON_NAME = ...` 依赖 `.toc` 传参；被 `dofile` 加载时为 nil。
48. `YiboQuestBlocker_Core.lua:22` —— `assert(_G.YiboCore, ...)` 错误消息为英文。
49. `YiboQuestBlocker_Core.lua:329` —— `DEFAULT_CHAT_FRAME:AddMessage` 无 nil 保护。
50. `YiboQuestBlocker_Core.lua:417-431` —— `GetAutomationAdapters` 每次调用做 5 次元数据查询，而 `Settings.lua:41` **每次面板重建都调用** → 无缓存。
51. `YiboQuestBlocker_Core.lua:1263-1276` —— `RemoveCharBlock` 的 `PersistDB`/`NotifyCorePageChanged` 被包在 `if` 块内（1273-1274）→ 数据不存在时**静默不落盘**；1270-1272 行的 `elseif` 嵌套易误读。
52. `YiboQuestBlocker_Core.lua:1192-1213` —— `AddBlock` 的 `scope` 只判 `== "global"`，其它任何值都走 char 分支；1206 行 `GetQuestName` 返回值被丢弃。
53. `YiboQuestBlocker_Core.lua:172-173` —— `local blocked`/`local cache` 使用不一致（`IsQuestBlocked:479`、`GetBlockedQuestList:1156` 直接用 `YQBDB.globalBlocked`）。
54. `YiboQuestBlocker/Settings.lua:14` 与 `:61` —— **同一个 `388` 两处**（`SetHeight(388)` 与 `return y`），动态算出的高度不用来设置。
55. `YiboQuestBlocker/Settings.lua:18/34` 与 `:36/56` —— 高度参数两套数字（250 vs 260、118 vs 128）。
56. `YiboQuestBlocker/Settings.lua:12` —— `math.max(560, parent:GetWidth() or 0)` 兜底失效，窄窗口下溢出。
57. `YiboQuestBlocker/Settings.lua:21` —— 缓存的 `modeControl` 父级不更新（每次可能返回新 section）→ 重建后仍挂旧父级。
58. `YiboQuestBlocker/Settings.lua:23` —— `SetOptions(options)` 重复设置（创建时已传）。
59. `YiboQuestBlocker/Settings.lua:32` —— 约 200 字中文长文案硬编码。
60. `YiboQuestBlocker/Settings.lua:51` —— `adapter.label` 无 nil 保护；三元表达式内联在 `string.format` 参数里。
61. `YiboQuestBlocker/Settings.lua:44-50` —— `continuationStatus` 是自由字符串无枚举白名单；三条状态文案中文硬编码。
62. `YiboQuestBlocker/Settings.lua:58-60` —— 注释在解释被删除的功能，与代码脱节。
63. `YiboQuestBlocker/Settings.lua` —— **缺少 `数据与缓存` 分区**（AGENTS.md 要求）。
64. `YiboQuestBlocker/Settings.lua:61` —— `return y` 返回值语义未说明。
65. `YiboQuestBlocker/AccountPage.lua:10` —— `CHARACTER_COL_W` 与 `COMPACT_CHARACTER_COL_W` **赋成同一个值**，语义丢失，且模块加载时求值。
66. `YiboQuestBlocker/AccountPage.lua:11` —— `PREVIEW_MARGIN`/`PREVIEW_SCROLLBAR_GUTTER`/`CORE_PREVIEW_BORDER` **全文件无使用点**。
67. `YiboQuestBlocker/AccountPage.lua:131-132` —— `row.characterBands`/`row.characterBandEdges` 创建后**无任何读写**。
68. `YiboQuestBlocker/AccountPage.lua:151-153` —— `FinishCharacterColumnBorder` 是空函数，但 407 行仍调用。
69. `YiboQuestBlocker/AccountPage.lua:106-108` —— `ScopeControlsWidth` 全文无调用点（含魔数 96）。
70. `YiboQuestBlocker/AccountPage.lua:294/338` —— `instance.scopeButtons` 是死集合（无写入点）。
71. `YiboQuestBlocker/AccountPage.lua:284-314/336/367` —— **等级过滤控件是死 UI**（336 行 `Hide()` 使其永不可见，控件未删除）。
72. `YiboQuestBlocker/AccountPage.lua:275-276/330` —— `instance.title`/`instance.summary` 永不可见（全文无 `Show()`），但 361/369 行仍 `SetText`。
73. `YiboQuestBlocker/AccountPage.lua:34-39` —— 依赖 Core 主题控件的**私有字段** `check.box`/`check.label`/`check.mark`/`check.disableHoverAccent`。
74. `YiboQuestBlocker/AccountPage.lua:52` —— `CharacterIdentity` 用 `-` 分割"名-服务器"（`^(.-)%-(.+)$`），角色名/服务器名含 `-` 时解析错误。
75. `YiboQuestBlocker/AccountPage.lua:61` —— `DB()` 在两者都不可用时返回**一次性空表** → 写入丢失且无告警。
76. `YiboQuestBlocker/AccountPage.lua:66` —— `IsGroupExpanded` 读函数写 db 且不落盘；默认表在 66 行与 73 行重复定义。
77. `YiboQuestBlocker/AccountPage.lua:84-88` —— `SetGroupExpanded` 无节流（快速点击累积 `C_Timer.After(0)`，每个都触发全量 `context:Refresh()`）；76 行 `SuppressPreviewClose(0.75)` 硬编码。
78. `YiboQuestBlocker/AccountPage.lua` —— **分组行与数据行共用同一个 `rows` 池**（`RenderGroup`/`RenderQuest` 都调 `Row()`）→ 同一 Frame 在不同渲染轮次角色互换，`groupArrow` 残留、`task:SetWidth(600)` 与 `taskWidth-12` 来回切换。
79. `YiboQuestBlocker/AccountPage.lua:203/241/398` —— **循环内 O(n) 次调用 `YQB.GetCurrentCharacterID()`**（3 处）。
80. `YiboQuestBlocker/AccountPage.lua:232/242/267` —— 每次渲染重绑 `SetScript("OnClick")`/`OnMouseUp`。
81. `YiboQuestBlocker/AccountPage.lua:167-175` —— `SetStatusCheckbox` 每格重建 `visualizer` 闭包；176 行 `SetChecked(blocked)` 的 `blocked` 可能为 nil。
82. `YiboQuestBlocker/AccountPage.lua:328` —— `local db, filters = DB(), DB().filters or {}` 调 `DB()` 两次，且 `db` 变量全文未使用。
83. `YiboQuestBlocker/AccountPage.lua:391/396` 与 `:440/447` —— `GetBlockedQuestList`/`GetCurrentQuestList` 在 `Refresh` 与 `GetSurfaceMetrics` 各取一次，结果可能不一致。
84. `YiboQuestBlocker/AccountPage.lua:383/384` —— 硬编码 page id `"quest-blocker"`，与 `CoreIntegration.PAGE_ID` 重复。
85. `YiboQuestBlocker/AccountPage.lua` —— 魔数 `250` 三处（380/432/473）、`29` 两处（295/466）、`156` 两处（283/344）。
86. `YiboQuestBlocker/AccountPage.lua:382` —— `instance:GetWidth() or 1` 兜底失效（0 非 nil）→ 负数被 `math.max` 侥幸兜住。
87. `YiboQuestBlocker/AccountPage.lua:344/352-353` —— 同一控件反复换父级（`SetTitleBarControl` ↔ `SetParent`）→ 每次刷新触发重排。
88. `YiboQuestBlocker/AccountPage.lua:451/453` —— `GetSurfaceMetrics` 有写副作用（`IsGroupExpanded` 会写 `db.settings.groupExpanded`）。
89. `YiboQuestBlocker/AccountPage.lua:495-501` —— `GetLayoutMetrics` 全文无调用点（注释自称"no longer drive layout"）；`GetPreviewSize` 仅被 `GetHoverMetrics` 调用。
90. `YiboQuestBlocker/AccountPage.lua:505` —— `GetHoverMetrics` 硬编码 `minWidth = 420`、`minHeight = 150`。
91. `YiboQuestBlocker/AccountPage.lua:115` —— 行 Frame 只隐藏不回收。
92. `YiboQuestBlocker/AccountPage.lua` —— 约 20 处中文硬编码文案，无本地化。
93. `YiboQuestBlocker/AccountPage.lua:304-306` —— `self:SetChecked(not self:GetChecked())` 手动翻转三处重复，若 Core 的 checkbox 自带翻转会双重翻转。
94. `YiboQuestBlocker/AccountPage.lua:315-318` —— `Add(scope)` 的 `tonumber` 无范围校验；`YQB.AddBlock` 返回值忽略。
95. `YiboQuestBlocker/CoreIntegration.lua:47` —— 失败路径不设"已尝试"标记，任何再次调用都重走完整流程。
96. `YiboQuestBlocker/CoreIntegration.lua:54/63/84/114` —— **无回滚**：`RegisterAddon` 成功后若后续注册失败，已注册部分不撤销且 `initialized` 保持 false → **重复注册**。
97. `YiboQuestBlocker/CoreIntegration.lua:13` —— `GetEligibleCharacters` 的 `context` 参数完全未使用（死参数）。
98. `YiboQuestBlocker/CoreIntegration.lua:9/16/39` —— `HasCharacterSnapshot` 每次都调 `YQB.GetDatabase()`；`GetEligibleCharacters` 与 `GetSummary` 各遍历一遍角色表 → 每角色至少 2 次全库获取。
99. `YiboQuestBlocker/CoreIntegration.lua:36-44` —— `GetSummary` 重复了 `GetEligibleCharacters` 的计数逻辑。
100. `YiboQuestBlocker/CoreIntegration.lua:23-28` —— `PreviewFields` 读函数带写副作用且不落盘。
101. `YiboQuestBlocker/CoreIntegration.lua:30-34` —— `SetPreviewField` 每次 `PersistDB()` + `NotifyCorePageChanged()`，无节流。
102. `YiboQuestBlocker/CoreIntegration.lua:48` —— 做了方法存在性检查（好），但只判真值未检查 `type(...) == "function"`。
103. `YiboQuestBlocker/CoreIntegration.lua:58-61` 与 `:92` —— 默认 `levelExpr = "90"` / `defaultExpression = "90"` **两处硬编码**。
104. `YiboQuestBlocker/CoreIntegration.lua:64-80` —— `Inspect`/`Delete` 闭包中 `YQB.GetDatabase().characterData[...]` 无 nil 保护；文案中文硬编码。
105. `YiboQuestBlocker/CoreIntegration.lua:84-112` —— `RegisterPage` 参数表约 28 个键（含 6 个 `YQB.AccountPage.*` 透传）→ 契约面过大。
106. `YiboQuestBlocker/CoreIntegration.lua:111` —— `GetActions = function() return {} end` 恒返回空表，与 AGENTS.md"任务、目标和行动数据必须完全一致"的表述不符。
107. `YiboQuestBlocker/CoreIntegration.lua:115` —— `legacyIDs = { "YiboQuestBlocker" }` 保留旧 ID 但无移除计划记录。
108. `YiboQuestBlocker/CoreIntegration.lua:123-129` —— 检查了 `Core.AccountView` 却未检查 `Core` 本身；`Toggle(PAGE_ID)` 无 `type()` 检查。
109. `YiboQuestBlocker/CoreIntegration.lua:136-139` —— `SLASH_YQB1` 三字母合规，但**无子命令、无诊断入口、无输出**（与 AGENTS.md"探针与诊断仅通过插件 Slash 命令触发"不符）；`SlashCmdList["YQB"]` 键名与插件名不一致。
110. `YiboQuestBlocker.toc:4` —— `## Author: Reasonix` 与其它插件的 `YiboSoft` 不一致。
111. `YiboQuestBlocker.toc:2` —— `## Title` 只有技术名、无中文名，与 `YiboAutoOpen - 自动开包` 格式不一致。
112. `YiboQuestBlocker.toc:5` —— `## Version: 3.0.2` 与 `CoreIntegration.lua:54` 的 `version = "3.0.2"` 两处硬编码。
113. `YiboQuestBlocker.toc:12-15` —— **`YiboQuestBlocker_UI.lua`（1291 行）不在加载列表，是永不加载的死文件**。
114. `YiboQuestBlocker.toc:6` 与 `CoreIntegration.lua:85/116` —— **icon 路径三处重复**，且无扩展名。
115. `YiboQuestBlocker.toc` —— `## LoadOnDemand: 0` 无意义声明；`## Notes` 中文硬编码无 `Notes-zhCN`。
116. `YiboQuestBlocker/.tmp-libdatabroker/LibDataBroker-1.1.lua` —— 与 `Libs/LibDataBroker-1.1/` 重复的散落临时文件。
117. `YiboQuestBlocker/dist/` —— 3 套 `1.2.x` 构建产物与当前 `3.0.2` 严重脱节，混在源码目录中。

---

## 六、跨插件补充发现（第二轮新增）

118. **"默认值合并"实现已达 8 份**：新增 `YiboReputation/Bootstrap.lua:7-12`、`YiboCurrency/Bootstrap.lua:6-10`、`YiboBuilds/Bootstrap.lua:11-20`、`YiboMounts`（无，改由生成数据提供默认）。语义仍各不相同，无共享实现。
119. **时钟混用新增 3 处**：`YiboBuilds/Bootstrap.lua:22-24`（`GetServerTime() or time()`，且 `GetServerTime()` 返回 `0` 时 `0` 为真值，兜底失效）、`YiboBuilds/AccountPage.lua:216`（用本地 `date()` 格式化服务端时间戳）、`YiboMounts/InventoryProbe.lua:62`（用 `!` 前缀取 UTC，属**正确**用法，可作范例）。
120. **本地化即逻辑（中文做判据）新增 6 处**：`YiboBuilds/Snapshot.lua:49/63/74/84`（`"通用"` 既是返回值又是判据）、`YiboBuilds/AccountPage.lua:143`、`YiboBuilds/AccountPage.lua:513/556/558/559/560/569`（`"当前"/"已学"/"未学"` 状态串参与分支）。
121. **`GetWidth()`/`GetHeight()` 返回 `0`（真值）导致 `or N` 兜底失效的新增 6 处**：`YiboCurrency/AccountPage.lua:212`、`YiboCurrency/Settings.lua:27`（**直接导致两栏布局永不生效**）、`YiboBuilds/AccountPage.lua:420`、`YiboBuilds/AccountPage.lua:382`、`YiboMounts/SourceFactProbe.lua`（无）、`YiboReputation` 多处。**`YiboBuilds/AccountPage.lua:675-678` 用 `< 1` 判定是唯一正确写法。**
122. **版本号三处不同源**：`.toc` 的 `## Version`、`Namespace.lua`/`Bootstrap.lua` 的 `Addon.VERSION`、生成数据的 `catalogVersion`。`YiboMounts` 最严重（`.toc` 0.6 / `NS.VERSION` 0.5 / `catalogVersion` "0.10"）；`YiboCurrency`（0.5）、`YiboBuilds`（0.1.0）、`YiboReputation`（1.3）为两处。
123. **`schemaVersion` 只写不读/静默门禁**：`YiboBuilds/Bootstrap.lua:29/31`（写两次、无任何迁移比较）、`YiboQuestBlocker` 的 `SCHEMA_VERSION`、`YiboMounts/Bootstrap.lua:7`（**版本不匹配时插件静默完全失效，无任何提示**）。
124. **在共享 `GameTooltip` 上挂私有字段**：`YiboReputation/AccountMatrixView.lua:33-39`（`GameTooltip.YiboReputationMatrix`）、`YiboMounts/Tooltip.lua:10/24`（`tooltip.__yiboMountsSignature`）。两个插件各自向同一单例写自定义字段，互相不可见且无清理约定。
125. **具名 `GameTooltip` 帧泄漏 + UI 重名**：`YiboMounts/SourceFactProbe.lua:6`（`"YiboMountsNameProbe"`）、`:32`（`"YiboMountsQuestProbe"`）、`YiboBuilds/Snapshot.lua:65`（`"YiboBuildsGlyphRequirementTooltip"`）、`:179`（`"YiboBuildsEnchantScanTooltip"`）—— 均定义在函数体内且用固定全局名，**每次调用新建一个帧**，第二次同名会被客户端自动改名，随后 `_G[name .. "TextLeft1"]` 取错控件。
126. **伪造链接/伪造 GUID 解析名称**：`YiboMounts/SourceFactProbe.lua:8`（`unit:Creature-0-0-0-0-%d-0000000000`）、`:34`（`"quest:" .. id`）—— 依赖未文档化的 tooltip 行为。
127. **内部数据格式泄漏进玩家可见 UI**：`YiboMounts/SourceFormatter.lua:124/126`（输出 `"1500 currency:392"` / `"10 item:70909"`）、`:61`（未知类型输出 `"boss_drop"`）、`:100`（未知难度输出 `"TEN"`）、`:111`（缺失键输出 `"STANDING_XXX"`）。
128. **排序比较函数不安全**：`YiboMounts/Catalog.lua:44-57` 的 6 分支比较函数依赖 `tonumber(priority)` 与 `tostring(sourceID)` 的字节序；`YiboReputation/ReputationHierarchy.lua:143-149`、`YiboCurrency` 亦按字节序排中文。反例（正确写法）：`YiboMounts/InventoryProbe.lua:48`。
129. **`record`/`payload` 等参数缺 nil 守卫**：`YiboMounts/SourceFormatter.lua:58`（`record.status`）、`YiboCurrency/CoreIntegration.lua:48`（`payload.domainID`）、`YiboCurrency/Data.lua:135`（`value.total`）—— 均会在异常路径直接报错。
130. **死本地化键**：`YiboMounts/Locale/{enUS,zhCN}.lua` 的 `PRICE`、`ALLIANCE`、`HORDE` 三键（两语言共 6 条）**全项目无引用**；`Namespace.lua:24` 的 `NS:SafeCall` **零调用者**。
131. **`数据与缓存` 分区缺失扩大**：`YiboCurrency/Settings.lua`、`YiboBuilds`（**完全没有设置页**）、`YiboMounts`（**完全没有设置页与命令开关**）—— AGENTS.md 要求的业务设置分区与"破坏性动作置于该分区末尾"在这三个插件中均未落实。
132. **角色数据清理登记只有 1/5**：仅 `YiboBuilds/CoreIntegration.lua:14-26` 注册了 `Core.CharacterCleanup:RegisterOwner`；`YiboCurrency`、`YiboReputation`、`YiboQuestBlocker`、`YiboTodo` 均未注册 → 删除角色的缓存会永久残留在 SavedVariables。
133. **诊断数据写入生产 SavedVariables 且无上限**：`YiboMounts/InventoryProbe.lua:61-69`（含整个坐骑图鉴的本地化 description/sourceText）、`YiboMounts/SourceFactProbe.lua:114-116`（全坐骑 × 8 类名称）。
134. **发布包违规**：`YiboMounts/Data/SourceFacts.generated.lua:1` 自述 `GENERATED NON-RELEASE PROBE DATA`，却出现在生产 `.toc:19` 的加载清单中。

---

## 七、YiboReputation（14 个文件）

### `YiboReputation.toc`（25 行）
135. 第 8–9 行 `RequiredDeps`/`Group: YiboCore` 合规。
136. 第 5 行 `## Version: 1.3` 与 `Bootstrap.lua:4` 重复；图标带 `-v1` 且无扩展名。
137. 第 1 行 `## Interface: 50504` 与 `YiboBeastPaths` 的 `50400` 冲突；无 `## X-GameFlavor`；`## LoadOnDemand: 0` 无意义；`## SavedVariables` 未标 schemaVersion。
138. 第 3 行 `## Notes` 中文且无 `Notes-zhCN`；加载顺序约束仅隐含。

### `YiboReputation/Bootstrap.lua`（45 行）
139. 第 7–12 行 `Defaults` 是第 6 份默认值合并实现。
140. 第 13–37 行 `EnsureDB` 一次做 6 件事。
141. **第 24 行 `settings.viewMode, settings.expansion, settings.category, settings.previewColumns = nil, nil, nil, nil` 在每次 `EnsureDB` 时执行，而 `EnsureDB` 被 `GetSettings`（第 38 行）调用 → 读设置即清空 4 个字段。**
142. **第 27 行 `settings.hoverMode = "monitored"` 无条件覆盖用户设置；第 28 行把 `defaultMatrixFilter` 强制为 `"all"`。**
143. 第 29–34 行静默清空非法 `matrixLevelExpr`；第 35 行静默截断 `monitoredFactionIDs` 至 10 项。
144. **第 39 行 `"|cff20e070[Yibo] 声望总览:|r "` 的 `|r` 位置错误（整条消息被染色）。**
145. 第 40 行匿名帧两个 `RegisterEvent` 无守卫；第 44 行只在 `PLAYER_LOGIN` 处理 `Initialize` 返回值，**无重试**；无 `PLAYER_LOGOUT`；`ADDON_NAME = ...` 在 `dofile` 下为 nil。

### `YiboReputation/CoreIntegration.lua`（85 行）
146. 第 1 行顶层 `_G.YiboCore` 捕获无守卫。
147. **第 4–7 行 `HasSnapshot` 只排除 `unavailable` → `error`/`stale`/`not-yet-scanned` 快照被当作有效数据渲染。**
148. 第 15–40 行 `RefreshPage` 手工隐藏 10+ 控件（主视图与悬停**共用同一页面实例**）。
149. 第 43 行只检查 `Core`/`Core.AccountView`，未检查 `DataDomains`/`Entry`/`Events`/`LevelFilter`；第 45 行**丢弃 `RegisterAddon` 返回值**；魔数 `5` 两处。
150. 第 64–66 行 `xpcall` 配 nil 返回的 handler → **静默吞掉度量错误并回退到魔数 21（第 63 行）**；`rows > 20`（68）/ 21（63）/ `math.min(rows, 20)`（76）三个不同步魔数。
151. 第 50 行重建含魔数 `200` 的兜底表；第 75–77 行 6–7 项单行表达式。
152. **第 80 行 `fields = {}` 空表 + `Bootstrap.lua:24` 清空 `previewColumns` → 预览列机制被整体移除，与 AGENTS.md 冲突**；`order = 40` 与 `YiboQuestBlocker/CoreIntegration.lua:85` 重复。
153. 第 80 行 `SetExpression` 重复校验；`defaultExpression = ""` 与 `Bootstrap.lua:14` 重复；**无回滚**（`RegisterPage` 成功后 `RegisterBusinessEntry` 失败仍会重复注册）；第 82 行无 `legacyIDs`；第 83 行用 `Addon` 表作 handler key 且 `NotifyChanged()` 无节流；`Addon._matrixSnapshotCache`/`_matrixNodeCaches` 下划线私有字段构成跨文件契约；icon 路径三处重复；`PAGE_ID` 重复 4 次；全部文案中文。

### `YiboReputation/Commands.lua`（7 行）
154. **第 5 行 `/yrp tree` 是保留的旧子命令（AGENTS.md 禁止旧别名）**；且该 `if` 未用 `elseif` → 打印提示后仍会执行 `ShowPage`。
155. 第 4 行只取首个 token，`%s` 不含 U+3000；第 6 行 `_G.YiboCore.AccountView:ShowPage(...)` 无 `type()` 检查并硬编码 `"reputation"`；**无任何诊断子命令**；无参数校验与用法文本；单行无缩进。

### `YiboReputation/Settings.lua`（24 行）
156. 第 4 行 `panel:SetWidth(parent:GetWidth())` 无兜底；`panel.yiboSettingsOwner` 硬编码；插件自行 `panel:Show()`。
157. **第 5 行无条件改写用户 `defaultMatrixFilter`（与 `Bootstrap.lua:28` 重复）。**
158. 第 5 行把按钮与复选框混在同一 `items` 表、用 `i == 1 or i == 3` 硬编码索引区分（4 处）；**第 1 项的 action 为 nil → 该按钮点了没反应**；第 16–18 行闭包捕获创建时的 `index`（增删后失效）。
159. 第 12 行显示 `（N / 10）` 但第 15–19 行**无上限校验**；第 20 行隐藏多余行不回收；第 22 行**返回数字**且未设 `BOTTOMRIGHT`；6 处 `context.notifyPageChanged()` 无节流；魔数 `252`；**缺 `数据与缓存` 分区，且无"添加监控"入口**。

### `YiboReputation/ReputationCatalog.lua`（99 行）
160. 第 3–33 行 `Addon.Catalog` 裸赋值、无 `Validator`、expansion 标题全中文。
161. **第 37–44 行 `NativeGroupExpansions` 以中文为键 → 非 zhCN 客户端全部键失配，第 74–79 行的推断分支整体落到 `"unclassified"/"未归类"`。**
162. 第 21/26 行设 `flat=true`、第 30 行不设（不一致）；第 27–29 行只有 3 个分类有 `primaryFactionID` → 第 58 行 `isPrimaryFaction` 几乎恒 false。
163. **第 63–64 行双 API 回退丢失元数据**（`RegisterReputationFactionIDs` 只收 ID 数组，丢掉 expansion/category/isPrimaryFaction）；无 `type()` 检查；"注册须在 PLAYER_LOGIN 前"仅存在于注释。
164. 第 66–70 行三个查询都是线性扫描并重建表；第 74 行 `tonumber(factionID) or 0`；**第 79 行用中文 group 名拼出 `"detected:" .. name` 作为分类 ID**；第 91 行硬编码中文；第 84/95 行两次调用 `C_Reputation.GetFactionDataByID`。
165. 第 93–99 行 `GetFactionIcon` 探测三个字段名（`icon`/`texture`/`factionIcon`）显示 API 不确定；**`C_Reputation` 是 8.0+，第 83/94 行在 MoP Classic（50504）上是死代码，与第 35–36 行注释矛盾**。

### `YiboReputation/ReputationHierarchy.lua`（152 行）
166. 第 27–46 行 v1/v2 分支每次查询全量扫描，v3+ 分支才用弱键缓存（不对称）；第 8 行弱键缓存以 `snapshot` 表为键，若 Core 每次返回新表则永不命中。
167. **第 32/140 行用中文 `"未知声望 "` 做逻辑（`== 1` 与子串匹配两种写法），且 `ReputationCatalog.lua:91` 又硬编码一次。**
168. 第 36 行静默放弃有歧义的迁移；**第 84–95 行 `primaryFactionID` 分支丢数据**：第 87 行先 `seen[primaryID] = true`，第 95 行 `if group` 为假时该阵营被标记已见却从未渲染。
169. **第 97–100 行硬编码公会阵营 ID `1168` 两处**（另见 `ReputationCatalog.lua:32`）；第 75 行 `not data` 在 73 行之后冗余。
170. **第 120–131 行 `FindExpansion`/`FindCategory` 是线性扫描且按需追加 → 每个未归类阵营一次 O(n) 遍历，整体 O(n²)**；第 122/128 行用中文标题做键；三个不同名的"未归类"（`"其它"` 137 / `"其它阵营"` 138 / 目录里的 `"未归类"`）。
171. 第 80 行反向遍历 Catalog（mop→cata→wotlk→tbc→classic）无注释；第 143–149 行只对 `:detected:` 分类按中文标题字节序排序；第 63–66 行叶子节点也分配 `children = {}`；第 57 行版本门禁正确（正面）但第 60 行探测两种键形；**三套版本阈值（`< 3`、`< 4`、`REPUTATION_SNAPSHOT_VERSION = 5`）无共享常量**。

### `YiboReputation/ReputationFormat.lua`（120 行）
172. **第 6 行 `STANDING` 与第 7 行 `STANDING_COLORS` 是两张按下标对齐的表**（改一处忘另一处即错位），且 `STANDING` 全中文。
173. **第 15 行 `FRIENDSHIP_NAME_COLOR_STANDINGS` 以中文名为键（`"陌生人"`…`"挚友"`）→ 非 zhCN 客户端永不匹配，友度颜色静默回退**；第 13–14 行注释同样硬编码中文。
174. **第 29/34 行每次调用分配新的默认颜色表**（每个矩阵单元格一次）；第 21–24 行 `HasFriendProgress` 与第 16–20 行 `Progress` 字段名不同（`current` vs `value`）却语义重复；"完成"判定在 4 处重复（46/48、55/65、73/75/79、89/97）。
175. **第 54/72/87 行用中文 `"好友度数据未就绪"` 做逻辑**（第 5 处中文即逻辑）；第 56 行硬编码阵营 ID `1359`。
176. **第 36–43 行 `FormatState` 返回 Unicode `⌚`（U+231A）可能显示为方框**，并用半角 `?` 作状态标记；**第 115/116 行返回同一个 `"—"`，115 行是死代码**；`FormatSnapshotValue` 只处理 `"not-yet-scanned"/"unavailable"`，而 `GetFactionState` 还会返回 `"known"/"stale"/"error"` → **`error` 未处理**；第 110 行忽略 `stale` 的 `state`（有意但未注释）；第 76 行长 `and/or` 链；`"— 不适用"`/`"—"` 硬编码 4 处；约 20 处中文。

### `YiboReputation/Friendship.lua`（25 行）
177. **`FALLBACK`（第 2 行）是中文名数组；`NAT_PAGLE_FALLBACK`（3）与 `LABEL_RANKS`（4）以中文为键 → 非 zhCN 客户端 `LABEL_RANKS[name]` 永不匹配、`rank` 为 nil，`IsComplete`/`FormatReputation`/`FormatCompact`/`FormatMatrix` 全部退化到"未就绪"分支。**
178. `LABEL_RANKS` 7 项而 `FALLBACK` 6 项，无交叉校验（`"熟人"`/`"同伴"` 同为 rank 2）；**第 7 行硬编码阵营 ID `1358`**（第 3 个硬编码阵营 ID，另见 `ReputationCatalog.lua:29` 的 `{1302,1358}`、`ReputationFormat.lua:56` 的 `1359`）。
179. 纳特·帕格特例被写死在函数内无扩展点；**第 18 行定义 `"好友度数据未就绪"` 被 `ReputationFormat.lua:54/72/87` 当逻辑哨兵使用**（跨文件隐式契约）；第 18 行长 `and/or` 链不处理空串 `reactionName`；第 23 行 `maxRank = tonumber(friend.maxRank) or (rank and 6)` 在 `rank` 为 nil 时产出两字段皆 nil 的表；第 24 行每次返回新表无缓存。第 11–13 行"仅在 Core 真正采集到友度数据时才视作友度阵营"的注释是好设计（正面）。

### `YiboReputation/Nomi.lua`（12 行）
180. 第 8 行是单行 4 条件 `and` 链；**第 8 行 `(GetServerTime and GetServerTime()) or time()` 混用服务器纪元与本地纪元**；**`cached.confidence == "verified"` 是自由字符串枚举且全项目无写入方 → 该分支不可达，第 11 行恒执行**。
181. 第 7 行 `self:EnsureDB().nomi` 在渲染路径触发 `Bootstrap.lua:24/27/28` 的副作用；`cached.expiresAt` 无类型检查；`cached.course` 直接 `tostring`；第 4 行中文；`GetNomiDisplay` 被 `ReputationFormat.lua:56` 动态调用。

### `YiboReputation/PreviewController.lua`（91 行）
182. **第 26–45 行 `GetPreviewMetrics` 全文无调用者**，其 31–44 行是注释掉的旧代码（14 行）；第 65–89 行又是 25 行注释掉的旧代码 → **约 39 行死代码**。
183. **第 13/16 行创建 `previewModeButton` 后立即 `Hide()`，全项目无 `Show()`**（死控件）。
184. 第 22–23 行两条约 300 字符语句手工隐藏约 14 个控件，**与 `CoreIntegration.lua:16/23-37` 的隐藏清单重叠但不完全一致**；**第 63 行 `verticalOverflow = "none"` 与 `CoreIntegration.lua:77` 的 `"content"` 互相矛盾**。
185. 第 30 行魔数 `420/1080/150/132/171`（`150` 又与第 60 行重复）；第 51 行 `#self:GetSettings().monitoredFactionIDs` 在热路径触发 `EnsureDB`；第 27–28 与 50–51 行的 `rows` 初始化方式不一致。第 4–10 行 `MatrixColumnWidth` 有 `type()` 检查与 `math.max(1, ...)`（正面）。

### `YiboReputation/CurrentCharacterPreview.lua`（45 行）
186. **整个模块是死代码**：`CreateCurrentPreview` 创建的控件被 `PreviewController.lua:16/22/23` 与 `CoreIntegration.lua:23-27` 每次刷新隐藏，而 **`RefreshCurrentPreview`（12–45）全项目无调用者**。
187. 第 13 行锚定到"创建后即永久隐藏"的 `parent.previewModeButton`（死依赖）；**第 20 行两次 `GetSettings()`，且两个字段已被 `Bootstrap.lua:24` 清空 → 恒取 `chosen = "mop"`**。
188. 第 23 行 `Row(kind)` 的 `kind` 参数未使用（死参数），该行约 400 字符、含 8 个方法调用与 4 个 `or` 兜底；第 27 行 `Count` 闭包每轮循环重建并遍历每个资料片子树 → O(节点 × 资料片)；第 28 行每次刷新重绑 `SetScript` 并触发整页重建。
189. **第 30 行是约 900 字符单行**（含嵌套递归函数、两个循环、`SetScript` 闭包与硬编码 `Core.AccountView:ShowPage("reputation",{autoFit=true})`）；用 `string.rep(" ", depth*2)` 做缩进（CJK 下错位）。
190. **第 33–43 行通过读取已渲染 UI 文本推断行类型**（`row.text:GetText()` 前两字符是否 `"+ "`/`"- "`），且 `string.sub(label, 1, 2)` 对中文标题只取到两个字节（半个字）；宽度魔数 4 处不一致（390/450/-150&142/430）；第 32 行隐藏多余行不回收；插件内三种缩进风格；约 8 处中文。

### `YiboReputation/MonitoredPreview.lua`（115 行）
191. 第 8–10/12–14 行 `Snapshot`/`Data` 调 `Core.DataDomains:Get` 对 `character`/`character.id` 无 nil 守卫，且与 `AccountMatrixView.lua:15-25` 各实现一份（缓存策略不同）。
192. **第 16–29 行 `ShowMonitoredTooltip` 手写命中测试**（`cursorX - (row:GetLeft() or cursorX)` 再按 `cellWidth` 取角色），无边界检查，滚动或 `cellWidth == 0` 时错位。
193. **第 22–25 行把 `/` 当作数据格式哨兵**（`detail:find("/")`），格式一改悬停直接消失（跨文件隐式契约）；第 26 行用共享 `GameTooltip` 且未加命名空间，与 `AccountMatrixView` 的 `GameTooltip.YiboReputationMatrix` 方案并存互扰。
194. 第 31–36 行 `monitoredPagerAnchor` 是裸 1×1 Frame，直接写 `parent` 字段（无重名检查）；第 48 行 `nameWidth = 150` 是同一魔数的**第四份**拷贝。
195. 第 49 行 `(tonumber(...) or parent:GetWidth() or 1)` 兜底失效；第 58–72 行表头循环用索引 `0` 表示名称列并反复 `SetScript(nil)`。
196. 第 76–109 行每次刷新重建 `Backdrop`、`ApplyDataColumnTints`、`values`，**并在第 97 行再次 `Data(character, factionID)` 取颜色，而第 88 行已取过一次同一快照 → 每格双倍查询**。
197. 第 105–107 行每行重绑 `OnEnter`/`OnUpdate`/`OnLeave`，**`OnUpdate` 在 `monitoredTooltipTracking` 为真时持续执行手写命中测试（每帧）**；第 111 行 `UpdateCurrentCharacterColumnOutline(..., 0, cellWidth, ...)` 传入字面量 `0`；第 112–113 行每次刷新重锚并调用 `UpdateColumnPager`（无节流）。

### `YiboReputation/AccountMatrixView.lua`（341 行）
198. 第 6–9 行 `MatrixColumnLayout = { nameWidth = 200, starWidth = 20 }` 与 `MonitoredPreview.lua:48`、`PreviewController.lua:30/60`、`CurrentCharacterPreview.lua` 构成**五套互不同步的列宽来源**。
199. **第 11–13 行 `GetMatrixCharacterColumnWidth(context, availableWidth, fixedWidth, characterCount)` 四个参数全部未使用**（恒返回常量 200）。
200. 第 15–25 行 `Addon._matrixSnapshotCache` 用 `false` 作"无快照"哨兵且 `return cached or nil`；**缓存只由 `CoreIntegration.lua:83` 整体置 nil**（跨文件下划线私有契约）。
201. 第 33–39 行在共享 `GameTooltip` 上挂 `GameTooltip.YiboReputationMatrix`/`YiboReputationMatrixRow`；第 41–77 行 `UpdateMatrixTooltip` 是**第二套独立手写命中测试**（含 effective-scale 手工换算与每行 tooltip 缓存）。
202. 第 79–81 行 `Faction`、第 83–86 行 `IsMonitored` 均为线性扫描，后者在每格渲染时被调用 → O(行 × 列 × 监控列表)；第 87–92 行 `ToggleMonitored` 以中文提示 10 项上限并 `return nil`；上限 `10` 与 `Settings.lua:12` 是两份常量。
203. 第 94–106 行 `CreateMatrixView` 直接写 `parent.matrixNodeCache`/`matrixBody` 等字段（无重名检查）。
204. **第 108–119 行 `GetMatrixNodeCache` 以 `table.concat(characterIDs,"|") .. ":" .. revision` 为键写入 `self._matrixNodeCaches`，该表只增不减（永不裁剪）→ 内存泄漏。**
205. **第 121–198 行 `GetMatrixNodes` 与 `ReputationHierarchy.GetTreeNodes` 大面积重复**：同样实现 primary/guild/flat/detected 四分支、同样硬编码 `seen[1168]`、同样用 `"其它"/"其它阵营"`、第 189 行同样用 `"未知声望 "` 做逻辑、第 194 行同样按字节序排 `:detected:`、第 137 行同样反向遍历 Catalog。
206. 第 200–204 行 `IsOpen` 依赖 `expanded.__all` 与默认 `"matrix:mop"`（`__all` 与真实节点键共用同一表）；第 205–210 行 `Matches` 对中文标题做 `string.lower`（无效）且不 trim。
207. 第 212–229 行 `GetMatrixSurfaceRowCount` 的遍历与第 290–298 行 `RefreshMatrixView` 内的 `Add` 是**同一遍历的第二份实现**（行数统计与渲染可能不一致）。
208. **第 231–340 行 `RefreshMatrixView` 是单个 110 行函数**；**第 234 行在 `matrixFilter ~= "monitored"` 时强制改写为 `"all"` —— `defaultMatrixFilter` 强制覆盖的第三份拷贝**（另见 `Bootstrap.lua:28`、`Settings.lua:5`）。
209. 第 237 行搜索框硬编码背景色（未走 `Theme.Colors`）；魔数 `152`（241）、`x = x + 72`（243）、按钮宽 `72`（254–255）散落三处；第 242 行硬编码过滤选项 `{"all","全部"}/{"monitored","已监控"}`；第 243 行每次刷新重绑 `SetScript`。
210. 第 246–253 行 `SetAllExpanded` 会清空逐节点展开/折叠覆盖（无撤销）；第 262 行 `(tonumber(...) or parent:GetWidth() or 1)` 兜底失效。
211. 第 264–265 行硬编码 `"reputation"`/`"matrix"`/`"角色"`；第 277 行传入中文 `recovery="登录该角色后同步声望数据。"`；第 288 行 `settings.matrixFilter or settings.defaultMatrixFilter` 与第 234 行的强制改写并存（过滤来源不唯一）。
212. **第 301–336 行行循环是一个约 2000 字符跨度**（302/304/309/310/317 各为超长单行），每行重绑 4 个脚本（`OnClick`/`OnEnter`/`OnUpdate`/`OnLeave`）；第 310 行用 `string.rep("　", depth)`（全角空格）缩进，与 `CurrentCharacterPreview.lua:30` 的半角缩进不一致；第 317 行每格重新 `NodeFaction(Snapshot(...), node)`；第 338–339 行在无节流热路径上设置尺寸并更新列描边。

---

## 八、YiboCurrency（8 个文件）

### `YiboCurrency.toc`（19 行）
213. 第 8–9 行 `RequiredDeps`/`Group: YiboCore` 合规。
214. 第 5 行版本与 `Bootstrap.lua:4` 重复；第 6 行图标带 `-v1` 无扩展名；第 1 行 `50504`；无 `## X-GameFlavor`（而 `Commands.lua:13-19` 同时探测 `C_CurrencyInfo` 与 `GetCurrencyInfo` 两代 API）；`## LoadOnDemand: 0` 无意义；`SavedVariables` 未标 schemaVersion；`## Notes` 中文无 `Notes-zhCN`；加载顺序约束仅隐含。

### `YiboCurrency/Bootstrap.lua`（39 行）
215. 第 6–10 行 `Defaults` 是第 7 份默认值合并实现；第 8–9 行单行 `if/elseif` + 递归，`type(...) == "table" and target[key] or {}` 在 `target[key] == false` 时返回 `{}`（语义错误分支）。
216. 第 13 行 `YiboCurrencyDB = YiboCurrencyDB or {}` 直接写全局。
217. 第 18–22 行 `if version < 3` 无条件清空 `previewColumns`/`hoverOrderOverride` 并重置 `monitored`，而第 24 行 `Defaults` 又把 `monitored = {}` 作为默认（两处重复）。
218. **第 25 行 `version = math.max(version, 3)`：若版本字段被写成非数字（第 14 行 `tonumber` 容错为 0），下次启动会重跑 `version < 3` 分支 → 用户监控列表被反复清空。**
219. 第 23 行用字符串 `"90"` 作迁移判据（跨插件第 3 处同类硬编码）；第 24 行的 `version = 3` 是死配置；第 28 行 `GetSettings()` 每次触发 `EnsureDB()`（读路径写副作用）。
220. 第 29 行前缀 `|cff20e070[Yibo] 货币管家:|r ` 颜色正确但插件名与 `.toc` 标题不一致，项目内无统一打印函数；第 31–32 行匿名帧无守卫；**第 35 行 `Addon.CoreIntegration` 不存在时静默跳过、不打印、不重试**。

### `YiboCurrency/Catalog.lua`（75 行）
221. 第 5–9 行 `Currency()` 写死 `icon = nil`（该字段实际由 `Data.lua:76-96` 运行时解析，属死字段）；**第 8/17 行 `verified` 出现 `"pending-client"`/`"implemented"` 两值，而 `Data.lua:34` 又写 `"client"` → 三种自由字符串枚举跨文件做逻辑。**
222. 第 20–22 行 `CURRENT`/`LEGACY`/`EXPIRED` 是**中文字符串常量**（第 3–4 行注释称 status 只是来源标记，但它会进入 UI 与逻辑）；第 24 行裸表赋值无 `Validator`；第 25 行 `money` 条目内联手写（第三种构造风格）。
223. 第 28–74 行 47 个条目的 `expansion` 全为中文展示串（若被用于分组/排序则非 zhCN 客户端失效）；**无 flavor 门禁**：同时收录 Classic/WotLK/Cata 已移除货币（`1900`/`241`/`81`/`61`/`361`/`393`/`416`）与 MoP 现行货币 → 在 MoP Classic 上大量条目落到 `Data.lua:115` 的 `"unsupported"`。
224. 第 30 行 `EXPIRED` 与 `LEGACY` 在数据层无任何差异处理（装饰性分类）；第 69/70/71/72/74 行 `sourceType = "专业货币"/"考古货币"` 作分类键；第 31–40 行用**物品代币**承载 PvP 奖章（MoP 已改为货币 ID）；所有条目 `totalAllowed = true`（常量字段）；`id` 前缀规则散落 5 处（`Catalog.lua:6/12`、`Data.lua:10/30/99`）。

### `YiboCurrency/Data.lua`（211 行）
225. 第 1 行 `Core` 在加载期捕获，全文件 20+ 处直接使用（仅第 18/90 行做了存在性检查）。
226. 第 2 行 `MAX_MONITORED = 16` 与 `Settings.lua:23` 文案、`Settings.lua:47` 判定共三处。
227. 第 4 行 `Copy` 是全项目第 4 份浅拷贝助手；**第 6–15 行 `GetCatalog()` 每次调用重建整表 + `known` 映射**，被 `GetMonitoredCount`/`GetMonitoredCatalog`/`AddCustomItem`/`RegisterCatalogWithCore`/`Settings`/`AccountPage` 反复调用。
228. 第 9 行 `or {}` 冗余且与第 35 行**各调用一次 `GetSettings()`（即 `EnsureDB()`）**；第 11 行 `"当前可获取"` 是 `Catalog.lua:20` 的第二份中文常量拷贝。
229. 第 17–25 行 `RegisterCatalogWithCore` 的一次性标志无重试触发点；第 24 行 `Core.DataDomains:Dispatch` 未检查 `Core.DataDomains`；第 20–21 行 `money` 条目两者皆无被静默跳过；第 18 行 `type()` 未检查。
230. 第 27–38 行 `AddCustomItem`：第 31 行用全表重建查重；第 32 行 `GetItemInfo` **解构 10 个返回值**；第 33 行要求物品已被缓存；**第 34 行 `verified = "client"` 绕过 `pending-client` 保护**；第 35 行两次 `GetSettings()`；第 36 行注册但**不派发第 24 行的目录事件**。
231. 第 40 行 `IsVisible` 用 `~= false`、第 42 行 `IsMonitored` 用 `== true`（**同一文件两套相反真值约定**）；第 43 行 `GetMonitoredCount` 每次 `IsMonitored` 都触发 `EnsureDB`；第 47 行单次操作 3 次全表遍历；第 57–63 行构建两张表后两轮遍历。
232. 第 65–73 行 `MoveMonitored` 写回整个顺序表（与第 49–50 行注释一致，正面）；第 76–96 行 `GetIcon` 使用 `GetItemIcon`（**Retail 已移除**）与 `C_CurrencyInfo`/`GetCurrencyInfo` 双路；**第 90–94 行用"当前角色"的经济域快照取图标**（把角色范围数据混入表现层），第 91 行 `Core.Characters:GetCurrent()` 无守卫；第 95 行硬编码问号图标。
233. 第 98–117 行 `GetValue`：第 99 行两个域名字符串内联；第 100 行无 `Core` 守卫；第 101 行只接受 `"known"`；**第 107 行在"读"路径改写 `entry.verified`（且 `GetCatalog` 返回的是 `Addon.Catalog` 的同一批表对象 → 等于修改模块级目录表），并在此处调用 `GetItemInfo`（每格一次）**；第 111/115 行自由字符串状态。
234. **第 119–124 行 `ValueState` 永不返回 `"bank"`，但第 145/196 行 `FormatCell`/`FormatFullCell` 与第 204/207 行 `TotalFor` 都专门处理 `"bank"`，第 210 行 `bankPending` 因此恒为 `false`（四处死分支，`complete` 的 `not bankPending` 恒真）。**
235. 第 135/149/188 行三处逐字重复同一取值表达式，而**第 205 行 `TotalFor` 的版本漏掉 `or value.quantity` → 物品代币合计可能与单元格显示不一致**；第 135 行在 `value` 为 nil 时直接解引用。
236. 第 137–139 行 `money` 分支先 `math.floor(quantity/10000)` 再与 `10000`/`1000` 比较（换算后阈值未同步），且 `万`/`k` 逻辑重复三份；第 183–186 行 `FormatLimitCell` 是纯透传包装（还把 `self` 当第一个参数传给 formatter，约定与 `FormatCell` 相反）；第 142–147 与 193–198 行两个近乎重复的函数；第 169–182 行 `"capped"`/`"near-cap"` 在本文件无消费方。

### `YiboCurrency/AccountPage.lua`（289 行）
237. 第 1–2 行 `Core`/`Theme` 模块级捕获无守卫；第 4 行 `ROW_HEIGHT` 在加载期取值。
238. 第 7–11 行 `Eligible` 每角色两次 `DataDomains:Get`；第 12/15 行中文兜底与 `"empty"` 魔法哨兵（与第 219 行配对）。
239. 第 17–20 行 `IconText` 手工拼纹理串（尺寸魔数 16 与 22/40/264 行各写一份）；第 21–23 行魔数 `52`/`18`；**第 24–41 行 `PreviewColumnWidth` 为量宽至少做 2 倍快照查询（第 37 行 `TotalFor` 内部又对每角色取一次）**；第 38 行 `bankPending` 死分支。
240. 第 46–58 行 `CharacterColumnWidth` 双层循环只为量宽，与第 170–176 行渲染循环重复同一批昂贵调用；第 63–65 行用中文字面量 `"货币"`/`"总计"`/`"29.4万"` 作测量基准。
241. 第 68–71 行 `StateColor` 处理不可达的 `"bank"`；第 76–77 行硬编码兜底色；第 81–84 行 `SnapshotMeta` 又调两次 `DataDomains:Get`。
242. **第 86–93 行 `CreateCurrencyPage` 直接往共享页面帧写 7 个字段**（无冲突检查）。
243. **第 98–122 行 `AddEntryTooltip` 第 110–118 行对每角色串行调用 7 个函数，其中 `ValueColor` 内部又重算 `ValueState` 与 `GetLimitState` → 同一格最多 4 次重复计算**；第 105 行"银行未完整扫描"图例与第 116 行的 `"bank"` 均为死逻辑；第 108 行 `AddDoubleLine` 一次传 8 个颜色参数。
244. 第 127 行 `#Addon:GetCatalog()` **重建整表只为取长度**；第 131 行只隐藏不回收遗留按钮。
245. **第 144–197 行 `Layout` 是单个 54 行函数**；第 164/177/194/246/247/256/268/271 行共 **8 处"隐藏超出索引的池对象"循环**（只隐藏不回收，分散在 5 个函数）；第 168/259 行每次刷新重设不变的 backdrop。
246. **第 173 行对每一行都调用 `TotalFor`（内部遍历全部角色）→ O(行 × 角色)**；第 174/265 行每格 4 次重复状态推导。
247. **第 179/183/184 行每次刷新为每行重绑 `OnEnter`/`OnLeave`/`OnClick` 并重复 `RegisterForClicks`**；闭包内再调 `IsMonitored`（触发 `EnsureDB`）。
248. **第 201 行对每个条目调用 `IsVisible`（一次 `EnsureDB`）**，配合 `GetCatalog` 全表重建，单次刷新成本 O(n) 次 DB 检查。
249. **第 211 行 `local scrollbarGutter = 0`，而其上方 207–210 行注释明确说明滚动条占 14px 且"必须计入角色列预算" → 注释描述了正确做法而代码设为 0。**
250. 第 212 行 `(tonumber(...) or parent:GetWidth() or 1)` 兜底失效（第 5 处）；第 226/232 行硬编码 `"matrix"`/`"角色"`；**第 230 行把视图状态写回共享目录条目表**；第 265 行 `entry.source == "empty"` 永不可达；第 281 行 `math.min(#characters+1, 21)` 与第 287 行 `math.min(#entries, 20)` 是 21/20 两个不同步魔数。

### `YiboCurrency/Settings.lua`（73 行）
251. 第 1–2 行 `Core`/`Theme` 模块级捕获无守卫；第 4 行 `StaticPopupDialogs["YIBO_CURRENCY_CONFIRM_ITEM"]` 无 `_G.` 前缀、无重名检查，第 5 行 `whileDead = true`；第 6 行 `OnAccept` 的 `data` 无 nil 守卫。
252. 第 8–13 行 `Label` 把控件池写在共享 `parent.ycuLabels` 上，用绝对 `y` 偏移定位；**第 14–16 行 `CatalogLabel` 与 `AccountPage.lua:17-20` 是同一纹理串构造的第二份拷贝。**
253. 第 19 行 `panel:SetWidth(parent:GetWidth())` 无兜底；插件自行 `panel:Show()` 并占用 `parent.ycuSettings` 字段；第 20 行 `GetCatalog()` 全表重建；第 23 行 `GetMonitoredCount()` 又全表扫描。
254. **上限 `16` 在本插件内出现三处**（`Data.lua:2`、第 23 行中文提示串、第 47 行判定）；第 23 行 `math.max(260, panel:GetWidth() - 8)` 魔数。
255. **第 27 行 `availableWidth = math.max(1, panel:GetWidth() or 1)`：`GetWidth()` 返回 `0`（真值）使 `or 1` 失效，`math.max(1, 0) = 1` → 第 35 行 `availableWidth >= groupWidth * 2 + columnGap` 几乎必为假 → 第 28–30 行注释所描述的两栏布局永远不生效。**
256. 第 31–32 行对全目录做 `MeasureText`；第 34 行 `104`/`108` 与第 46/47 行复选框宽度是同一组魔数的两份；**第 38 行 `rowsPerColumn` 在 `#catalog == 0` 时为 0，第 42 行 `math.floor(localIndex / rowsPerColumn)` 除零**；第 51 行又重算一遍同一量。
257. 第 46/47 行依赖 `Theme:CreateCheckbox` 返回对象带 `.label` 的 Core 私有契约；两处 OnClick 都同时调 `notifyPageChanged` + `refreshPage` → **每次勾选双重整页重建**；第 47 行 `SetAlpha(disabled and .45 or 1)` 魔数且失败时无回滚。
258. 第 24/25 行 `settings.visible={}` 是整表替换，且两个"恢复"按钮位于页面中部（**不在 `数据与缓存` 分区末尾**）；6 次 `notifyPageChanged` + 6 次 `refreshPage` 全部无节流与存在性检查；第 50/67 行只隐藏不回收。
259. 第 69 行 `SetFont(STANDARD_TEXT_FONT, Theme.Font.body, "")` 传空 flags；第 70 行依赖 `GetItemInfo` 首个返回值为名称；**只有"添加"没有"删除"，自定义条目一旦加入无法移除**。
260. **第 71 行 `panel:SetHeight(y); return y` 返回数字而非面板**，未设 `BOTTOMRIGHT`、无滚动支持（超长会被裁切）；**缺 `数据与缓存` 分区**。

### `YiboCurrency/CoreIntegration.lua`（53 行）
261. 第 1–2 行 `Core` 模块级捕获无守卫；第 4 行 `ICON` 是 `.toc:6` 的第二份拷贝（第 46 行再引用一次）。
262. 第 7 行有"已完成"标志但**无"已尝试"标志**；第 13 行 `RegisterPage` 成功后第 46 行失败仍会重复注册且无回滚。
263. **第 8 行未检查 `Core.LevelFilter`（20）、`DataDomains`（25/26）、`Events`（48）、`Characters`、`UITheme`（32/38）**；第 9 行 `CheckAPIVersion(5)` 与第 10 行 `requiredAPI = 5` 两处魔数；第 14 行 `order = 50`；`RegisterAddon` 无 `schemaVersion`；错误返回形式不统一。
264. **第 15 行 `fields` 只有一个字段且全插件不存在 `previewColumns`** → 悬停改由插件自有 `settings.monitored` 驱动，**废止了 Core 的预览列投影机制**；AGENTS.md 允许"任务型例外"但要求记录在实施方案中（需核对 `实施计划.md`）。
265. 第 17 行 `defaultExpression = ""` 与 `Bootstrap.lua:24` 重复；第 18 行 `GetExpression` 触发 `EnsureDB`；第 19–23 行 `SetExpression` 调用 `NotifyChanged()` 无节流。
266. 第 25 行每角色两次 `DataDomains:Get`；**第 26 行 `GetEligibleCharacters` 与 `AccountPage.lua:7-11` 是逐字重复的同一准入规则（第三份拷贝）**；第 32/38 行**绕过主题 API 直接读 `Core.UITheme.Geometry.*`**。
267. 第 27/28/29 行是三层多余包装闭包；**第 30–42 行 `GetHoverMetrics` 每次悬停重算全量度量**；**第 40 行硬编码 `horizontalOverflow = "content"`，与 `AccountPage.lua:287` 的 `"paginate"` 矛盾**。
268. 第 43 行 `GetSummary` 每次悬停全表扫描且 `#characters` 无 nil 守卫；**第 46 行 `text = "[Yibo] 货币总览"` 缺 `|cff20e070[Yibo]|r` 前缀**，无 `legacyIDs`。
269. 第 48 行用 `Addon` 表作 handler key、`payload` 无 nil 守卫、`NotifyChanged()` 无节流；第 49 行 `initialized = true` 顺序正确但无回滚；第 51–52 行无 `type()` 检查且 **`OpenAccountPage` 用 `Toggle` 实现（`/ycu` 连按两次会关闭页面）**。
270. **本文件只"读" `"economy"`/`"economy-items"` 两个数据域，全插件未见任何生产者代码** —— 域数据来源依赖 Core 或其它插件，属未闭合契约。

### `YiboCurrency/Commands.lua`（31 行）
271. 第 2 行 `/ycu` 三字母合规；第 3 行 `SlashCmdList["YCU"]` 键名与插件名不一致（项目内已出现 4 种命名风格）；第 4 行 `strtrim` 不含 U+3000。
272. 第 5 行未校验 `Addon.OpenAccountPage` 是否存在；**第 6–27 行 `probe` 分支是独立 `if` 而非 `elseif`**（与 `YiboReputation/Commands.lua:5` 同一结构缺陷，此处靠第 26 行 `return` 侥幸不出错）。
273. 第 9 行域名字符串 `"economy"` 内联；第 13–14 行输出 `snapshot.state` 的英文枚举给玩家；**第 15–25 行每个条目一次 `C_CurrencyInfo.GetCurrencyInfo`（无缓存）**；第 17 行只判真值未检查 `type()`。
274. **第 19 行旧 API 回退路径 `GetCurrencyInfo` 在 8.0+ 已移除**，与 `C_CurrencyInfo` 并存但 `.toc` 无 flavor 声明；第 20/23 行输出 `entry.status` 作兜底；第 28–29 行 `add` 子命令**只打印"将在下一轮开放"的占位分支**（`/ycu add 12345` 静默不生效）。

---

## 九、YiboBuilds（6 个文件）

### `YiboBuilds.toc`（17 行）
275. 第 8–9 行 `RequiredDeps`/`Group: YiboCore` 合规；第 5 行版本与 `Bootstrap.lua:6` 重复；第 6 行图标与 `Bootstrap.lua:9` 重复（两处拷贝）；第 1 行 `50504`（第 3 个）。
276. **无 `## X-GameFlavor`，而 `Snapshot.lua` 同时使用 `C_Spell`/`C_TooltipInfo`/`C_GlyphInfo`（Retail）与 `GetGlyphSocketInfo`/`GetNumGlyphs`/`SetInventoryItem`（旧版）三代 API**；`LoadOnDemand: 0` 无意义；`SavedVariables` 未标 schemaVersion；`Notes` 中文无 `Notes-zhCN`。
277. **加载清单共 5 个 Lua，没有 `Settings.lua`** —— 需核对 `CoreIntegration.lua:37` 是否提供业务设置分区（实测未提供，见 292）。

### `YiboBuilds/Bootstrap.lua`（111 行）
278. 第 1 行 `ADDON_NAME = ...` 在 `dofile` 下为 nil；第 5 行 `Addon.NAME = ADDON_NAME or "YiboBuilds"` 有兜底，但**第 79 行仍用原始 local，兜底在事件分支失效**。
279. 第 6/7/8/9 行四个常量集中定义（正面），但 `REQUIRED_CORE_API = 5` 与 `CoreIntegration.lua:8` 的硬编码 `5` 并存；第 11–20 行 `Defaults` 是第 8 份默认值合并实现。
280. **第 22–24 行 `Addon:Now() = (GetServerTime and GetServerTime()) or time()` 混用服务器与本地纪元，且 `GetServerTime()` 返回 `0` 时（`0` 为真值）兜底失效，会把 0 当有效时间戳存储。**
281. 第 27 行直接写全局；**第 29 行 `schemaVersion` 与第 31 行 `Defaults` 里的同名字段重复定义，且全文件无任何迁移比较（版本号只写不读）**；`previewColumns` 15 个布尔键是**项目内首个真正实现预览列的插件（正面）**，但 `appearanceMode = "transmog"` 为自由字符串无白名单。
282. 第 44 行 `self.db = db` 形成第二份 DB 访问路径；第 48–50 行 `GetSettings` 触发 `EnsureDB`（第 9 处读路径写副作用）；第 54 行打印前缀颜色正确但插件名 `构筑` 与 `.toc`/`CoreIntegration` 三处不一致；**第 58–62 行 `NotifyChanged` 读 `self.Core`，其赋值点在 `CoreIntegration.lua:11`**。
283. **第 64–76 行连续 12 个 `RegisterEvent` 无 `pcall`**；**第 70–73 行注册 `GLYPH_UPDATED`/`GLYPH_ADDED`/`GLYPH_REMOVED`/`USE_GLYPH` —— MoP 已改为天赋系统，这 4 个事件是死事件**，但第 100/103 行仍为其安排处理与 0.35s 延迟抓取。
284. 第 87 行与第 89 行用同一 `"login"` reason 重复调度（0.3 / 0.5 两个魔数延迟）；**第 90–97 行 `C_Timer.After(2, ...)` 无取消令牌**（与 `Snapshot.lua:349-353` 的 `captureToken` 模式不一致），2 秒内登出/切角色会作用于错误状态。
285. **第 104–107 行 `MarkEquipmentDirty()` 设置的 `equipmentDirty` 全项目无读取点（死状态）**；第 108–109 行 `PLAYER_LOGOUT` 同步执行完整抓取（19 槽 + 附魔扫描），登出阶段时间受限存在 SavedVariables 被截断的风险。

### `YiboBuilds/Snapshot.lua`（383 行）
286. 第 5–11 行 `SLOT_IDS` 用 `INVSLOT_XXX or N` 兜底（常量缺失时静默换用错误槽位号），且顺序与游戏装备栏不符；第 13 行 `SlotKey = tostring` 使装备槽与 `"primary"/"secondary"` 处于同名 `slots` 概念下的两套键空间。
287. **第 22–24 行 `SlotForGroup` 全项目无调用者（死函数）**；第 30–32 行 `ItemIDFromLink` 是 `item:(%d+)` 模式的第 6 份拷贝；第 14–20 行 `ActiveGroup` 向不接受参数的 API 传 `false`。
288. 第 34–45 行 `SpellInfoSafe` 在同一函数内混用"返回表"与"多返回值"两种约定；**第 47–90 行 `GlyphSpecializationRequirement` 第 49/63/74/84 行用中文 `"通用"` 同时作返回值与逻辑判据**。
289. **第 65 行与第 179 行创建两个具名全局 `GameTooltip` 帧**（`YiboBuildsGlyphRequirementTooltip`/`YiboBuildsEnchantScanTooltip`，重名风险 + 双 tooltip 帧）；**第 77/185 行依赖 `GameTooltipTemplate` 内部字体串命名**（Retail 已改）；第 80–84 行用 `string.find` 在 tooltip 文本中模糊匹配专精名来推断雕文专精需求。
290. 第 95/97 行硬编码 `tier 1..6` × `column 1..3`（MoP 正确、Retail 为 7 层，无 flavor 分支）；**第 100 行 `GetTalentInfo` 传 `target = "player"`（应为 `inspectUnit`）**；**第 107 行只要任一层有名称即置 `ready` → 第 299 行会接受残缺天赋数据**；第 109/113 行中文哨兵；**第 115 行把布尔 `ready` 混入数组表（`pairs` 遍历者会把它当一层天赋）**。
291. 第 121/122/126 行 `GLYPH_TYPE_MAJOR or 1` 与硬编码 6 槽类型表；**第 132 行在 API 未返回类型时用硬编码表"补造" `glyphType`，第 158 行据此分入 major/minor（可能分类错误）**；第 135–143 行 `C_GlyphInfo.GetGlyphInfoByID` 解构 7 值；第 149 行从链接正则提取名称（对含颜色码/非拉丁名不可靠）；**第 150 行 `present` 可能是字符串（链接）而非布尔**；第 160 行 `ready = readySlots == 6` 使客户端不返回类型时 `ready` 恒 false（数据永不更新）。
292. 第 165–168 行 `C_TooltipInfo.GetInventoryItem` + **魔数枚举 `or 15`**；**第 172/188 行用中文正则 `^附魔：` 解析附魔名（非 zhCN 客户端解析失败）**；**第 183 行 `SetInventoryItem` 在 Retail 已移除**；第 178–180 行第二个具名 tooltip 帧；第 196–212 行 `ReadSpecialization` 中途替换 `getInfo`（两套 API 语义混用）与中文 `"副天赋"/"主天赋"`。
293. **第 218 行 `GetNumGlyphs()` 是 WotLK 雕文书 API，MoP 已移除 → `count = 0`，整个雕文目录为空**（第 304 行仍写入 `record.glyphCatalog`），进而使 `AccountPage.lua:511-516` 的"所有雕文"视图在目标客户端恒走兜底；第 221 行 `name ~= "header"` 依赖 API 内部分隔符；第 223–226 行第二次调用 `C_GlyphInfo.GetGlyphInfoByID`；第 230 行对目录中每个雕文调用专精需求解析；第 231 行自由字符串 `state`。
294. 第 242 行每次调用新建表；第 251 行 `Addon:Now()` 混用时钟；**第 262 行 `local _, gemLink = GetItemGem(link, gemIndex)` 取第二个返回值作宝石链接（首个返回值才是链接）；第 263 行 `GetItemIcon(gemLink)` 把链接当 itemID 传入 → 宝石图标恒为 nil**。
295. 第 266 行从链接第二字段取附魔 ID（格式正确但脆弱）；**第 267 行对每件带附魔的装备触发一次 tooltip 扫描（每次抓取最多约 10 次）**；第 285/289 行 `secondary = nil` 冗余写法；第 288 行每次 `EnsureCharacter` 覆写 `identity`；`identity.classFile` 字段名与内容不符。
296. **第 296–303 行四处"新数据看起来不完整就保留旧数据"的启发式，使快照成为新旧字段混合体且各字段无独立时间戳**；第 304 行 `(record.lastActiveSlot or slot)` 在 nil 时恒真；第 309–317 行六条件跨三行的 `and` 链。
297. **第 329 行 `CaptureSlot(..., true)` 无条件抓取装备 —— 由 `Bootstrap.lua:103` 的雕文/天赋事件触发的 `"glyph-catalog-update"` 也会跑完 19 槽 + 附魔扫描**；第 343 行 `equipmentDirty = false` 与第 361–363 行构成只写不读的死状态；第 354 行 `else Run()` 在事件回调线程内同步执行完整抓取；**第 372 行 `data.confirmedEquipment = data.observedEquipment` 按引用赋值（无拷贝）**；**全插件未注册 `Core.CharacterCleanup`，删除角色的记录永久残留**。

### `YiboBuilds/AccountPage.lua`（921 行）
298. 第 1–7 行 `Core`/`Theme`/`C` 模块级捕获无守卫；第 9/10 行两个 56 常量分开定义；第 11–18 行 `SLOT_LABELS` 是槽位表第二份拷贝；**第 24–30 行 `LEFT/RIGHT/BOTTOM_SLOTS`/`SLOT_ORDER` 是第三、四、五份拷贝，且第 20–22 行注释称 pre-MoP ranged 槽"不得渲染"而 `Snapshot.lua:10` 仍包含 `INVSLOT_RANGED`（两文件约定矛盾）**。
299. 第 36–39 行 `IsCurrent` 每次调 `Core.Characters:GetCurrent()`，本次刷新内被调 ≥8 次；第 41–47 行 `Eligible` 每角色触发 `EnsureDB`，与 `CoreIntegration` 的准入规则是两套；第 49–51 行 `IconText` 是第 3 份拷贝；第 53–56 行 `ClassColor` 表达式求值两次且返回键式颜色（与 `YiboCurrency` 的数组式不一致）。
300. 第 58–68 行中文兜底与 `GetDisplayName(character, "short")` 私有约定；**第 70–77 行 `ItemBorderColor` 假定 6 位 RGB 色码，遇 8 位 ARGB 即解析失败并静默退回默认色**。
301. **第 87/94 行宝石与附魔按钮以 `parent.buildsEquipment` 为父（不是 `button`），不随按钮移动/隐藏，因此第 409–411 行需手工逐级 `SetFrameLevel` 补偿；第 100 行 `RegisterForClicks` 但从未绑定 OnClick（点击无行为）**；第 104–139 行是三个近乎相同的 tooltip 绑定助手；**第 143 行 `requirement ~= "通用"` 是中文哨兵作判据的第 4 处**。
302. **第 146–156 行 `GetFields`、`Bootstrap.lua:34-39` 的 `previewColumns`、第 178–183 行 `PreviewColumns` 三处各自硬编码同一批 15 个字段 id**；第 175 行魔数 16；**第 191 行 `while #eligible > 20 do table.remove(eligible) end` 从尾部截断而非按优先级**。
303. 第 203–212 行"先置 nil 再条件重绑"的隐式手法；**第 216 行 `date("%Y-%m-%d %H:%M", equipment.capturedAt)` 用本地时区解释服务端时间戳（时钟混用第 2 处）**。
304. **第 222–336 行 `Page.Create` 是单个 115 行函数，向共享 `parent` 写入约 20 个字段**；第 233/318 行 `HookScript` 重复调用会累积；**第 247 行 `SetParent` 重新指定父级** + 第 248 行 `+10` 魔数；第 261–290 行手工实现拖拽旋转/滚轮缩放（魔数 `0.65`/`1.55`/`0.08`，第 289 行 `SetModelScale(1/zoom)` 与 `SetCamDistanceScale` 语义相反）。
305. **第 297–298 行 `appearance` 按钮以 `buildsEquipment` 为父却挂在 `buildsToolbar.appearance` 字段（跨父级赋值）**；第 309/310/322/323 行临时占位尺寸；第 341 行以宽度 1 建行；第 357–363 行用 `C.matrixLine[4]`/`C.line[4]`（数组含 alpha）而 `ClassColor` 返回键式（第 378 行 `color.r or color[1]` 正是在兼容两套表示法）。
306. 第 381–382 行魔数与失效兜底；第 394–403 行魔数 `-16`/`0.015`/`8 +`；第 421 行四个魔数；**第 420 行 `box.layoutHeight or box:GetHeight() or 500` 在 `GetHeight()` 返回 0 时失效，公式退化为负值靠 `math.max(39, ...)` 兜底**。
307. **第 429–435 行对每槽做三层线性搜索确定 side/visualIndex（O(n²)）**；第 443 行魔数 `88`/`128`/`0.28`；**第 462/469/472 行在同一刷新内为每个宝石/附魔/物品重绑 tooltip 脚本（19 槽 × 5 ≈ 95 次 `SetScript`）**；第 470–471 行每槽重算边框色。
308. 第 487–492 行 `appearanceMode == "prototype"` 时对 19 槽逐个 `TryOn`（无脏检查）；第 506 行 `GLYPH_TYPE_MAJOR or 1`（第 2 份）；**第 513 行 `"当前"/"已学"/"未学"` 中文状态串在第 556/558/559/560/569 行作逻辑判据**；第 511–516 行遍历的 `record.glyphCatalog` 在 MoP 上恒为空（见 293）→ **"所有雕文"视图永远走第 517–521 行兜底**。
309. 第 523–546 行魔数 `-42`/28/`-76`/`-8`；第 527–537 行 `buildsTooltipBound` 一次性绑定（正面）；第 538–545 行每次刷新重绑两个 OnClick；第 555/595/603 行三组图标尺寸（46/50、56/60、40/44）；第 558–559 行用 6 个三元选择文本与边框色。
310. 第 586 行 `math.floor(height / 6)`；第 590/593 行中文默认；**第 592 行四段 `and/or` 链与第 600 行 `isMajor = index <= 3` 是同一分组规则的两份表达**；第 604 行单行 8 语句。
311. **第 613–668 行 `SetupMainButtons` 被 `Page.Refresh` 末尾调用 → 每次刷新重绑全部 7 个按钮的 OnClick + OnEnter/OnLeave**；**第 627 行 `Core.AccountView:RefreshPage()` 不传 `PAGE_ID`**（与其它插件的调用方式不一致）；第 657–662 行在点击回调内懒创建 `StaticPopupDialogs`，第 660 行 `OnAccept` 忽略返回值与错误；第 615/620/638/643/648 行混用 `Addon.AccountPage.Refresh` 与 `Page.Refresh`。
312. 第 670–720 行 `LayoutMain`：第 672–674 行主动隐藏预览控件而 `RefreshPreview`（802 行）隐藏 roster/main（**两种模式争抢同一批 `parent` 字段**）；**第 675–678 行正确处理了 `GetWidth() == 0`（全项目少见的正面写法）**；第 690–691 行 `mainHeight` 算完从未使用；第 692 行写 `Addon.buildsBuildCollapsed` 与第 908 行读取构成跨文件全局状态（度量函数带副作用依赖）；**第 712/714 行在锚定后立即 `GetHeight()` 取布局高度（此时 Core 尚未设置尺寸，极可能为 0，而 0 为真值使下游兜底失效）**。
313. **第 838 行 `slotData and (slot == "secondary" and "副天赋" or "主天赋")` 引用了本函数内不存在的局部变量 `slot`（只在 `Page.Refresh` 第 756 行定义）→ 恒读 nil 全局，预览模式槽位列永远显示"主天赋"**；第 843/846/849 行用 `string.find(col.id, "talent")` 等子串匹配决定取值分支；第 856 行复合条件重复推导 837–855 的分支集合；第 866–870 行**为每个单元格**、第 872–877 行为每行重绑 OnClick。
314. 第 884 行 `EXPANDED_CONTENT_WIDTH = 1120`、第 888 行 `rosterWidth = 210`、第 890 行 `0.55` 与 `LayoutMain` 的 682/699/700 行各自独立硬编码（无单一来源）；第 904/910/919 行三处 overflow 取值与 `YiboCurrency` 策略又不相同；**约 10 个视图状态字段全部存在共享页面帧上（主视图与悬停互相污染）**。

### `YiboBuilds/CoreIntegration.lua`（60 行）
315. 第 6 行无"已尝试"标志；第 27 行成功后第 48 行失败仍会重复注册且无回滚；**第 8 行把 `CheckAPIVersion(5)` 嵌在存在性判断内，导致"Core 缺失"与"版本过低"返回同一文案**，且未检查 `DataDomains`/`Events`/`UITheme`。
316. 第 11 行 `Addon.Core = Core` 是 `Bootstrap.lua:59`/`Commands.lua:9` 的唯一赋值点，却位于 `RegisterAddon` 之前（部分初始化状态）；第 12 行用 `Addon.REQUIRED_CORE_API`（正面）但第 8 行仍硬编码 `5`。
317. 第 14–26 行注册 `Core.CharacterCleanup`（**本项目唯一实现角色清理登记的插件，可作范例**）；第 28 行 **`order = -15`（项目内第 5 个不同排序值且为负）**；第 29 行 `compactWidth = true` 为他插件未用参数。
318. 第 31 行每角色触发 `EnsureDB`；**第 32–36 行在同一文件内把准入规则写了第二遍**（第三遍在 `AccountPage.lua:41-47`）。
319. **第 37 行 `settings` 无 `CreateSettingsPanel` → `Bootstrap.lua:33-42` 定义的业务设置没有设置页分区**（`appearanceMode` 只能通过账号页工具栏切换），不符合 AGENTS.md；第 38 行 `fields` 是静态表而第 39 行 `GetPreviewFields` 是动态回调（两种字段提供方式并存）；**第 39 行返回内部表引用**；第 40 行每次切换都 `NotifyChanged()`（无节流）。
320. 第 41–44 行按引用传函数（**优于 `YiboCurrency` 的包装闭包**）但依赖这些函数不使用 `self`；第 45 行 `GetSummary` 统计的是传入角色数而非有快照的角色数；**第 50 行 `text = "[Yibo] 角色构筑"` 缺 `|cff20e070[Yibo]|r` 前缀**。
321. 第 53–54 行无失败回滚；**第 57–59 行 `Addon:OpenAccountPage` 在本插件内无调用者（死函数）**；未注册 `DATA_DOMAIN_UPDATED` 监听（与 `YiboCurrency` 不一致），页面刷新完全依赖 `Snapshot:Capture` 内部调用。

### `YiboBuilds/Commands.lua`（15 行）
322. 第 1 行 `/ybb` 三字母合规；第 2 行 `SlashCmdList.YIBOBUILDS` 是第 4 种命名风格；第 4 行 `%s` 不含 U+3000，只取首个 token，且**会 `lower` 命令名（其它插件不会，行为不一致）**。
323. **第 6 行 `Addon.Snapshot:Capture("diagnostic")` 在斜杠命令内同步执行完整抓取**（19 槽 + 附魔扫描），`Addon.Snapshot` 无守卫；第 9 行 nil 链式守卫正确（正面）。
324. **无任何诊断/probe 子命令与缓存重置入口**（AGENTS.md 要求诊断通过 Slash 命令触发）；`scan` 与 `YiboCurrency` 的 `probe` 职责重叠但命名不同；handler **无 `pcall`**；第 13 行用法文案与 `Bootstrap.lua:54` 的 `构筑`、页面标题 `角色构筑` 三处命名不一致。

---

## 十、YiboMounts（17 个文件）

### `YiboMounts.toc`（23 行）
325. 第 1 行 `50504`（第 4 个）；**第 3–4 行 `## Notes` 与 `## Notes-zhCN` 成对提供（全项目唯一，正面）**；第 5 行 Author。
326. **第 6 行 `## Version: 0.6` 与 `Namespace.lua:4` 的 `"0.5"` 冲突**；**第 7 行 `## Group: Yibo` 是自创分组名（其余插件均为 `YiboCore`）**；无 `## RequiredDeps`（本插件确实不依赖 Core，但分组名不规范）。
327. 第 8 行 `## SavedVariables: YiboMountsDiagnostics` —— 只持久化诊断数据，**全插件无用户设置**（零可配置项）；第 9 行图标带 `-v1` 无扩展名；第 14 行 37,572 行的生成数据无条件加载；加载顺序约束仅隐含。

### `YiboMounts/Namespace.lua`（28 行）
328. 第 1 行标准 vararg 模式（正面）；第 4 行版本与 `.toc` 冲突；**第 5 行 `NS.INTERFACE = 50504` 是接口号第二处硬编码**。
329. **第 8–11 行 `GetLocaleTable` 三级兜底（当前 locale → enUS → `{}`）是全项目唯一的本地化基础设施（正面）**；**第 14–15 行 `L(key)` 在已回退到 enUS 后又查一次 `self.Locale.enUS[key]`（重复查找）**；第 15 行以 `key` 自身作最终兜底（会让缺失翻译静默显示键名）。
330. 第 18–22 行 `GetLocalizedText` 支持按 locale 键的数据表（正面），但第 20 行再次调用 `GetLocale()`（未复用第 9 行的结果），且第 21 行未对 `values.enUS` 缺失做键名兜底（与 `L` 策略不一致）。
331. **第 24–28 行 `SafeCall` 在 `pcall` 失败时直接 `return nil`，不返回错误、不记录日志 → 调用方无法区分"正常 nil"与"出错"；且该函数全项目零调用者（死代码）。**

### `YiboMounts/Bootstrap.lua`（10 行）
332. 第 3 行匿名帧（**无全局名，不存在 UI 重名风险，正面**）；第 6 行提前返回写法简洁（正面）；**第 4 行只注册 `ADDON_LOADED`**（无 `PLAYER_LOGIN`/`PLAYER_LOGOUT`，诊断数据无落盘时机）。
333. **第 7 行 `if not NS.Data or NS.Data.schemaVersion ~= 1 then return end` —— 静默硬门禁：数据版本不匹配时插件完全无提示、无日志地不工作**；**第 9 行 `NS.Tooltip:Initialize()` 无守卫而第 8 行对 `NS.Probe` 有守卫（同文件两种策略）**；第 8 行诊断模块每次登录无条件初始化；处理完**未 `UnregisterEvent`**；第 8/9 行两个 `Initialize()` 无 `pcall`。

### `YiboMounts/Locale/enUS.lua` / `zhCN.lua`（各 33 行）
334. 两表各 30 键、键集合完全对齐（正面），但**无任何键集合一致性校验测试**；**`PRICE`、`ALLIANCE`、`HORDE` 三键（两语言共 6 条）全项目无引用（死本地化键）**。
335. **第 20 行 `SOURCE_LABEL_SEPARATOR` 在 enUS 是 `": "`、zhCN 是 `"｜"`（U+FF5C 全角竖线）；第 21 行 `PATH_SEPARATOR` 是 `" > "` vs `" › "`（U+203A）；第 22 行 `CONDITION_SEPARATOR` 两语言逐字相同** —— 渲染代码需对三种分隔符做宽度/断行兼容。
336. 第 18 行 `SOURCE_RESEARCH` 英文值 `"Source clue"` 与中文 `"来源线索"` 语义不对称；第 26/27 行只有 10/25 两个难度键；第 28 行只有 `STANDING_EXALTED` 一个声望键；**全部为纯字符串无格式模板 → 语序无法本地化**；zhCN 第 30 行 `"当前已无法获取"` 长度是 enUS 的 2.6 倍，**tooltip 宽度按 enUS 设计时中文会溢出**。

### `YiboMounts/Data/MountCatalog.generated.lua`（37,572 行）
337. 第 1–2 行标注来源与禁止手改（正面）；**第 5 行 `catalogVersion = "0.10"`（字符串）与 `schemaVersion = 1`（数字）类型不一致，且与 `.toc` 0.6、`NS.VERSION` 0.5 构成三个不同源版本号**。
338. **第 37571 行 `schemaVersion = 1` 是 `Bootstrap.lua:7` 的硬门禁 —— 重新生成时改为 2 会让插件静默完全失效**；第 7 行起的 `mountKeyBySpellID` 值含 `"provisional-…"`/`"fact-…"`/`"manual-source-…"` 三种前缀，**`provisional-` 占位记录直接进入生产目录（无过滤）**。
339. 尾部记录可见 `status = "candidate"`（自由字符串枚举，`SourceFormatter.lua:58` 依赖 `"rejected"`）与 **`enUS = nil, zhCN = nil` 的空本地化字段 → `SourceFormatter.lua:88/93` 静默丢弃整条来源，无诊断**；`price` 同样为空。
340. 整个文件是单个巨型表字面量，**每次登录解析 37,572 行**（无 `LoadOnDemand`）；无按名称/物品 ID 索引（只能线性扫描）；无记录总数与重复 spellID 校验；**生成侧与消费代码之间无字段契约校验**（`path[].kind` 的 `"instance"/"zone"/"custom"/"faction"` 只出现在消费代码字面量中）。

### `YiboMounts/Data/SourceFacts.generated.lua`（3 行）
341. **第 1 行自述 `GENERATED NON-RELEASE PROBE DATA`，却出现在生产 `.toc:19` 的加载清单中（发布包违规）**；第 3 行是单行超长表（以百 KB 计）。
342. **键为字符串 spellID（`["100332"]`），而 `Catalog:GetBySpellID` 要求 number → 字符串/数字键不一致的潜在陷阱**；数据仅含 6 类 ID 而 `SourceFactProbe.lua` 还会处理 `professionIDs`/`factionIDs`（生成侧与消费侧不对称）；**本文件无 `schemaVersion`**；未标注生成源文件，无法追溯。

### `YiboMounts/Catalog.lua`（60 行）
343. **第 3–5 行在加载期捕获 `NS.Data`/`mounts`/`mountKeyBySpellID` —— 加载顺序一旦变化，`data = {}` 且三张表永久为空，配合 `Bootstrap.lua:7` 的门禁使插件完全不工作且无提示**；第 4/5 行 `or {}` 静默降级；第 7 行裸表赋值。
344. 第 10/16/24/32 行均有 `type()` 检查（**正面，优于 `YiboAutoOpen/BagAdapter.lua`**）；第 19–21 行 `IterateMounts` 直接暴露生成文件的 `pairs` 顺序（tooltip 输出顺序不可控）；第 23–29 行 `GetPrimarySource` 每次线性扫描。
345. 第 36 行 `source.active ~= false`（nil 视为启用）与项目内 `== true` 约定相反；第 37 行把**原始记录表引用**放进新列表；**第 44–57 行 6 分支比较函数依赖 `tonumber(priority)` 与 `tostring(sourceID)` 字节序，`priority` 类型混用时可能违反严格弱序并抛 "invalid order function"**；每次调用重新过滤+排序（无缓存）；第 45–47 行拍卖行降级有注释（正面）。

### `YiboMounts/SourceFormatter.lua`（165 行）
346. 第 5–21 行 `TYPE_KEYS` 用 locale 键间接层（**全项目唯一正确做法，正面**）；但第 23–28 行 `DIFFICULTY_KEYS` 用大写键、第 30–33 行 `AVAILABILITY_KEYS` 用小写键（**同一文件三种键名约定**）；第 35–43 行 `Join` 静默丢弃非字符串。
347. **第 45–55 行 `FormatMoney` 硬编码 `"g"/"s"/"c"` 后缀**（未本地化、未用标准金币图标/颜色）；**第 58 行 `record.status` 未做 nil 守卫**；`source.type == "research"` 静默返回 nil。
348. **第 61 行未知类型时把内部蛇形串（`"boss_drop"`）显示给玩家；第 100 行未知难度显示 `"TEN"`；第 111 行 `NS:L("STANDING_" .. ...)` 缺失键时显示 `"STANDING_XXX"`。**
349. 第 63–70 行与第 71–90 行两次遍历同一路径；**第 74–86 行三条 `omit*` 规则共 11 个条件全部硬编码 `source.type` 字面量**（新增类型时静默不生效）；第 84 行 `node.kind == "custom"` 未文档化。
350. 第 92/144 行用本地化分隔符（正面）而**第 102/129 行用硬编码 `" / "`（不一致）**；**第 124/126 行把内部格式串（`"currency:392"`/`"item:70909"`）直接渲染进玩家可见 tooltip**；第 132–137 行"notes 不得泄漏"注释（正面）。
351. 第 152–164 行 `FormatAll` 用 `"\031"` 拼接签名去重（每来源一个完整字符串），且底层 `GetTooltipSources` 会过滤+排序 → **每次 tooltip 渲染 = 过滤 + 排序 + N 次 `FormatSource`，全链路无缓存、无 `pcall`**。

### `YiboMounts/Probe.lua`（79 行）
352. 第 7 行打印前缀颜色合规但**插件名用英文 `Mounts`**（`.toc` 为 `坐骑图鉴`，其它插件用中文）；第 11 行把本地 `Print` 公开为公共方法；第 13–16 行 `Report` 每次 5 次 `tostring` 拼接（热路径）。
353. **第 22 行 `SLASH_YIBOMOUNTS1 = "/ymt"` 注册在 `Probe:Initialize()` 内且本插件没有 `Commands.lua`** —— 与 AGENTS.md"新增命令前必须全工作区搜索确认未被占用""命令注册集中在插件命令入口"不符；第 23 行点号写法（第 5 种风格）；第 24 行 `%s` 不含 U+3000。
354. **第 25–27 行 `debug` 翻转全局开关，无超时、无时间戳、无可见状态**；第 26 行依赖闭包捕获的 `self`。
355. 第 29–31 行读取当前 `GameTooltip` 状态并**假定 `GetSpell()` 第 3 个返回值是 spellID**（版本相关，无 `type()` 校验）；第 43–46 行检查 `FindMountAuras` 存在（正面）但**第 47 行调用无 `pcall`**，且依赖"返回 (列表, 计数)"的未文档化契约。
356. **第 53–58 行 `inventory` 子命令是空壳（只打印"received"）而第 75 行帮助文本承诺它会保存库存**；`PrintCapture` 只在 `sources` 分支（第 68 行）被调用。
357. **第 59–71 行 `sources` 分支中第 64 行 `SourceFactProbe:Capture()` 无 pcall 而第 68 行有 pcall（保护不对称），且该命令顺带执行 inventory 抓取（职责混合）**；第 72–77 行帮助文本为英文（与其它插件中文风格不一致、未走 `NS:L`）；本文件未写入 `.toc:8` 声明的 SavedVariables，且无关闭/重置入口。

### `YiboMounts/InventoryProbe.lua`（82 行）
358. 第 5–7 行未检查 `GetMountInfoExtraByID`（第 21 行单独守卫）；第 12 行返回错误字符串（正面）；**第 18–19 行解构 `GetMountInfoByID` 的 12 个返回值**（Retail 已改为返回单表）。
359. 第 20–26 行 `GetMountInfoExtraByID` 用 `ignored` 占位（注释说明用途，正面），但第 43–44 行把本地化 `description`/`sourceText` 存入记录，**第 68 行随 `mountInventory` 整体写入 SavedVariables**；第 27 行按 spellID 去重并保证数值类型（正面）。
360. 第 48 行排序比较函数安全（**与 `Catalog.lua:44-57` 形成正反对照**）；第 52 行 `NS.Catalog` 无守卫；第 54 行 nil 视为可见；**第 60 行直接写全局**；**第 61–69 行每次抓取整体覆盖 `mountInventory`（无历史）**；第 62 行用 `!` 前缀取 UTC（正面）。
361. **第 63 行 `select(4, GetBuildInfo())` 被命名为 `build` —— 第 4 个返回值是 TOC/接口版本号，真正的 build 是第 2 个**；第 70 行返回内部表引用；第 79/80 行英文输出与硬编码 `"/"` 拼接；第 17/19 行两处 API 调用无 `pcall`。

### `YiboMounts/SourceFactProbe.lua`（117 行）
362. **第 6 行 `CreateFrame("GameTooltip", "YiboMountsNameProbe", ...)` 位于函数体内且用固定全局名 —— 每次调用新建具名帧，同名会被客户端自动改名导致第 9 行 `_G[GetName() .. "TextLeft1"]` 取错控件，同时每次调用泄漏一个 Frame（`AppendResolved` 对每个未解析 ID 都会调用）→ 本插件最严重的内存泄漏 + UI 重名问题。**
363. **第 32 行 `QuestName` 回退路径同样在函数体内创建固定具名帧 `"YiboMountsQuestProbe"`（同一缺陷）**；第 8 行伪造单位 GUID、第 34 行伪造 `"quest:"` 链接解析名称（依赖未文档化行为）；第 9/35 行依赖 `GameTooltipTemplate` 内部字体串命名。
364. **第 20–25 行 `AchievementName` 只解构 2 个返回值再用 `type(...) == "string"` 猜测哪个是名称（启发式，靠返回顺序碰巧正确）**；第 22 行无 `pcall`；第 41–55 行 `InstanceName`/`EncounterName` 无 `type()` 校验。
365. **第 57–67 行 `ProfessionName` 内置硬编码中文 ID→名称表 —— 本插件唯一绕过 `NS:L` 的用户可见文案，非 zhCN 客户端会看到中文专业名；且该表定义在函数体内，每次调用重新分配**；第 58–59 行注释记录了"不能用 `GetSpellInfo`"的真实陷阱（正面）；第 69–75 行 `FactionName` 无 flavor 分支。
366. **第 79 行 `if result[field][id] == nil then ... resolver(id) end` 无负缓存 → 解析失败的 ID 下次抓取会再次创建 tooltip 帧重试（放大第 362/363 行的泄漏）**；第 79 行 `resolver(id)` 无 `pcall`。
367. 第 82–117 行 `Capture` 是八层嵌套循环（O(事实 × 提示 × ID)）；第 84 行 `NS.SourceFacts or {}` 静默降级；第 112 行结果混合 8 类名称无 schema；**第 114–116 行把完整结果写入全局 `YiboMountsDiagnostics`（无时间戳、无大小限制）**；抓取过程**无任何进度输出**，创建的 tooltip 帧既不注销也不复用。

### `YiboMounts/Tooltip.lua`（129 行）
368. **第 5–7 行 `IsSupportedUnit` 只接受 `"player"`/`"target"`，而第 89–97 行 `ApplyHoveredUnit` 对任意 `UnitExists` 单位为真都生效 → 同一文件两套矛盾的单位策略。**
369. **第 9–11 行 `ClearMarker` 把签名写在共享 `GameTooltip` 的自定义字段上**（与 `YiboReputation/AccountMatrixView.lua` 同一手法，两插件互不可见且无清理约定）。
370. **第 14 行 `GetBySpellID` 在第 17 行类型校验之前执行；第 15 行每次调用都执行 `NS.Probe:Report`（热路径）**；第 20 行每次分配签名字符串；**第 28 行 `FormatAll` 每次调用执行"过滤 + 排序 + N 次格式化"（跨 tooltip 无缓存）**。
371. **第 34–35 行硬编码 RGB `0.12, 0.88, 0.44` 与 `0.82, 0.82, 0.82`**（绕开主题，且绿色与品牌色 `|cff20e070` 不一致）；**第 38 行在 hook 回调内主动 `tooltip:Show()`（可能重入 hook / 与游戏 tooltip 生命周期冲突）**。
372. **第 50–71 行 `GetAuraSpellID` 是启发式扫描器**：第 52 行每次调用分配表；**第 53 行硬编码 `values[11]` 为 spellID（第 55–56 行注释已承认该假设不成立）**；第 57–60 行按光环名称反查 spellID（依赖本地化名称、`GetSpellInfo` 在 Retail 已由 `C_Spell` 取代）；**第 62–69 行遍历 1..20 个返回值，任何命中目录的数值都被当作 spellID → 层数/剩余时间等数值可能巧合命中产生误报**；第 70 行"是否存在"由第 1 个返回值非 nil 推断；第 52 行 `api(...)` 无 `pcall`。
373. 第 79 行硬编码 40 个光环槽位；第 80 行 `UnitBuff` 未传 filter；第 83–84 行每个命中分配表；**第 94–96 行对每个命中单独 `AppendSource` → 同一坐骑由多个光环命中时会产生重复来源行**。
374. **第 100/104 行 `NS.Tooltip:Apply(tooltip, unit, GetAuraSpellID(...), "SetUnitAura")` —— `GetAuraSpellID` 返回 `(spellID, present)`，第二个返回值（布尔）被当作 `Apply` 的 `source` 参数 → 第 15 行 `Probe:Report` 打印的 source 恒为 `true`/`false`（实参错位缺陷）。**
375. 第 108 行幂等 + `GameTooltip` 守卫（正面）；第 120–125 行 `hooksecurefunc` 前后有 `type()` 检查（正面）但**无 unhook 路径**；**第 126–127 行只对 `GameTooltip` 挂 `OnTooltipCleared`/`OnHide`，而第 121/124 行的 hook 可能作用于其它 tooltip 实例 → 那些实例的签名表不会被清理（累积）**；未覆盖 `ItemRefTooltip`/`ShoppingTooltip1/2` 等（边界未文档化）。
376. **本插件无任何用户设置**（无 `/ymt off`、无过滤开关、无行数控制），玩家无法关闭或调整 tooltip 追加内容；且 hook 回调内全部无错误隔离（`FormatAll` 抛错会连带游戏 tooltip 链路报错）。

### `YiboMounts` 测试（4 个文件，281 行）
377. `RunLuaSmokeTests.lua:3-5` 定义全局 `GetLocale` 且不还原；`:10-11` 用 `loadfile` + `chunk("YiboMounts", namespace)` **绕过真实加载路径**；**`:14-25` 的加载顺序是 `.toc` 的手工副本且无一致性断言**。
378. **`RunLuaSmokeTests.lua` 未加载 `Bootstrap.lua`、`SourceFactProbe.lua`、`Data/SourceFacts.generated.lua`** → `schemaVersion` 门禁、hook 装配、`SourceFactProbe:Capture`（含帧泄漏）**零覆盖**；第 27 行无退出码、无用例计数；`Load` 无 `pcall`；测试直接改写生产表（覆盖 `NS.Probe.Print`、设全局 `UnitBuff`/`UnitExists`）且从不还原。
379. `InventoryProbeSmoke.lua` 只断言失败路径与生产代码的**英文错误字面量**；成功路径（12 值解构、去重、排序、写 SavedVariables）与 `YiboMountsDiagnostics` 内容零覆盖。
380. **`TooltipStateSmoke.lua:24/30/41/61/67` 断言的行数与生成目录强绑定（40192 恰 1 行、107516 恰 3 行、127170 恰 2 行）**；第 27 行手工清空私有字段 `__yiboMountsSignature` 来模拟重置（测试实现而非行为）；第 28/65 行是死准备代码；**第 44/58 行设全局 `UnitBuff`/`UnitExists` 且不还原**；**第 46 行把 spellID 放在第 11 个返回值（正是 `Tooltip.lua:53` 的假设）→ 第 57–69 行的两条启发式回退从未执行**；**第 63–67 行"不支持单位保持不变"断言 `#lines == 3`，而第 30 行已产生 3 行 → 即使调用无效断言同样通过（弱断言）**；`Initialize()` 从未被调用（hook 装配零覆盖）。
381. **`FormatterSmoke.lua` 的 19 条断言全是"生成数据 + 格式化器"的联合快照断言**（如 `"Drop: 风神王座 > 奥拉基尔"`），目录重新生成或文案微调即失效，且**无法区分数据变更与逻辑变更**；期望值在同一条字符串里混用中英文（因 `GetLocale` 被桩为 enUS，说明**生成数据的 `enUS` 字段里存了中文**，属数据质量缺陷被测试固化）。
382. `FormatterSmoke.lua` 第 17/22/26 等行调用 `GetBySpellID` 后**从不断言记录存在** → 记录消失时会走到 `SourceFormatter.lua:58` 抛 Lua 错误而非清晰断言失败（正是生产代码缺 nil 守卫的体现）。
383. `FormatterSmoke.lua:68-91` 的内联 fixture 覆盖了拍卖行降级但**未覆盖 `priority` 分支与 `tostring(sourceID)` 字节序分支**；`eventRecord`/`conciseRecord`/`researchRecord` 三条边界用例设计良好（正面）；**完全未覆盖 `costs` 的 money/currency/item 三支、难度键回退、`STANDING_` 拼接、`status == "rejected"`** → 上述"内部格式泄漏"缺陷因此无人拦截；`Locale/zhCN.lua` 被加载却从未激活，中文路径零本地化测试。

---

## 十一、待审插件

**无 —— 8 个插件（YiboTodo、YiboBeastPaths、YiboAutoOpen、YiboQuestBlocker、YiboReputation、YiboCurrency、YiboBuilds、YiboMounts）已全部审查完毕。**

---

## 十二、优先级建议（按修复收益排序）

### 本轮新增（YiboReputation / YiboCurrency / YiboBuilds / YiboMounts）最高优先级

- **数据/设置被静默破坏**：`YiboReputation/Bootstrap.lua:24/27/28`（读设置即清空 4 个字段并覆盖 `hoverMode`/`defaultMatrixFilter`）；`YiboCurrency/Bootstrap.lua:25`（版本字段非数字时监控列表被反复清空）。
- **UI 泄漏与重名**：`YiboMounts/SourceFactProbe.lua:6/32`（函数体内创建固定具名 `GameTooltip`，每次调用泄漏一帧并触发客户端改名）；`YiboBuilds/Snapshot.lua:65/179`（同类）；`YiboReputation/AccountMatrixView.lua:108-119`（`_matrixNodeCaches` 只增不减）。
- **玩家可见的错乱输出**：`YiboMounts/SourceFormatter.lua:124/126`（`"currency:392"` 直接进 tooltip）、`:61/100/111`（`"boss_drop"`/`"TEN"`/`"STANDING_XXX"` 泄漏）；`YiboBuilds/AccountPage.lua:838`（未定义变量 `slot` 导致预览槽位列恒显示"主天赋"）。
- **功能整体失效**：`YiboCurrency/Settings.lua:27`（两栏布局永不生效）；`YiboBuilds/Snapshot.lua:218`（`GetNumGlyphs` 在 MoP 已移除 → 雕文目录恒空）；`YiboMounts/Bootstrap.lua:7`（schemaVersion 不匹配即静默全失效）。
- **死模块/死代码**：`YiboReputation/CurrentCharacterPreview.lua`（整个模块死代码）；`YiboReputation/PreviewController.lua:26-45/65-89`（约 39 行死代码 + 死控件）；`YiboMounts/Namespace.lua:24` `SafeCall`、`YiboBuilds/CoreIntegration.lua:57-59`、`YiboBuilds/Snapshot.lua:22-24`（死函数）；`YiboMounts/Locale` 的 `PRICE`/`ALLIANCE`/`HORDE` 死键。
- **参数错位**：`YiboMounts/Tooltip.lua:100/104`（`present` 布尔被当作 `source` 字符串）。
- **发布包违规**：`YiboMounts/Data/SourceFacts.generated.lua`（自述 NON-RELEASE 却随生产加载）。
- **约定缺失**：三个新插件的 `数据与缓存` 分区缺失（`YiboBuilds`/`YiboMounts` 连设置页都没有）；`YiboBuilds`/`YiboCurrency` 的入口 `text` 缺 `|cff20e070[Yibo]|r`；`YiboReputation/Commands.lua:5` 保留旧别名 `/yrp tree`。

1. **功能性 bug（会误操作/丢数据）**：`YiboAutoOpen/Commands.lua:11` 的删除误操作；`YiboQuestBlocker_Core.lua:354-363` 的 `processMode` 被转成布尔；`YiboAutoOpen/Catalog.lua:5` 的空迁移表；`YiboQuestBlocker_Core.lua:1384-1398` 的 `SelectAvailableQuest` 参数误判。
2. **静默失效类**：`YiboAutoOpen/Queue.lua:108` 的 `_SHOW$` 永久暂停；`YiboAutoOpen/Safety.lua` 无 `Reset()`；`YiboBeastPaths/Renderer.lua:721` 参考层变换永不生效；`YiboQuestBlocker_Core.lua:924` 的中文任务判断。
3. **安全/合规**：`YiboQuestBlocker_Core.lua:773-790` 缺战斗锁定检查；`ConfirmVisibleAbandonPopup:732-744` 程序化点击系统弹窗；`AccountPage.lua:321-323` 破坏性操作无确认；`Safety.lua` fail-open。
4. **性能**：`YiboBeastPaths` 的每帧/每 0.05s 全量重建；`YiboQuestBlocker` 的逐任务 O(角色) 遍历；`YiboTodo/Catalog/Validator.lua:107` 的重复全量校验。
5. **约定一致性**：打印前缀、`RequiredDeps`、`X-GameFlavor`、`Interface` 版本、`数据与缓存` 分区、死文件与 `dist/` 清理。

---

## 十三、正面设计（修复时应保留，勿一并改掉）

1. **`YiboMounts` 是唯一建立了本地化体系的插件**：`Namespace.lua:8-22` 的 `GetLocaleTable`/`L`/`GetLocalizedText` 三级兜底，`Locale/enUS.lua` 与 `Locale/zhCN.lua` 键集合对齐，`SourceFormatter.lua:5-33` 用 locale 键间接层而非内联文案 —— 应作为其余 7 个插件的本地化改造模板。
2. **`YiboBuilds/CoreIntegration.lua:14-26` 是唯一注册 `Core.CharacterCleanup` 的插件** —— 角色删除时的缓存清理机制应推广到 `YiboCurrency`/`YiboReputation`/`YiboQuestBlocker`/`YiboTodo`。
3. **`YiboBuilds/Bootstrap.lua:30-43` 是首个真正实现 AGENTS.md `previewColumns` 预览列投影的插件**（`GetPreviewFields`/`SetPreviewFieldVisible` 成对提供，`AccountPage.lua:161-185` 由同一字段偏好生成悬停列）—— 方向正确，应作为标准做法（`YiboCurrency` 与 `YiboReputation` 均因缺失该机制而被记入问题清单）。
4. **`YiboBuilds/Snapshot.lua:348-355` 的 `captureToken` 取消令牌**：延迟抓取前的 token 比对避免了过期回调生效 —— 应推广到 `YiboBuilds/Bootstrap.lua:90-97` 的裸 `C_Timer.After`。
5. **`YiboCurrency/Data.lua:103-108` 与 `YiboMounts/Catalog.lua:10/16/24/32`、`YiboBuilds/Snapshot.lua:34-45` 的类型守卫**：`type(x) == "number"`/`"table"`/`"function"` 的显式校验是正确做法（对照 `YiboAutoOpen/BagAdapter.lua` 与 `YiboCurrency/Data.lua:107` 的缺失）。
6. **`YiboBuilds/AccountPage.lua:675-678` 用 `< 1` 判定处理 `GetWidth()` 返回 `0`** —— 全项目唯一正确写法，应替换所有 `GetWidth() or N` 失效兜底。
7. **`YiboMounts/InventoryProbe.lua:48` 的排序比较函数**（键已由第 27 行保证为数值）与 `:62` 用 `!` 前缀取 UTC 时间戳 —— 是 `Catalog.lua:44-57` 与项目内时钟混用问题的正确对照写法。
8. **`YiboQuestBlocker.toc:8-9`、`YiboCurrency.toc:8-9`、`YiboReputation.toc:8-9`、`YiboBuilds.toc:8-9` 的 `RequiredDeps: YiboCore` + `Group: YiboCore`** 与 `YiboMounts.toc:3-4` 的 `Notes`/`Notes-zhCN` 成对声明 —— 约定合规样例。
9. **`YiboMounts/Bootstrap.lua:3/6` 的匿名帧 + 提前返回写法**（无全局名、无 UI 重名风险）与 `YiboBuilds/AccountPage.lua:527-537` 的 `buildsTooltipBound` 一次性绑定标记 —— 避免了逐帧重复 `HookScript`。
10. **`YiboBuilds/Snapshot.lua:11-13` 与 `YiboCurrency/Data.lua:49-50` 的设计注释**、`YiboMounts/SourceFormatter.lua:132-133`（notes 不得泄漏到 UI）、`YiboMounts/SourceFactProbe.lua:58-59`（记录 `GetSpellInfo` 陷阱）、`YiboReputation/Friendship.lua:11-13`（仅在 Core 真正采集到友度数据时才视作友度阵营）、`YiboBuilds/AccountPage.lua:20-22/180-182/315-317` —— 注释解释了"为什么"，是本次审查中质量最高的部分。

---

## 十四、上帝文件 / 上帝函数（规模与职责集中度）

> 判定口径：**上帝文件** = 单文件承担 ≥4 个可独立拆分的子系统；**上帝函数** = 单函数 ≥150 行。
> 数据来源：`wc -l` + `grep -c '^\s*(local )?function'` + 相邻函数定义行距估算最长函数体。
> 注意：`YiboCore`、`YiboAltoBoss`、`YiboLegendary` **尚未逐行审查**，本节对这三个插件仅按规模与函数名抽样给出，不构成逐行结论。

### 14.1 规模排名（排除 `Libs/`、`dist/`、`tmp/` 打包副本）

| 文件 | 行数 | 函数定义数 | 最长函数（起始行） | 判定 |
|---|---|---|---|---|
| `YiboMounts/Data/MountCatalog.generated.lua` | 37,572 | 0 | — | **生成数据，非代码**（单个表字面量，无控制流） |
| `YiboCore/UI/AccountView.lua` | 3,006 | 128 | ~196 行（2488） | **上帝文件（最严重）** |
| `YiboBeastPaths/Maintenance/DebugCalibrator.lua` | 2,916 | 78 | **~541 行（2033）** | **上帝文件 + 上帝函数** |
| `YiboAltoBoss/Core.lua` | 2,916 | 153 | ~97 行（2361） | **上帝文件**（未逐行审查） |
| `YiboQuestBlocker/YiboQuestBlocker_Core.lua` | 1,492 | 58 | ~167 行（236） | **上帝文件** |
| `YiboBeastPaths/Renderer.lua` | 1,287 | 42 | ~68 行（310） | 偏大，职责尚单一（渲染） |
| `YiboAltoBoss/Settings.lua` | 1,162 | **25** | **~396 行（568）** | **上帝函数**（未逐行审查） |
| `YiboBeastPaths/RouteResolver.lua` | 969 | 48 | — | 可接受 |
| `YiboBuilds/AccountPage.lua` | 920 | 36 | ~116 行（222） | **上帝文件** |
| `YiboAltoBoss/AccountPage.lua` | 864 | 48 | ~115 行（535） | 偏大（未逐行审查） |
| `YiboBeastPaths/MinimapRenderer.lua` | 824 | 27 | — | 可接受 |
| `YiboCore/UI/Theme.lua` | 766 | 38 | ~52 行（487） | 可接受 |
| `YiboTodo/AccountPage.lua` | 635 | 28 | ~181 行（399） | 含上帝函数 |
| `YiboLegendary/UI.lua` | 589 | 50 | ~60 行（475） | 可接受 |
| `YiboQuestBlocker/AccountPage.lua` | 506 | — | — | 可接受 |
| `YiboMounts/Tooltip.lua` | 129 | 8 | — | 良好 |

### 14.2 `YiboCore/UI/AccountView.lua`（3,006 行 / 128 函数）—— 单文件 10 个子系统

同目录 `Theme.lua`（766）与 `Entry.lua`（529）说明 Core 本有分文件意识，但 AccountView 吞掉了"外壳 + 工作台 + 三个内置页面"：

| # | 子系统 | 代表函数 |
|---|---|---|
| 1 | 窗口外壳与尺寸 | `CreateFrame`(944)、`ApplyPageSize`(1091)、`ApplyMeasuredPageHeight`(1136)、`ApplyNormalLayout`(1418)、`ClampWindowSize`(279)、`ScreenBounds`(266)、`ShellMetrics`(271)、`SurfaceMetrics`(287) |
| 2 | 页面注册与生命周期 | `RegisterPage`(421)、`UnregisterPage`(522)、`ShowPage`(1325)、`RefreshPage`(1388)、`NotifyPageChanged`(1404)、`CallPage`(550)、`PageError`(538) |
| 3 | 列分页 | `GetColumnPage`(130)、`GetColumnPageByWidth`(168)、`SetColumnPage`(194)、`UpdateColumnPager`(207)、`HideColumnPagers`(230)、`FitRepeatedColumnWidth`(115) |
| 4 | 字段可见性与预览列 | `GetFieldVisible`(559)、`SetFieldVisible`(569)、`GetPreviewFieldVisible`(596)、`GetVisibleFields`(601) |
| 5 | 角色排序（13 个函数） | `GetDefaultCharacterSort`(619)、`GetPageCharacterSort`(623)、`GetEffectiveCharacterSort`(628)、`SetDefaultCharacterSort`(639)、`SetPageCharacterSort`(645)、`ResetPageCharacterSort`(653)、`GetCustomCharacterOrder`(660)、`MoveCustomCharacter`(674)、`RebuildCustomCharacterOrder`(682)、`NextSortMode`(695)、`DefaultDirection`(702)、`CycleCharacterSort`(706)、`UpdateSortButton`(722) |
| 6 | 范围切换 | `GetScopeDefinition`(736)、`IsKnownScope`(765)、`GetPageScope`(772)、`SetPageScope`(781)、`SetCharacterHidden`(807)、`RefreshScopeBar`(835) |
| 7 | **统一设置工作台（整站实现）** | `CreateSettings`(2015)、`SettingsRow`(2027)、`PlaceSettingsRow`(2098)、`CreateHostedSettingsSection`(2106)、`RefreshSettings`(2124)、`Heading`(2168)、`Check`(2172)、`Button`(2177)、`Input`(2183)、`Save`(2190)、`Dropdown`(2207)、`BusinessEntryRow`(2220)、`AddonPanel`(2265)、`SortControls`(2310)、`CharacterOrderRows`(2344)、`ShortNameRows`(2412)、`Save`(2432)、`CycleArchiveProfile`(2488) |
| 8 | **内置页面：概览 / 角色与归档 / 关于** | `CreateOverview`(1691)、`RefreshOverview`(1704)、`CollectPageValue`(1709)、`CreateCharacters`(1793)、`ShowCharacterDeleteConfirmation`(1840)、`ArchiveCharacters`(1874)、`GetArchiveColumnWidths`(1888)、`GetArchiveTableWidth`(1900)、`RefreshCharacters`(1909)、`ArchiveSettings`(27)、`GetArchiveFields`(58)、`CreateAbout`(2797)、`CreateAboutAddonRow`(2731)、`RefreshAbout`(2921) |
| 9 | 悬停预览 | `HoverMetrics`(1466)、`TrackPreviewControls`(1518)、`Track`(1519)、`Visit`(1529)、`ShowPreview`(1537)、`HidePreview`(1610)、`Toggle`(1626)、`GetPreviewPage`(1442) |
| 10 | 导航与标题栏 | `RefreshNavigation`(1163)、`NavigationRequiredHeight`(1077)、`SetTitleBarControl`(1231)、`ClearTitleBarControl`(1224)、`SetHeaderIdentity`(345) |

附带缺陷：
- **第 2190 行与第 2432 行各有一个 `local function Save(control)`**（间距 242 行）。经核验二者嵌套层级不同（8 空格 / 12 空格），**不构成遮蔽 bug**，但同文件内复用同名局部函数属可维护性隐患。
- 最长函数 `CycleArchiveProfile`（2488 行起，约 196 行）。
- 第 5 项"角色排序"13 个函数与 `YiboCore/Util/CharacterSort.lua`（173 行）职责重叠。

### 14.3 `YiboBeastPaths/Maintenance/DebugCalibrator.lua`（2,916 行 / 78 函数）—— 上帝函数之最

- **最长单个函数约 541 行（第 2033 行起）**，是全项目最长的函数体。
- 单文件内含：调试面板、布局（三份重复的元素清单：`sectionContents` / `sectionLayout[*].movableElements` / `autoHeightElements`）、`SerializeLua`（无环/深度保护）、导出、路线/节点/步进校准、小地图、预设、tooltip、`StaticPopupDialogs` 确认框、**自带的 `CopyDefaults` 第 5 份**、**自带的 `|cff4fd8ff[YBP调试]|r` 前缀色**（全项目第 5 种前缀色）。

### 14.4 `YiboQuestBlocker/YiboQuestBlocker_Core.lua`（1,492 行 / 58 函数）

单文件覆盖：DB 与迁移（无版本门禁）、默认值（与 `AccountPage` 重复定义）、等级表达式解析、**三套近乎重复的任务日志扫描实现**（1070-1109 / 1111-1133 / 1135-1148）、自动化适配器、自动放弃状态机（6 个模块级可变标志）、`StaticPopup_OnClick` 程序化点击、`AcceptQuest` 钩子、`C_GossipInfo` 选择守卫、诊断、事件注册（11 个 `RegisterEvent` 无 `pcall`）、Slash 命令。最长函数约 167 行（第 236 行）。

### 14.5 `YiboBuilds/AccountPage.lua`（920 行 / 36 函数）

单文件承担：五个面板的 UI 构建（`Page.Create` 116 行）、花名册行、悬停预览模式（`RefreshPreview` 82 行）、**装备纸娃娃 + `DressUpModel` 拖拽/滚轮交互**、雕文目录渲染（`RenderCatalog`）、构筑列（`RenderBuildColumns`）、主按钮与确认弹窗（`SetupMainButtons`）、布局（`LayoutMain`）、刷新（`Page.Refresh` 78 行）、surface metrics、hover metrics。**约 10 个视图状态字段直接存在共享页面帧上。**

### 14.6 明确**不是**上帝文件的

- **`YiboMounts/Data/MountCatalog.generated.lua`（37,572 行）**：单个表字面量，无控制流，属生成数据。其真实问题是"每次登录全量解析"与"`provisional-` 占位记录进生产"，**不是职责集中**。
- **拆分良好的样本**：`YiboCurrency`（Catalog 75 / Data 211 / AccountPage 288 / Settings 72 / CoreIntegration 53）、`YiboReputation`（14 个文件各司其职）、`YiboTodo`（`Model/` + `Providers/` + `Catalog/` 分层）、`YiboBeastPaths`（Renderer / RouteResolver / Transforms / Overlay 分离）、`YiboMounts`（Locale / Catalog / SourceFormatter / Probe / Tooltip 分离）、`YiboCore` 的 `Data/` 与 `Runtime/` 层（多数文件 < 200 行）。
  → 结论：**项目整体有正确的分层意识，上帝文件集中在少数几个"历史累积"文件上，不是全局性问题。**

### 14.7 建议的拆分方向（仅建议，未改动代码）

| 文件 | 建议拆分 |
|---|---|
| `YiboCore/UI/AccountView.lua` | `AccountView/Shell.lua`（外壳/尺寸/布局/导航）、`Paging.lua`（列分页）、`Sorting.lua`（复用 `Util/CharacterSort.lua`）、`Scope.lua`、`Preview.lua`（悬停）、`Settings/Workbench.lua`（工作台整站）、`Pages/Overview.lua`、`Pages/Characters.lua`（含归档）、`Pages/About.lua` |
| `YiboBeastPaths/Maintenance/DebugCalibrator.lua` | 先把 541 行函数拆为"布局"与"交互"两半；再按 `Panel.lua` / `Layout.lua` / `Serialize.lua` / `Calibrate.lua` / `Minimap.lua` / `Export.lua` 拆分 |
| `YiboQuestBlocker_Core.lua` | `Database.lua`（含版本门禁）、`LevelExpr.lua`、`QuestScan.lua`（**先合并三套重复扫描**）、`Automation.lua`、`Diagnostics.lua`、`Events.lua` |
| `YiboAltoBoss/Core.lua` | `WorldBoss.lua`（相位/击杀）、`Characters.lua`（角色/服务器排序）、`View.lua`（视图模式/过滤/悬停）、`Migration.lua`（含 `RepairV15WorldBossVisibility`） |
| `YiboBuilds/AccountPage.lua` | `Page.lua`（外壳与刷新）、`Equipment.lua`（纸娃娃 + 模型）、`GlyphCatalog.lua`、`Preview.lua`、`Metrics.lua` |
| `YiboAltoBoss/Settings.lua` | 拆分 568 行的单个函数 |

### 14.8 仓库卫生（与规模问题同源）

`tmp/` 下存在 **3 套打包副本**（`tmp/YiboAltoBoss-v2.1.1-stage/{github,curseforge}/...`、`tmp/YiboQuestBlocker-package-<hash>/{github,curseforge}/...`），使同一文件在规模统计中重复出现 2–3 次；`YiboQuestBlocker/dist/` 另有 3 套 `1.2.x` 陈旧构建。建议在统计与审查中排除，并清理（参见第五节第 113/117 条）。
