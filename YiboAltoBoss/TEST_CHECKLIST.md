# YiboAltoBoss 测试清单

用途：用于逐条验证 `YiboAltoBoss` 当前版本的功能、交互、数据准确性与稳定性。

建议使用方式：
- 每完成一项，就在对应条目前打勾，或在“结果记录”里写下现象。
- 如果发现问题，尽量补充“复现步骤 / 当前角色 / 当前服务器 / 是否重载过 / 是否跨角色”。

记录约定：
- 状态：`[ ] 未测` `[-] 进行中` `[x] 通过` `[!] 异常`
- 结果记录：可直接填写“通过 / 不通过 / 偶现 / 待复测”等。

---

## 1. 顶部入口交互

### 1.1 Broker 左键
- [x] 通过
- 测试步骤：左键点击 broker 图标。
- 预期结果：打开或关闭当前服务器视图。
- 结果记录：通过。可正常打开或关闭当前服务器视图。

### 1.2 Broker Shift + 左键
- [x] 通过
- 测试步骤：按住 `Shift` 后左键点击 broker 图标。
- 预期结果：打开或关闭所有服务器视图。
- 结果记录：通过。可正常打开或关闭所有服务器视图。

### 1.3 Broker 右键
- [x] 通过
- 测试步骤：右键点击 broker 图标。
- 预期结果：打开设置页。
- 结果记录：通过。可正常打开设置页。

### 1.4 Broker 悬停
- [x] 通过
- 测试步骤：鼠标悬停 broker 图标。
- 预期结果：显示临时窗口。
- 结果记录：通过。悬停可正常显示临时窗口。

### 1.5 Broker Shift + 悬停
- [x] 通过
- 测试步骤：按住 `Shift` 后悬停 broker 图标。
- 预期结果：显示所有服务器临时窗口。
- 结果记录：通过。按住 `Shift` 悬停时可正常显示所有服务器临时窗口。

### 1.6 小地图图标交互一致性
- [x] 通过
- 测试步骤：对小地图图标重复执行左键、`Shift+左键`、右键、悬停、`Shift+悬停`。
- 预期结果：与 broker 行为完全一致。
- 结果记录：通过。小地图图标交互与 broker 行为一致。

### 1.7 fallback 小地图按钮交互一致性
- [x] 通过
- 测试步骤：在 broker 初始化失败或手动验证 fallback 时，重复执行全部交互。
- 预期结果：与 broker、小地图图标行为完全一致。
- 结果记录：通过。fallback 小地图按钮交互与 broker、小地图图标行为一致。

---

## 2. 主窗口与悬停窗口

### 2.1 点击与悬停不冲突
- [x] 通过
- 测试步骤：先悬停，再点击图标；先点击打开主窗口，再移过图标。
- 预期结果：不会同时出现两个同类窗口；主窗口与悬停窗口行为清晰、不互相干扰。
- 结果记录：通过。点击与悬停行为不冲突，不会同时出现两个同类窗口。

### 2.2 悬停窗口位置
- [x] 通过
- 测试步骤：分别在屏幕左上、右上、小地图附近测试悬停。
- 预期结果：悬停窗口位置正常，不与入口图标重叠，不跑到屏幕外。
- 结果记录：通过。悬停窗口位置正常，不与入口图标重叠，也不会跑到屏幕外。

### 2.3 悬停窗口收起
- [x] 通过
- 测试步骤：悬停显示后移开鼠标。
- 预期结果：窗口按预期延迟收起，不残留。
- 结果记录：通过。悬停窗口可按预期延迟收起，无残留。

### 2.4 主窗口基础操作
- [x] 通过
- 测试步骤：测试拖动、关闭、再次打开、右下角缩放。
- 预期结果：窗口操作正常，无遮挡、无错位。
- 结果记录：通过。主窗口拖动、关闭、再次打开与右下角缩放均正常。

---

## 3. 视图切换

### 3.1 当前服务器视图
- [x] 通过
- 测试步骤：打开当前服务器视图。
- 预期结果：只显示当前服务器角色。
- 结果记录：通过。当前服务器视图显示正确。

### 3.2 所有服务器视图
- [x] 通过
- 测试步骤：打开所有服务器视图。
- 预期结果：显示所有已缓存角色。
- 结果记录：通过。所有服务器视图显示正确。

### 3.3 列头显示规则
- [x] 通过
- 测试步骤：对比当前服务器视图与所有服务器视图。
- 预期结果：当前服务器视图不重复显示服务器名；所有服务器视图显示 `角色名-服务器名`。
- 结果记录：通过。列头显示规则符合预期。

### 3.4 顶部标题与按钮状态
- [x] 通过
- 测试步骤：在当前服务器 / 所有服务器之间切换。
- 预期结果：顶部按钮选中态、标题、副标题都正确切换。
- 结果记录：通过。顶部标题与按钮状态切换正确。

---

## 4. Boss 排序与布局

### 4.1 Boss 显示顺序
- [x] 通过
- 测试步骤：检查上半区和下半区的 Boss 顺序。
- 预期结果：从上到下依次为 `纳拉克 -> 乌达斯塔 -> 炮舰 -> 怒之煞`。
- 结果记录：通过。Boss 显示顺序正确。

### 4.2 布局紧凑性
- [x] 通过
- 测试步骤：在少角色、多角色、多位面情况下查看整体布局。
- 预期结果：界面紧凑、清晰，无明显过宽列、无横向滚动条。
- 结果记录：通过。当前效果可接受，但可能与最初设计不完全一致。

### 4.3 多角色时列宽表现
- [x] 通过
- 测试步骤：缓存多个角色后查看上半区。
- 预期结果：角色列宽度自适应，信息可读，不明显浪费横向空间。
- 结果记录：通过。多角色时列宽表现正常。

---

## 5. 角色缓存

### 5.1 当前角色缓存
- [x] 通过
- 测试步骤：登录当前角色后打开窗口。
- 预期结果：当前角色名、服务器名、等级已被缓存。
- 结果记录：通过。当前角色缓存正常。

### 5.2 跨角色缓存
- [x] 通过
- 测试步骤：切换同服其他角色登录后查看。
- 预期结果：旧角色仍保留，新角色被加入缓存。
- 结果记录：通过。跨角色缓存正常。

### 5.3 跨服务器缓存
- [x] 通过
- 测试步骤：切换其他服务器角色登录后查看所有服务器视图。
- 预期结果：历史服务器与历史角色仍保留，显示正确。
- 结果记录：通过。跨服务器缓存正常。

### 5.4 重登后缓存保留
- [x] 通过
- 测试步骤：退出游戏后重登。
- 预期结果：角色缓存不丢失。
- 结果记录：通过。重登后角色缓存保留正常。

---

## 6. 等级过滤

### 6.1 输入 `90`
- [x] 通过
- 测试步骤：在设置页输入 `90`。
- 预期结果：只显示 90 级角色列。
- 结果记录：通过。

### 6.2 输入 `1-20`
- [x] 通过
- 测试步骤：在设置页输入 `1-20`。
- 预期结果：只显示 1 到 20 级角色列。
- 结果记录：通过。

### 6.3 输入 `<=3`
- [x] 通过
- 测试步骤：在设置页输入 `<=3`。
- 预期结果：只显示 3 级及以下角色列。
- 结果记录：通过。

### 6.4 输入 `>=85`
- [x] 通过
- 测试步骤：在设置页输入 `>=85`。
- 预期结果：只显示 85 级及以上角色列。
- 结果记录：通过。

### 6.5 输入 `<=3,89,90`
- [x] 通过
- 测试步骤：在设置页输入 `<=3,89,90`。
- 预期结果：显示满足任一条件的角色列。
- 结果记录：通过。

### 6.6 输入空值
- [x] 通过
- 测试步骤：清空等级过滤。
- 预期结果：恢复显示全部等级角色。
- 结果记录：通过。

### 6.7 输入 `0`
- [x] 通过
- 测试步骤：输入 `0`。
- 预期结果：等同于不过滤。
- 结果记录：通过。

### 6.8 错误格式
- [x] 通过
- 测试步骤：输入错误格式，例如 `abc`、`>==90`、`1,,2`。
- 预期结果：有明确错误提示，不会导致界面异常。
- 结果记录：通过。

### 6.9 下半区不受等级过滤影响
- [x] 通过
- 测试步骤：切换不同等级过滤表达式后观察下半区。
- 预期结果：下半区位面刷新矩阵不应被等级过滤影响。
- 结果记录：通过。

---

## 7. 上半区击杀记录

### 7.1 自己补刀
- [x] 通过
- 测试步骤：由当前角色自己完成击杀。
- 预期结果：对应 Boss 记录正确写入当前角色列。
- 结果记录：通过。结合当前记录表现判断正常。

### 7.2 队友补刀
- [x] 通过
- 测试步骤：组队时由队友完成最后一击。
- 预期结果：当前角色仍能正确记录击杀。
- 结果记录：通过。结合当前记录表现判断正常。

### 7.3 未选中 Boss 时击杀
- [x] 通过
- 测试步骤：击杀时不保持 Boss 为当前目标。
- 预期结果：不会因没选中 Boss 而漏记。
- 结果记录：通过。此前出现过的漏记情况已被自动补录。

### 7.4 切目标时击杀
- [x] 通过
- 测试步骤：击杀前后快速切换目标。
- 预期结果：不会因切目标而漏记。
- 结果记录：通过。结合当前记录表现判断正常。

### 7.5 击杀后立即查看
- [x] 通过
- 测试步骤：击杀后马上打开窗口。
- 预期结果：对应格子应已更新。
- 结果记录：通过。当前记录表现正确。

### 7.6 重载后查看
- [x] 通过
- 测试步骤：击杀后 `/reload` 再查看。
- 预期结果：击杀记录仍存在。
- 结果记录：通过。当前记录表现正确。

### 7.7 重登后查看
- [x] 通过
- 测试步骤：击杀后退出再登录查看。
- 预期结果：击杀记录仍存在。
- 结果记录：通过。当前记录表现正确。

---

## 8. quest 兜底补录

### 8.1 登录时补录
- [x] 通过
- 测试步骤：本周已击杀世界 Boss 后，重新登录角色。
- 预期结果：已击杀 Boss 可通过官方 quest 状态补录回来。
- 结果记录：通过。补录正常，之前漏掉的记录也已补上。

### 8.2 进世界时补录
- [x] 通过
- 测试步骤：切地图、进出位面或重新进入世界。
- 预期结果：可触发一次 quest 兜底校验。
- 结果记录：通过。补录正常。

### 8.3 主窗口打开时补录
- [x] 通过
- 测试步骤：在记录未显示时，主动打开主窗口。
- 预期结果：如果 quest 状态已完成，可在此时补录。
- 结果记录：通过。补录正常。

### 8.4 QUEST_LOG_UPDATE 补录
- [x] 通过
- 测试步骤：观察任务状态更新后是否同步击杀。
- 预期结果：任务状态变化时可触发补录。
- 结果记录：通过。补录正常。

---

## 9. 下半区位面 ID 与刷新记录

### 9.1 位面列为真实位面 ID
- [!] 异常
- 测试步骤：观察下半区列头。
- 预期结果：显示实际位面 ID，而不是错误的角色标识或固定 `0`。
- 结果记录：当前看起来有问题，需继续测试确认。

### 9.2 同位面跨角色共享
- [!] 异常
- 测试步骤：用不同角色在同一位面观测或击杀。
- 预期结果：共同维护同一位面列，而不是拆成多个角色列。
- 结果记录：当前看起来有问题，需继续测试确认。

### 9.3 不同位面区分
- [!] 异常
- 测试步骤：在多个不同位面分别观测或击杀。
- 预期结果：应拆分为不同位面列。
- 结果记录：存在“换地图会被认为是两个位面”的现象，需继续测试。

### 9.4 观测状态
- [!] 异常
- 测试步骤：只观测 Boss，不完成击杀。
- 预期结果：对应格子显示 `观测` 或相应观测状态。
- 结果记录：当前看起来有问题，需继续测试确认。

### 9.5 击杀后倒计时
- [-] 进行中
- 测试步骤：在某位面击杀 Boss 后查看下半区。
- 预期结果：对应格子显示刷新倒计时。
- 结果记录：目前可能尚未预测出刷新规律，当前显示为正计时，待观察。

### 9.6 可刷状态
- [-] 进行中
- 测试步骤：等待刷新时间到达后查看。
- 预期结果：对应格子显示 `就绪`。
- 结果记录：待刷新规律形成后继续观察。

### 9.7 未知状态
- [-] 进行中
- 测试步骤：查看没有记录的位面格子。
- 预期结果：显示 `?`。
- 结果记录：当前相关状态仍需继续观察。

---

## 10. 6 小时隐藏规则

### 10.1 超时自动隐藏
- [-] 进行中
- 测试步骤：等待位面记录超过 6 小时。
- 预期结果：旧记录自动隐藏。
- 结果记录：时间较长的位面记录会消失，但是否严格为 6 小时仍需观察。

### 10.2 重新观测后恢复
- [-] 进行中
- 测试步骤：超时隐藏后重新观测同位面。
- 预期结果：记录重新显示。
- 结果记录：需继续观察。

---

## 11. 设置页

### 11.1 设置页层级
- [x] 通过
- 测试步骤：打开主窗口后再打开设置页。
- 预期结果：设置页层级正确，不会被主窗口压住。
- 结果记录：通过。

### 11.2 自定义 NPC 仅在设置页
- [x] 通过
- 测试步骤：检查主窗口与设置页。
- 预期结果：自定义 NPC 输入只出现在设置页。
- 结果记录：通过。

### 11.3 小地图显示开关
- [x] 通过
- 测试步骤：在设置页切换“小地图入口”开关。
- 预期结果：入口能正确显示/隐藏。
- 结果记录：通过。

### 11.4 悬停模式切换
- [!] 异常
- 测试步骤：切换完整模式、简易模式、关闭。
- 预期结果：悬停行为按配置变化。
- 结果记录：仅简洁模式 UI 仍有问题，其它模式正常。

### 11.5 设置页状态保留
- [x] 通过
- 测试步骤：关闭设置页后重新打开。
- 预期结果：设置项状态与文案正确保留。
- 结果记录：通过。

---

## 12. SavedVariables 与稳定性

### 12.1 击杀记录持久化
- [x] 通过
- 测试步骤：退出游戏后重登。
- 预期结果：击杀记录保留。
- 结果记录：通过。

### 12.2 位面记录持久化
- [x] 通过
- 测试步骤：退出游戏后重登。
- 预期结果：位面记录保留。
- 结果记录：通过。

### 12.3 角色缓存持久化
- [x] 通过
- 测试步骤：退出游戏后重登。
- 预期结果：角色缓存保留。
- 结果记录：通过。

### 12.4 设置持久化
- [x] 通过
- 测试步骤：修改等级过滤、悬停模式、小地图开关后退出重登。
- 预期结果：设置保持不变。
- 结果记录：通过。

### 12.5 窗口状态持久化
- [x] 通过
- 测试步骤：调整窗口大小、切换视图、关闭/打开后重登。
- 预期结果：窗口状态按预期保留。
- 结果记录：通过。

---

## 13. 异常与兼容场景

### 13.1 `/reload`
- [x] 通过
- 测试步骤：执行 `/reload` 后重新检查插件。
- 预期结果：插件正常加载，无报错，无关键数据丢失。
- 结果记录：通过。

### 13.2 切角色
- [x] 通过
- 测试步骤：切换同服或跨服角色。
- 预期结果：插件正常加载，数据不串角色。
- 结果记录：通过。

### 13.3 周重置后首次登录
- [ ] 未测
- 测试步骤：在周重置后首次登录检查。
- 预期结果：周常击杀状态按规则重置，不保留旧周击杀。
- 结果记录：需等周四维护重置 CD 后测试。

### 13.4 窗口打不开 / 设置打不开
- [x] 通过
- 测试步骤：多次重复打开主窗口和设置页。
- 预期结果：不会出现打不开、层级错误或残留旧窗口的问题。
- 结果记录：通过。

### 13.5 报错监测
- [x] 通过
- 测试步骤：完整测试过程中观察是否有 Lua 报错。
- 预期结果：无报错。
- 结果记录：通过。当前未见报错。

---

## 14. 测试结论

### 14.1 已通过项
- `1.1 - 1.7`
- `2.1 - 2.4`
- `3.1 - 3.4`
- `4.1 - 4.3`
- `5.1 - 5.4`
- `6.1 - 6.9`
- `7.1 - 7.7`
- `8.1 - 8.4`
- `11.1 - 11.3`
- `11.5`
- `12.1 - 12.5`
- `13.1 - 13.2`
- `13.4 - 13.5`
- 记录：

### 14.2 未通过项
- `9.1 - 9.4`
- `11.4`
- 记录：

### 14.3 偶现问题
- `4.2` 当前效果可接受，但可能与设计存在轻微偏差。
- 记录：

### 14.4 待复测项
- `9.5 - 9.7`
- `10.1 - 10.2`
- `13.3`
- 记录：

### 14.5 是否进入下一轮修正
- [ ] 是
- [ ] 否
- 备注：优先继续观察 `9.x` 位面识别与刷新规律，以及 `11.4` 简洁模式 UI。

---

## 15. 战争使者与显示项配置

### 15.1 设置页显示项分组
- [ ] 未测
- 测试步骤：打开设置页，检查是否存在“世界 Boss / 战争使者 / 自定义目标”分组。
- 预期结果：分组与单项开关正常显示，文案可读。
- 结果记录：

### 15.2 组开关联动
- [ ] 未测
- 测试步骤：关闭任一分组总开关。
- 预期结果：组内单项开关变灰且不可操作。
- 结果记录：

### 15.3 单项显示过滤
- [ ] 未测
- 测试步骤：只关闭某一个单项，观察主窗口上下两部分。
- 预期结果：该目标从主窗口中消失，其它目标正常保留。
- 结果记录：

### 15.4 关闭显示后停止记录
- [ ] 未测
- 测试步骤：关闭某目标显示后，再对该目标执行观测或击杀。
- 预期结果：主窗口无新增数据，该目标不再继续记录。
- 结果记录：

### 15.5 重新开启后保留旧历史
- [ ] 未测
- 测试步骤：先关闭显示，再重新开启此前已有记录的目标。
- 预期结果：旧击杀/位面/样本记录仍可恢复显示。
- 结果记录：

### 15.6 战争使者 5 地图独立归属
- [ ] 未测
- 测试步骤：分别在不同地图观测或击杀战争使者。
- 预期结果：每张地图只更新自己的那一行，不与其它地图串线。
- 结果记录：

### 15.7 战争使者同 ID 跨地图不串线
- [ ] 未测
- 测试步骤：在两张不同地图遇到相同颜色 ID 的战争使者。
- 预期结果：两次记录按地图分别归属，不会覆盖同一目标。
- 结果记录：

### 15.8 自定义目标新增后显示项同步
- [ ] 未测
- 测试步骤：在设置页添加一个新的自定义 NPC ID。
- 预期结果：当前自定义目标列表更新，显示项配置中同步出现对应单项。
- 结果记录：

## P5 永恒岛首日观测

- 分别击杀任意一位天神，确认主面板只显示一个“四天神”周常格，并显示“已击杀”。
- 检查“四天神”格悬停提示，确认包含实际击杀的天神名称和战斗日志来源。
- 确认“四天神”行的“行动”和“位面”列均为空，且不产生位面、刷新或击杀计时记录。
- 对插件安装前的漏记角色执行 `/yab celestial`，确认“四天神”当前角色格变为“已击杀”；再次执行时不得覆盖已有记录。
- 在击杀后关闭插件或更换本机 SavedVariables，再登录角色，确认服务器周常锁定标记自动恢复“四天神”和奥尔多斯的本周击杀。
- 对奥尔多斯分别验证不可接近与可击杀角色；不可接近时不得写入击杀。

## Tracking v3 刷新生命周期

### V3.1 Reducer 自测

- [ ] 执行 `/yab selftest`。
- 预期：输出“自测通过：1 sample / 180 seconds / idempotent”。

### V3.2 尸体防误记

- [ ] `/reload` 后只接近一具 Boss 尸体，不在其存活时目标、鼠标划过或参与战斗。
- 预期：角色列、行动计时、待完成周期、已完成样本均不变化。

### V3.3 活体与死亡

- [ ] 对活体 Boss 产生一次目标、鼠标或战斗证据，再等待其死亡。
- 预期：行动 Tooltip 显示“待完成周期 1”，已完成样本暂不增加。
- [ ] 执行 `/yab debug ordos`（按实际 Boss key 替换）。
- 预期：当前周期为 `Dead`，存在死亡时间，最近诊断包含 `DEATH_ACCEPTED`。

### V3.4 Reload 持久化

- [ ] 存在待完成周期时执行 `/reload`。
- 预期：待完成周期和死亡计时保留。

### V3.5 下一次刷新完成样本

- [ ] 等待同服务器、同位面的下一 spawn 出现并产生一次活体证据。
- 预期：待完成周期减少 1，已完成样本增加 1，诊断包含 `SAMPLE_COMPLETED`。

### V3.6 连续三轮

- [ ] 连续完成三次“死亡 → 下一 spawn 活体”。
- 预期：每个完整周期恰好增加一条样本，无漏记、无重复。

### V3.7 重复事件幂等

- [ ] 对同一 spawn 反复目标、鼠标划过、攻击并观察重复死亡事件。
- 预期：生命周期只有一条，死亡时间不变，样本不重复。

### V3.8 周常隔离

- [ ] 观察 `UNIT_DIED` 后检查角色列。
- 预期：角色列不因世界死亡直接改变。
- [ ] 等服务器周常锁定更新或脱战。
- 预期：角色列由周常锁定同步为已击杀；待完成周期和样本数不受影响。

### V3.9 跨服务器公共模型

- [ ] 在两个服务器间切换同一 Boss 页面。
- 预期：已完成样本、参与预测和模型窗口一致；服务器/位面、死亡计时和预计时刻独立。

### V3.10 状态转换跟踪

- [ ] 执行 `/yab trace on` 后完成一次生命周期，再执行 `/yab trace off`。
- 预期：聊天框只显示 `NEW_SPAWN`、`ALIVE_CONFIRMED`、`DEATH_ACCEPTED`、`SAMPLE_COMPLETED` 等状态转换，不逐条打印伤害事件。
