# CurseForge 发布文案

## 简介

YiboQuestBlocker 是一个用于 WoW MoP Classic / 国服 5.5.4 的任务拒绝管理插件。

它适合和自动接任务类插件配合使用：你可以把不想接的任务加入拒绝列表，插件会在接取前自动拦截，避免反复误接。

## 主要功能

- 自动拒绝已拒绝任务
- 支持全局拒绝和角色独立拒绝
- 网格界面统一管理任务与角色
- 当前任务可直接勾选加入拒绝
- 手动输入任务 ID 添加拒绝
- 手动执行“放弃已拒绝”，并带倒计时确认
- 支持已拒绝任务自动放弃队列
- 支持聊天提示开关
- 小地图图标快速开关界面
- 支持角色列自定义排序
- 支持角色表头显示密度调节
- 支持日常/普通/已完成过滤
- 支持等级表达式过滤

## 适用版本

- Mists of Pandaria Classic
- 国服 5.5.4

## 命令

- `/yqb`
- `/yiboquestblocker`

## 说明

- “日常”过滤组包含日常、周常、月常以及可重复任务
- 四风谷农场产品订单类任务会按日常处理
- 安装 Questie 后，任务过滤分类精度会更高
- 等级过滤支持 `90`、`1-20`、`<=3`、`>=85` 和英文逗号分隔的多条件组合
- 可关闭聊天提示，适合更安静地使用
- 开启自动放弃后，插件会在合适时机处理已进入任务日志的已拒绝任务
- 手动“放弃已拒绝”带有短暂倒计时，降低误触风险
- 默认静默启动，不会在进入游戏时自动弹窗

## 适合人群

- 启用了自动接任务插件的玩家
- 多角色日常玩家
- 想拒绝重复、无用或不想接任务的玩家

## 1.2.2 更新内容

- 以正式发布版本整理共享 `YiboBrokerMinimap` 入口模块
- 统一内置 Broker / LibDBIcon / fallback 小地图入口代码结构
- 保持现有小地图图标点击、悬停展开、延迟收起与拖拽定位体验不变

## 1.2.1 更新内容

- 抽离可复用的 Broker / LibDBIcon / fallback 小地图入口模块
- 保持现有小地图图标点击、悬停展开、延迟收起与拖拽定位体验不变
- 便于后续在其它插件中直接复制复用这套入口源码

## 1.2.0 更新内容

- 新增 `LibDataBroker-1.1` 与 `LibDBIcon-1.0` 兼容支持
- 缺少相关库时会自动回退到插件内置小地图按钮
- 新增账号级小地图配置，用于保存 Broker 图标状态与位置
- Broker 图标与内置小地图图标支持悬停临时展开主窗口
- 悬停展开的主窗口会贴着图标下方出现，便于直接滑入窗口操作

## 1.1.1 更新内容

- 修复游戏小幅更新后自动放弃不生效的问题
- 调整自动放弃的可放弃判断方式，提升兼容性
- 统一自动与手动放弃流程，减少行为差异
- 优化放弃确认弹窗处理

## 1.1.0 更新内容

- 新增自动放弃队列与任务日志同步逻辑
- 新增聊天提示开关
- 等级过滤升级为表达式输入模式
- “放弃已拒绝”按钮加入倒计时确认
- 角色顺序侧窗增加表头字数调节
- 补强 UI 持久化与旧配置迁移

## 建议页面补充

发布到 CurseForge 页面时，建议再补：

- 1 张主界面截图
- 1 张右侧排序窗口截图
- 1 张当前任务快速勾选示意图
