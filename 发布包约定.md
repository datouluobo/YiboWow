# YiboWow 发布包约定

## 输出目录

所有 YiboWow 插件的安装包统一输出到仓库根目录 `Builds/`。`Builds/` 根目录只保留每个插件最近一次成功打包生成的 zip；该插件此前的所有 zip 都移入 `Builds/Archive/` 存档。两个目录均已加入 `.gitignore`，不提交到 Git；插件自己的 `Builds/`、`dist/` 和 `_NonRelease/Builds/` 不再作为正式安装包目录。

## 双包规则

每个可发布插件必须生成两个包，并使用以下命名：

- `插件技术名-v版本-curseforge.zip`：CurseForge 上传包。
- `插件技术名-v版本-github.zip`：GitHub Release 包。

两个 zip 都应保留一层顶级插件目录，例如 `YiboCore/`。CurseForge 包只包含运行所需文件；GitHub 包可包含公开 README、文档、截图等资料，但不得包含 `_NonRelease/`、构建输出或本地调试数据。

## 构建脚本

构建脚本可以继续放在各插件的 `_NonRelease/Tools/` 中，但默认 `OutputRoot` 必须指向仓库根目录 `Builds/`。脚本必须先在临时目录完整生成所有安装包，成功后才将同一插件的旧 zip 移入 `Builds/Archive/`，再写入新包；不得覆盖或删除旧包。上传或发布脚本也必须从 `Builds/` 根目录选择对应渠道的包，不能依赖存档或插件目录下的旧输出目录。

当前已统一：

- `YiboCore/_NonRelease/Tools/Build-ReleasePackages.ps1`
- `YiboLegendary/_NonRelease/Tools/Build-CurseForgePackage.ps1`
- `YiboBeastPaths/_NonRelease/Tools/Build-CurseForgePackages.ps1`
